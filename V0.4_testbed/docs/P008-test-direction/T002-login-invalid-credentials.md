あなたはExecutor(実装担当)です。以下の1テストタスクだけを実施してください。

# 【テストID】T002

## 【目的】

* 誤ったパスワードでのログインが `401 AUTH_INVALID_CREDENTIALS` になり、ID/パスワードのどちらが誤りかが判別できるメッセージになっていないことを確認する。

## 【参照テスト計画】

* `docs/P006-test-plan.md` §2.1(S01行)、§2.2(POST /api/auth/login行)

## 【対象モジュール】

* `server/app/api/auth.py`、`server/app/services/auth_service.py`

## 【前提条件】対象スプリントの全モジュールビルドが成功していること

* U001が完了していること。T001のシードデータが投入済みであること。

## 【使用するテストデータ】

* `employee_id=E0001`(実在), `password=WrongPassword`(誤り)
* `employee_id=NOT_EXIST`(存在しない社員ID), `password=Passw0rd1`

## 【事前準備】

* T001と同じテスト用DBを使用する。

## 【実行手順】

1. `POST /api/auth/login` に実在する社員IDと誤ったパスワードを送信する。
2. `POST /api/auth/login` に存在しない社員IDを送信する。

## 【実行コマンド】

* `cd server && uv run pytest tests/test_api_auth.py::test_login_invalid_credentials -v`

## 【期待結果】

* 手順1・手順2ともに `401`、エラーコード `AUTH_INVALID_CREDENTIALS`、メッセージがID/パスワードいずれの誤りかを区別しない文言であること。

## 【合否判定基準】

* 両手順が期待結果どおりであればPASS。

## 【失敗時に記録する内容】

* 実際のステータスコード・エラーコード・メッセージ文言を記録する。

## 【修正禁止事項】

* アプリケーションコードを修正しないでください。テストコードをその場で都合よく変更しないでください。

## 【次タスクへ進む条件】

* 記録完了後、T003の指示を待つ。

## 重要:

* アプリケーションコードを修正しないでください。
* テスト失敗時に、その場で修正して再テストしないでください。
* テスト失敗時は、失敗内容をテスト記録に残してください。
* 次のテストタスクには進まないでください。
