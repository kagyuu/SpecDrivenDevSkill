あなたはExecutor(実装担当)です。以下の1テストタスクだけを実施してください。

# 【テストID】T003

## 【目的】

* 無効化済み(`is_active=false`)ユーザーがログインできないことを確認する。

## 【参照テスト計画】

* `docs/P006-test-plan.md` §2.1(S07行)、§2.2(POST /api/auth/login行)

## 【対象モジュール】

* `server/app/services/auth_service.py`

## 【前提条件】対象スプリントの全モジュールビルドが成功していること

* U001が完了していること。

## 【使用するテストデータ】

* シードスクリプトで無効化済みユーザーを1件投入する: `employee_id=E0099`, `password=Passw0rd1`, `is_active=false`

## 【事前準備】

* テスト用DBに上記無効化済みユーザーを投入する。

## 【実行手順】

1. `POST /api/auth/login` に `E0099`/`Passw0rd1`(正しいパスワード)を送信する。

## 【実行コマンド】

* `cd server && uv run pytest tests/test_api_auth.py::test_login_inactive_user -v`

## 【期待結果】

* `401 AUTH_INVALID_CREDENTIALS`(存在しない場合と同一のエラーとする。`docs/P003-backend-spec.md` §2)。

## 【合否判定基準】

* 期待結果どおりであればPASS。

## 【失敗時に記録する内容】

* 実際のステータスコード・エラーコードを記録する。

## 【修正禁止事項】

* アプリケーションコードを修正しないでください。

## 【次タスクへ進む条件】

* 記録完了後、T004の指示を待つ。

## 重要:

* アプリケーションコードを修正しないでください。
* テスト失敗時に、その場で修正して再テストしないでください。
* テスト失敗時は、失敗内容をテスト記録に残してください。
* 次のテストタスクには進まないでください。
