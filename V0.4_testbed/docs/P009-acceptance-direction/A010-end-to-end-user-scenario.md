あなたはReviewer Loop(実装横断レビュー担当)です。以下の1テストタスクだけを実施してください。

# 【テストID】A010

## 【目的】

* 発注者・利用者視点で、ログインから会議室予約完了・変更・取消・ログアウトまでの一連の操作が、実際の画面操作で違和感なく行えることを確認する(受入テスト相当の主要シナリオ)。

## 【参照テスト計画】

* `docs/P006-test-plan.md` §1(受入テストの目的)

## 【対象モジュール】

* アプリケーション全体(client + server)

## 【前提条件】全モジュールビルドが成功していること

* U001〜U004が完了していること。A001〜A009が完了していること(個別の技術的な確認は完了済みで、本シナリオは通し確認に専念する)。

## 【使用するテストデータ】

* 一般ユーザー1名分のログイン情報、会議室1件、参加者1名

## 【事前準備】

* ブラウザ(またはヘッドレスブラウザ)でアプリケーションを起動しアクセスする。

## 【実行手順】

1. S01でログインする。
2. S02のカレンダーで空き時間帯を確認する。
3. 空きセルをクリックしS03へ遷移、予約を作成する。
4. S02に戻り、作成した予約が表示されることを確認する。
5. 予約バーをクリックしS04へ遷移、内容を確認後キャンセルする。
6. ヘッダーの「マイ予約」からS05に遷移し、予約が表示されることを確認する。
7. S05の一覧行からS04へ遷移し、予約を取消す。
8. S02に戻り予約が消えていることを確認する。
9. ログアウトする。

## 【実行コマンド】

* `cd client && npm run test:e2e -- --grep "A010"`(E2Eテストツールの導入・設定はU001〜U004のいずれかで前提として整備されていない場合、手動操作による確認でもよい)★FIXME★ E2Eテストの実行基盤(Playwright等)の導入タスクがP007のいずれのスプリントにも明記されていない。本テストでは手動確認を代替手段として許容する。

## 【期待結果】

* 各手順が画面遷移図(`docs/P001-requirement.md` の画面遷移図)どおりに動作し、エラー表示が出ないこと。

## 【合否判定基準】

* 9手順すべてが期待結果どおりであればPASS。

## 【失敗時に記録する内容】

* どの手順で画面遷移図と異なる挙動になったかを具体的に記録する。

## 【修正禁止事項】

* アプリケーションコードを修正しないでください。

## 【次タスクへ進む条件】

* 記録完了後、全テスト(A001〜A010)完了をP009目次に反映する。P201(実装横断レビュー)の判断に引き渡す。

## 重要:

* アプリケーションコードを修正しないでください。
* テスト失敗時に、その場で修正して再テストしないでください。
* テスト失敗時は、失敗内容をテスト記録に残してください。
* 次のテストタスクには進まないでください。
