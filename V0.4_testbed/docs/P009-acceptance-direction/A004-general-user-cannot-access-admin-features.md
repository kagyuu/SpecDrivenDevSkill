あなたはReviewer Loop(実装横断レビュー担当)です。以下の1テストタスクだけを実施してください。

# 【テストID】A004

## 【目的】

* 一般ユーザーが管理者専用画面(S06・S07)およびすべての管理者専用API(rooms系のPOST/PUT/DELETE、users系の全メソッド)にアクセスできないことを横断的に確認する(P001「権限まわりは結合テストで必ず確認する」に対応するシステムテスト相当の確認)。

## 【参照テスト計画】

* `docs/P006-test-plan.md` §2.2(rooms/users行)、§3(セキュリティ観点)

## 【対象モジュール】

* `server/app/api/rooms.py`、`server/app/api/users.py`、`server/app/api/deps.py`、`client/src/App.tsx`(画面メニューの表示制御)

## 【前提条件】全モジュールビルドが成功していること

* U001〜U004が完了していること。

## 【使用するテストデータ】

* 一般ユーザー `E0001` のCookie

## 【事前準備】

* `E0001` でログインする。

## 【実行手順】

1. `POST/PUT/DELETE /api/rooms` をそれぞれ呼ぶ。
2. `GET/POST/PUT/DELETE /api/users` をそれぞれ呼ぶ。
3. フロントエンドで一般ユーザーとしてログインした際、S06・S07への遷移メニューが表示されないことを確認する。

## 【実行コマンド】

* `cd server && uv run pytest tests/acceptance/test_a004_general_user_forbidden.py -v`

## 【期待結果】

* 手順1・2: すべて `403 FORBIDDEN`。
* 手順3: メニューにS06・S07へのリンクが表示されない。

## 【合否判定基準】

* 全項目が期待結果どおりであればPASS。

## 【失敗時に記録する内容】

* 各APIの実際のステータスコード、メニュー表示状態を記録する。

## 【修正禁止事項】

* アプリケーションコードを修正しないでください。

## 【次タスクへ進む条件】

* 記録完了後、A005の指示を待つ。

## 重要:

* アプリケーションコードを修正しないでください。
* テスト失敗時に、その場で修正して再テストしないでください。
* テスト失敗時は、失敗内容をテスト記録に残してください。
* 次のテストタスクには進まないでください。
