あなたはReviewer Loop(実装横断レビュー担当)です。以下の1テストタスクだけを実施してください。

# 【テストID】A006

## 【目的】

* カレンダー画面表示に必要なAPI(`GET /api/rooms`、`GET /api/reservations`)のレスポンスタイムが、P001の非機能要件(3秒以内)を満たすことを確認する。

## 【参照テスト計画】

* `docs/P006-test-plan.md` §3(性能)

## 【対象モジュール】

* `server/app/api/reservations.py`、`server/app/repositories/reservation_repository.py`

## 【前提条件】全モジュールビルドが成功していること

* U001〜U004が完了していること。想定規模(会議室10室、1週間分の予約 約50件程度)のテストデータが投入されていること。

## 【使用するテストデータ】

* 会議室10室、1週間分(7日 × 平均7件/日程度)の予約データをシード投入する。

## 【事前準備】

* シードスクリプトで上記データ量を投入する。

## 【実行手順】

1. `GET /api/rooms` のレスポンスタイムを計測する(10回実行し平均を取る)。
2. `GET /api/reservations?date_from=<週初め>&date_to=<週末>` のレスポンスタイムを計測する(10回実行し平均を取る)。

## 【実行コマンド】

* `cd server && uv run pytest tests/acceptance/test_a006_performance.py -v`

## 【期待結果】

* 手順1・2ともに、平均レスポンスタイムが1秒未満であること(フロントエンドの描画時間を考慮し、API単体では3秒の目標に対し十分な余裕を持たせる基準として1秒未満とする)。★FIXME★ 「3秒以内」はエンドツーエンド(API+描画)の目標値であり、API単体の合格基準はP001/P006に明記がないため、本テストで独自に1秒未満という基準を設定した。

## 【合否判定基準】

* 両手順の平均レスポンスタイムが1秒未満であればPASS。

## 【失敗時に記録する内容】

* 実測の平均・最大レスポンスタイムを記録する。

## 【修正禁止事項】

* アプリケーションコードを修正しないでください。性能劣化が見つかった場合もその場でチューニングせず記録に残す。

## 【次タスクへ進む条件】

* 記録完了後、A007の指示を待つ。

## 重要:

* アプリケーションコードを修正しないでください。
* テスト失敗時に、その場で修正して再テストしないでください。
* テスト失敗時は、失敗内容をテスト記録に残してください。
* 次のテストタスクには進まないでください。
