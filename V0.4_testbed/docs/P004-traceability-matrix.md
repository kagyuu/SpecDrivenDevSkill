# 要求トレーサビリティマトリクス — 会議室予約システム

> 本書は `spec-driven-dev` Skill フェーズP004の成果物です。インプット文書: `docs/P001-requirement.md`、`docs/P002-frontend-spec.md`、`docs/P003-backend-spec.md`。

## 0. 本フェーズの実施内容

* `docs/P001-requirement.md` に記載された要求事項(画面7件・API17件・非機能要件6件、計30件)それぞれに要求IDを付与し、`docs/P002-frontend-spec.md`・`docs/P003-backend-spec.md` の対応箇所を確認した。
* 初回確認の時点で、非機能要件6件のうち性能・可用性・セキュリティ・スケーラビリティ・ログ出力先の5件について、`docs/P003-backend-spec.md` に明示的な対応記述が無いことが判明した(想定同時利用者数のみ§5の排他制御で対応済みだった)。これは要求自体がP002/P003ではなくインフラ構成寄りの内容であったため見落としたものであり、`docs/P003-backend-spec.md` §8(非機能要件への対応)を追記して全項目をカバーした後、本表を作成している(そのため本表は「追記後」の状態を最終結果として記載する)。
* 併せて、`docs/P002-frontend-spec.md`・`docs/P003-backend-spec.md` がP001にない項目を追加していないか(過剰実装チェック)も確認した(§3参照)。

## 1. 画面(S01〜S07)

| 要求ID | 要求内容(要約) | 対応するP002の箇所 | 対応するP003の箇所 | 状態 |
| --- | --- | --- | --- | --- |
| REQ-SCREEN-001 | S01 ログイン画面(ID/パスワード認証) | §3 S01 | §2(認証・セッション内部設計) | OK |
| REQ-SCREEN-002 | S02 予約カレンダー画面(会議室×時間帯の空き状況表示) | §3 S02, §6.3 | §7.4, §7.8 | OK |
| REQ-SCREEN-003 | S03 予約作成画面 | §3 S03, §6.2 | §7.11, §5 | OK |
| REQ-SCREEN-004 | S04 予約詳細・編集画面(本人・管理者のみ編集/取消) | §3 S04 | §4, §7.12, §7.13 | OK |
| REQ-SCREEN-005 | S05 マイ予約一覧画面 | §3 S05 | §7.9 | OK |
| REQ-SCREEN-006 | S06 会議室管理画面(管理者用、論理削除) | §3 S06 | §7.5〜§7.7 | OK |
| REQ-SCREEN-007 | S07 ユーザー管理画面(管理者用、論理削除) | §3 S07 | §7.14〜§7.17 | OK(★注1参照) |

★注1: S07の「パスワード」欄はP001の入出力項目一覧に無く、P002が新規に補った項目である(`docs/P002-frontend-spec.md` §3 S07に★FIXME★として明記済み)。ユーザー登録という要求自体はP001に存在し、パスワード設定はその要求を実現する上で不可欠な補完のため、要求未充足(MISSING)ではなく要求充足のための正当な補完(OK)として扱う。ただし§3(過剰実装チェック)で改めて扱いを記載する。

## 2. API(17エンドポイント)

| 要求ID | 要求内容(要約) | 対応するP002の箇所 | 対応するP003の箇所 | 状態 |
| --- | --- | --- | --- | --- |
| REQ-API-001 | POST /api/auth/login | §4.1 | §7.1, §2, §3 | OK |
| REQ-API-002 | POST /api/auth/logout | §4.2 | §7.2 | OK |
| REQ-API-003 | GET /api/me | §4.3 | §7.3 | OK |
| REQ-API-004 | GET /api/rooms | §4.4 | §7.4 | OK |
| REQ-API-005 | POST /api/rooms(管理者のみ) | §4.5 | §7.5 | OK |
| REQ-API-006 | PUT /api/rooms/{room_id}(管理者のみ) | §4.6 | §7.6 | OK |
| REQ-API-007 | DELETE /api/rooms/{room_id}(論理削除、管理者のみ) | §4.7 | §7.7 | OK |
| REQ-API-008 | GET /api/reservations(カレンダー表示用) | §4.8 | §7.8, §8 | OK |
| REQ-API-009 | GET /api/reservations/mine | §4.9 | §7.9 | OK |
| REQ-API-010 | GET /api/reservations/{reservation_id} | §4.10 | §7.10 | OK |
| REQ-API-011 | POST /api/reservations(重複チェックあり) | §4.11 | §7.11, §5 | OK |
| REQ-API-012 | PUT /api/reservations/{reservation_id}(重複チェックあり) | §4.12 | §7.12, §5 | OK |
| REQ-API-013 | DELETE /api/reservations/{reservation_id} | §4.13 | §7.13 | OK |
| REQ-API-014 | GET /api/users(管理者のみ) | §4.14 | §7.14 | OK |
| REQ-API-015 | POST /api/users(管理者のみ) | §4.15 | §7.15 | OK |
| REQ-API-016 | PUT /api/users/{user_id}(管理者のみ) | §4.16 | §7.16 | OK |
| REQ-API-017 | DELETE /api/users/{user_id}(論理削除、管理者のみ) | §4.17 | §7.17 | OK |

## 3. 非機能要件

| 要求ID | 要求内容(要約) | 対応するP002の箇所 | 対応するP003の箇所 | 状態 |
| --- | --- | --- | --- | --- |
| REQ-NFR-001 | 性能(カレンダー表示3秒以内) | - | §8(インデックス設計・JOIN最小化) | OK |
| REQ-NFR-002 | 可用性(平日日中99%以上) | - | §8(インフラ観点、P005/P302で具体化する方針を明記) | OK(★注2参照) |
| REQ-NFR-003 | セキュリティ(HTTPS、パスワードハッシュ化、権限チェック) | §1(Cookie属性・エラーコード) | §3, §4, §8(TLS終端はP302へ) | OK(★注2参照) |
| REQ-NFR-004 | スケーラビリティ(単一サーバーで十分、将来スケールアウト検討) | - | §8(将来のRDB移行方針をADR記録推奨として明記) | OK(★注2参照) |
| REQ-NFR-005 | 想定同時利用者数(ピーク時30接続) | - | §5(排他制御), §8 | OK |
| REQ-NFR-006 | ログ出力先(標準出力→クラウドログ管理サービス) | - | §8(標準出力への構造化ログ出力を明記、収集基盤側はP302へ) | OK(★注2参照) |

★注2: REQ-NFR-002/003(TLS部分)/004/006は、性質上デプロイ構成(サーバー冗長化、リバースプロキシ、ログ収集基盤)に依存する要求であり、P002(フロントエンド外部仕様)・P003(バックエンド内部仕様)の対象範囲だけでは実現方式を確定しきれない。本書では「P003に、その要求をP005(実装計画)・P302(納品物作成)で具体化する方針を明記すること」をもって`OK`(要求を見失っていない状態)と判定した。厳密な意味での実現方式の確定はP005/P302まで持ち越しである点に留意されたい。この判定基準はSKILL側に明文化がなく、本書独自の判断である(★FIXME★、詳細は本書末尾「本フェーズ実施上の所見」参照)。

## 4. 過剰実装チェック(P001にない項目をP002/P003が追加していないか)

| 追加箇所 | 内容 | 判定 |
| --- | --- | --- |
| `docs/P002-frontend-spec.md` §3 S07 / §4.15 / §4.16、`docs/P003-backend-spec.md` §7.15 | ユーザー登録・編集APIへの「初期パスワード」「パスワードリセット」項目の追加 | 対応する要求なし。ただしP001のS07にはユーザー新規登録という要求自体は存在し、パスワード設定手段はその要求を実現する上で技術的に不可欠(パスワードなしでは新規ユーザーがログインできない)。よって「実装側から削るべき」ものではなく、「要求書(P001)側にパスワード初期設定に関する記載を追加すべき」というのが本書の判断である。P001更新(P902相当)を将来的に検討することを推奨するが、本フェーズでは一方的に削除・追加はしない。 |
| `docs/P002-frontend-spec.md` §4.17、`docs/P003-backend-spec.md` §7.17 | 管理者が自分自身を無効化することを禁止するルール | 対応する要求なし。一般的な業務要件として安全側に倒した追加。実害は小さいため許容するが、要求書に明記がない旨を記録する。 |

他に、P001にない画面・API・DB項目を追加している箇所は確認されなかった。

## 5. 判定結果

* 要求ID 30件すべてが `OK`。`PARTIAL` / `MISSING` は0件。
* 次フェーズ(P005 実装計画)に進む。

## 6. 本フェーズ実施上の所見

* 非機能要件(特にインフラ構成に依存するもの)を「P002/P003で完全に満たすべき要求」として扱うか、「P005/P302で具体化されれば良い要求」として扱うかの基準がSKILLの各フェーズ定義に明記されておらず、本書で独自に判断した。この判断基準は後続のP010(設計書横断レビュー)でも踏襲する。
