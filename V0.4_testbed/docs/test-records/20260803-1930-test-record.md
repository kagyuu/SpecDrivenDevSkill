# 受け入れ結合テスト再実行記録 — 2026-08-03 19:30 (P205、F001〜F003適用後)

> 本ファイルはP205(結合テスト再実施)フェーズの成果物。P203で適用したF001(セッションCookie `Secure` 属性の環境変数化)・
> F002(構造化ログ追加)・F003(A008期待結果のドキュメント更新)を反映したサーバーに対し、`docs/P201-review-report.md`
> 第1回でFAIL/BLOCKEDだったA001〜A007・A009・A010、および混合判定だったA008を再実行した。

## テスト環境

* サーバー起動コマンド: `cd server && DATABASE_PATH=/tmp/a9_test_data/app.db SESSION_COOKIE_SECURE=false python3 -m uvicorn app.main:app --host 127.0.0.1 --port 8811`(F001適用、TLSなしのローカル動作確認用起動方法)。
* シードデータ: 管理者 `A0001`、一般ユーザー `E0001`〜`E0012`、会議室10室。A006向けに会議室10室・7日分・週49件の予約を追加投入(`seed_a006_api.py`)。
* クライアント: Python `requests` ライブラリ。

## テスト結果一覧(P205再実行)

| ID | 結果 | 詳細 |
| --- | --- | --- |
| A001 | PASS | 手順1(200)・手順2(200、既存予約の内容維持)・手順3(会議室Bが一般ユーザーの一覧から除外)を確認。手順4(フロントエンド`reservationCreatePage.js`の会議室プルダウン)はコードレビューで確認: `roomApi.list()` は既定 `includeInactive=false` で呼ばれており、API側の除外と同じ結果になることを確認済み(headlessブラウザ非搭載のため実描画確認は不可、`docs/P008-test-direction.md` T0NN記録と同種の制約)。 |
| A002 | PASS | 作成→カレンダー反映→マイ予約表示→編集→カレンダー反映→取消→カレンダー/マイ予約から消滅、の7手順すべて確認。 |
| A003 | PASS | 管理者による他ユーザー予約の編集(200)・削除(200)を確認。 |
| A004 | PASS | 一般ユーザーによる `POST/PUT/DELETE /api/rooms`・`GET/POST/PUT/DELETE /api/users` がすべて403 FORBIDDENであることを確認(F001適用によりauthは正しく通過し、認可チェックまで到達するようになった)。手順3(S06/S07メニュー非表示)はコードレビューで確認: `client/js/app.js` が `currentUser.role === "admin"` の場合のみリンクを表示するロジックであることをソースで確認。 |
| A005 | PASS | 管理者による無効化後、`GET /api/me` が2回連続で401 AUTH_REQUIREDになることを確認(1回目でセッション削除、2回目は既に存在しないセッション)。 |
| A006 | PASS | 会議室10室・週49件のデータで `GET /api/rooms` 平均3.23ms・`GET /api/reservations`(1週間分)平均3.65ms。いずれも合否基準(平均1秒未満)を大きく下回る。 |
| A007 | PASS | 10ユーザーによる同一会議室・同一時間帯への同時 `POST /api/reservations` が、1件のみ201・残り9件409になることを確認(`BEGIN IMMEDIATE`による排他制御が正しく機能)。 |
| A008 | PASS | 手順1: `GET /api/users` のレスポンスにパスワード関連フィールドなし(200)。手順2: `password_hash` が `scrypt1$...` 形式で平文と異なる(F003でP009文書の期待結果を更新済み、その基準でPASS)。手順3: SQLインジェクション文字列でのログイン試行が401 AUTH_INVALID_CREDENTIALS(500・認証バイパスなし)。 |
| A009 | PASS | `GET /api/health`(正常)・`GET /api/reservations/99999`(エラー)いずれについても、標準出力にJSON Lines形式のログ(`{"level":"info","event":"http_request",...}` / `{"level":"error","event":"app_error",...}`)が出力されることを確認。 |
| A010 | PASS | ログイン→カレンダー閲覧→予約作成→カレンダー反映確認→詳細画面から取消→マイ予約反映確認→マイ予約から取消→カレンダー/マイ予約から消滅→ログアウト→ログアウト後401、の一連をAPIレベルで確認。`client/js/pages/*.js` の各コントローラが対応するAPIを正しく呼び出していることをコードレビューで確認済み(実ブラウザ/ヘッドレスブラウザ環境が本サンドボックスにないため、実際のクリック操作そのものは確認できていない。この制約自体はA010の指示書に元々★FIXME★として明記されている)。 |

## 既存テストの回帰確認(P204参照)

* バックエンド単体テスト: `cd server && python3 -m unittest discover -s tests -v` → 121件全PASS。
* フロントエンド単体テスト: `cd client && node --test tests/*.test.mjs` → 35件全PASS。
* P008結合テスト: T001〜T020を個別テストIDで再実行 → 20件全PASS。

## サマリ(P205)

* P201第1回でPASS以外だった10件(A001〜A007・A009・A010・A008)すべてPASSに転じた。
* 既存の合格済みテスト(単体121+35件、P008 20件)に回帰なし。
* → P201(実装横断レビュー、第2回)に判定を戻す。
