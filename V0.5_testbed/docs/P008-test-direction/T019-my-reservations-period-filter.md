あなたはExecutor(実装担当)です。以下の1テストタスクだけを実施してください。

# 【テストID】T019

## 【目的】

* マイ予約一覧APIが、作成者本人の予約のみを返し、期間フィルタ(`upcoming`/`past`)が正しく機能することを確認する。

## 【参照テスト計画】

* `docs/P006-test-plan.md` §2.1(S05行)

## 【対象モジュール】

* `server/app/api/reservations.py`、`server/app/repositories/reservation_repository.py`(`list_by_creator`)

## 【前提条件】対象スプリントの全モジュールビルドが成功していること

* U004が完了していること。`E0001` に未来日・過去日それぞれの予約が1件ずつ登録済みであること(過去日予約はシードスクリプトで直接投入する。通常のPOST APIでは過去日予約を作成できないため)。

## 【使用するテストデータ】

* `E0001` の未来予約1件、過去予約1件(シード投入)。他ユーザー `E0002` の予約1件。

## 【事前準備】

* `E0001` でログインしCookieを取得する。

## 【実行手順】

1. `GET /api/reservations/mine`(パラメータ省略、既定 `upcoming`)を呼ぶ。
2. `GET /api/reservations/mine?period=past` を呼ぶ。

## 【実行コマンド】

* `cd server && uv run pytest tests/test_api_reservations.py::test_mine_period_filter -v`

## 【期待結果】

* 手順1: `E0001` の未来予約のみ含まれる(過去予約・他ユーザーの予約は含まれない)。
* 手順2: `E0001` の過去予約のみ含まれる。

## 【合否判定基準】

* 両手順が期待結果どおりであればPASS。

## 【失敗時に記録する内容】

* 実際に返された予約一覧を記録する。

## 【修正禁止事項】

* アプリケーションコードを修正しないでください。

## 【次タスクへ進む条件】

* 記録完了後、T020の指示を待つ。

## 重要:

* アプリケーションコードを修正しないでください。
* テスト失敗時に、その場で修正して再テストしないでください。
* テスト失敗時は、失敗内容をテスト記録に残してください。
* 次のテストタスクには進まないでください。
