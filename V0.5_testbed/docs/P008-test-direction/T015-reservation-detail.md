あなたはExecutor(実装担当)です。以下の1テストタスクだけを実施してください。

# 【テストID】T015

## 【目的】

* 予約詳細取得APIが、参加者・備考・作成者名まで含めて正しく返すことを確認する。

## 【参照テスト計画】

* `docs/P006-test-plan.md` §2.1(S04行)

## 【対象モジュール】

* `server/app/api/reservations.py`、`server/app/repositories/reservation_repository.py`(`find_with_detail`)

## 【前提条件】対象スプリントの全モジュールビルドが成功していること

* U001〜U004が完了していること。参加者`E0002`を含む予約が1件登録済みであること。

## 【使用するテストデータ】

* T011で作成した予約ID(または本テスト用に新規作成した予約ID)

## 【事前準備】

* 一般ユーザーでログインしCookieを取得する。参加者付きの予約を1件作成する。

## 【実行手順】

1. `GET /api/reservations/{reservation_id}` を呼ぶ。

## 【実行コマンド】

* `cd server && uv run pytest tests/test_api_reservations.py::test_get_detail -v`

## 【期待結果】

* `200 OK`。レスポンスに `participants`・`notes`・`created_by` が含まれ、登録内容と一致する。

## 【合否判定基準】

* 期待結果どおりであればPASS。

## 【失敗時に記録する内容】

* 実際のレスポンスボディを記録する。

## 【修正禁止事項】

* アプリケーションコードを修正しないでください。

## 【次タスクへ進む条件】

* 記録完了後、T016の指示を待つ。

## 重要:

* アプリケーションコードを修正しないでください。
* テスト失敗時に、その場で修正して再テストしないでください。
* テスト失敗時は、失敗内容をテスト記録に残してください。
* 次のテストタスクには進まないでください。
