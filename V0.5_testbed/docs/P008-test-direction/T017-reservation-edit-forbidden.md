あなたはExecutor(実装担当)です。以下の1テストタスクだけを実施してください。

# 【テストID】T017

## 【目的】

* 予約者本人でも管理者でもない一般ユーザーが他人の予約を編集・取消しようとした場合に `403 FORBIDDEN` になることを確認する(P001「予約者本人または管理者のみ編集可」に対応)。

## 【参照テスト計画】

* `docs/P006-test-plan.md` §2.1(S04行)

## 【対象モジュール】

* `server/app/services/reservation_service.py`(`check_editable`)

## 【前提条件】対象スプリントの全モジュールビルドが成功していること

* U004が完了していること。T011の予約(作成者 `E0001`)が存在すること。第三者ユーザー `E0002`(一般)が存在すること。

## 【使用するテストデータ】

* T011の予約ID、第三者ユーザー `E0002` のCookie

## 【事前準備】

* `E0002` でログインしCookieを取得する。

## 【実行手順】

1. `E0002` のCookieで `PUT /api/reservations/{reservation_id}` を呼ぶ。
2. `E0002` のCookieで `DELETE /api/reservations/{reservation_id}` を呼ぶ。

## 【実行コマンド】

* `cd server && uv run pytest tests/test_api_reservations.py::test_update_delete_forbidden_for_others -v`

## 【期待結果】

* 両手順とも `403 FORBIDDEN`。予約データが変更・削除されていないこと。

## 【合否判定基準】

* 両手順が期待結果どおりであればPASS。

## 【失敗時に記録する内容】

* 実際のステータスコード、予約データの変化有無を記録する。

## 【修正禁止事項】

* アプリケーションコードを修正しないでください。

## 【次タスクへ進む条件】

* 記録完了後、T018の指示を待つ。

## 重要:

* アプリケーションコードを修正しないでください。
* テスト失敗時に、その場で修正して再テストしないでください。
* テスト失敗時は、失敗内容をテスト記録に残してください。
* 次のテストタスクには進まないでください。
