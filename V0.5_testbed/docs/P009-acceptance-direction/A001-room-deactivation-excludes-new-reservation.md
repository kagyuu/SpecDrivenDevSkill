あなたはReviewer Loop(実装横断レビュー担当)です。以下の1テストタスクだけを実施してください。

# 【テストID】A001

## 【目的】

* 会議室を無効化した後も既存の予約は残ること、かつ新規予約作成時の会議室選択肢から無効化済み会議室が除外されることを確認する(sprint2の会議室管理機能とsprint3の予約作成機能をまたぐ確認)。

## 【参照テスト計画】

* `docs/P006-test-plan.md` §2.1(S06行「受入/システム」列)

## 【対象モジュール】

* `server/app/services/room_service.py`、`server/app/services/reservation_service.py`、`client/src/pages/ReservationCreatePage.tsx`

## 【前提条件】全モジュールビルドが成功していること

* U001〜U004(全スプリント)が完了していること。

## 【使用するテストデータ】

* 会議室「会議室B」(有効)、既存予約1件(会議室B、未来日)

## 【事前準備】

* 管理者でログインし、会議室Bと会議室Bを使う予約を1件作成しておく。

## 【実行手順】

1. 管理者が `DELETE /api/rooms/{room_bのid}` で会議室Bを無効化する。
2. `GET /api/reservations/{既存予約のid}` で既存予約が引き続き取得できることを確認する。
3. `GET /api/rooms`(`include_inactive` 省略)を一般ユーザーで呼び、会議室Bが含まれないことを確認する。
4. フロントエンドのS03予約作成画面の会議室プルダウンに会議室Bが表示されないことを確認する。

## 【実行コマンド】

* `cd server && uv run pytest tests/acceptance/test_a001_room_deactivation.py -v`

## 【期待結果】

* 手順1: `200 OK`。
* 手順2: `200 OK`、既存予約の内容が変化していない。
* 手順3: `rooms` に会議室Bが含まれない。
* 手順4: プルダウンの選択肢に会議室Bが表示されない。

## 【合否判定基準】

* 4手順すべてが期待結果どおりであればPASS。

## 【失敗時に記録する内容】

* 各手順の実際の結果を `docs/test-records/` に記録する。

## 【修正禁止事項】

* アプリケーションコードを修正しないでください。テスト失敗時にその場で修正して再テストしないでください。

## 【次タスクへ進む条件】

* 記録完了後、A002の指示を待つ。

## 重要:

* アプリケーションコードを修正しないでください。
* テスト失敗時に、その場で修正して再テストしないでください。
* テスト失敗時は、失敗内容をテスト記録に残してください。
* 次のテストタスクには進まないでください。
