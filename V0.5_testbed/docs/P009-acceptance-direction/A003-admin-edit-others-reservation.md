あなたはReviewer Loop(実装横断レビュー担当)です。以下の1テストタスクだけを実施してください。

# 【テストID】A003

## 【目的】

* 管理者が他の一般ユーザーの予約を編集・取消できることを確認する(P001「予約者本人または管理者のみ編集可」の管理者側の確認。sprint2のユーザー権限とsprint4の予約編集権限判定をまたぐ確認)。

## 【参照テスト計画】

* `docs/P006-test-plan.md` §2.1(S04行「受入/システム」列)

## 【対象モジュール】

* `server/app/services/reservation_service.py`(`check_editable`)

## 【前提条件】全モジュールビルドが成功していること

* U001〜U004が完了していること。一般ユーザー `E0001` の予約が1件存在すること。管理者 `A0001` が存在すること。

## 【使用するテストデータ】

* `E0001` の予約1件、管理者 `A0001` のCookie

## 【事前準備】

* `A0001` でログインする。

## 【実行手順】

1. 管理者Cookieで `PUT /api/reservations/{E0001の予約id}` を呼び、件名を変更する。
2. 管理者Cookieで `DELETE /api/reservations/{同予約id}` を呼ぶ。

## 【実行コマンド】

* `cd server && uv run pytest tests/acceptance/test_a003_admin_edit_others.py -v`

## 【期待結果】

* 手順1: `200 OK`、件名が変更される。
* 手順2: `200 OK`、予約が削除される。

## 【合否判定基準】

* 両手順が期待結果どおりであればPASS。

## 【失敗時に記録する内容】

* 各手順の実際のステータスコードを記録する。

## 【修正禁止事項】

* アプリケーションコードを修正しないでください。

## 【次タスクへ進む条件】

* 記録完了後、A004の指示を待つ。

## 重要:

* アプリケーションコードを修正しないでください。
* テスト失敗時に、その場で修正して再テストしないでください。
* テスト失敗時は、失敗内容をテスト記録に残してください。
* 次のテストタスクには進まないでください。
