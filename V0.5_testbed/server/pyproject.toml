[project]
name = "server"
version = "0.1.0"
description = "会議室予約システム backend"
requires-python = ">=3.11"
dependencies = [
    "starlette",
    "uvicorn",
    "pydantic>=2",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
