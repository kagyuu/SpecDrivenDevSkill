あなたはReviewer Loop(実装横断レビュー担当)です。以下は1テストタスク分の作業範囲と完了条件を定義したものです。実施後は、結果(PASS/FAIL/BLOCKED/NOT RUNいずれであっても)を記録したうえで、Reviewer Loopの「停止条件」(`SKILL.md` 参照)に該当しない限り、`docs/P009-acceptance-direction.md` のWBSに従って自動的に次のテストタスクへ進んでください。人間の指示を待って停止しないでください。

# 【テストID】A006

## 【目的】

* S02予約カレンダー画面の表示が3秒以内に完了することを実測で確認する(`docs/P001-requirement.md` 非機能要件、`docs/P002-frontend-spec.md` 7章の設計時見積りの実測検証)。

## 【参照テスト計画】

* `docs/P006-test-plan.md` 3章(性能観点)

## 【対象モジュール】

* `client/src/pages/CalendarPage.tsx`, `server/app/routers/reservations.py`

## 【前提条件】全モジュールビルドが成功していること

* U001〜U005すべてが完了していること

## 【使用するテストデータ】

* 会議室10件、直近1週間分の予約データ150件程度(`docs/P002-frontend-spec.md` 7章の見積り上限に近い件数)を事前投入する

## 【事前準備】

* テストデータ投入スクリプトで会議室10件・予約150件をシードする

## 【実行手順】

1. サーバー・クライアントをローカル環境で起動する
2. ブラウザ(またはヘッドレスブラウザ)でログイン後、S02への画面遷移を開始した時刻から、グリッドの全予約セルが描画完了するまでの時間を計測する
3. 3回計測し、いずれも3秒以内に収まるか確認する

## 【実行コマンド】

* `cd client && node --test tests/acceptance/test_calendar_performance.js`(Playwright等のヘッドレスブラウザ計測、またはfetch応答時間+レンダリング完了イベントの計測)

## 【期待結果】

* 3回の計測すべてで3秒以内に表示が完了する

## 【合否判定基準】

* 3回とも3秒以内であればPASS。1回でも超過すればFAIL(ただしローカル環境の計測値は本番環境のネットワーク条件と異なるため、参考値であることをテスト記録に明記する)。

## 【失敗時に記録する内容】

* 実際の計測時間(3回分)、ボトルネックと疑われる箇所(API応答時間/レンダリング時間の内訳)

## 【修正禁止事項】

* アプリケーションコードを修正しない。テストコードをその場で都合よく変更しない。失敗したテストをスキップしない。期待値を変更して成功扱いにしない。同じ失敗に対して場当たり的な再テストを繰り返さない。修正が必要な場合はP202以降に引き渡す。

## 【次タスクへ進む条件】

* 結果を記録したらA007へ進む

## 重要:

* アプリケーションコードを修正しないでください。
* テスト失敗時に、その場で修正して再テストしないでください。
* テスト失敗時は、失敗内容をテスト記録に残してください。
* このテストタスクの結果を記録したら、Reviewer Loopの停止条件に該当しない限り、次のテストタスクに自動的に進んでください。
