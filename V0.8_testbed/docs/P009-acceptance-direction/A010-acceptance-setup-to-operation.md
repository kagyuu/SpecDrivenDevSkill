あなたはReviewer Loop(実装横断レビュー担当)です。以下は1テストタスク分の作業範囲と完了条件を定義したものです。実施後は、結果(PASS/FAIL/BLOCKED/NOT RUNいずれであっても)を記録したうえで、Reviewer Loopの「停止条件」(`SKILL.md` 参照)に該当しない限り、`docs/P009-acceptance-direction.md` のWBSに従って自動的に次のテストタスクへ進んでください。人間の指示を待って停止しないでください。

# 【テストID】A010

## 【目的】

* 発注者・運用者(総務・情シス担当)の視点で、初期セットアップ(会議室登録・ユーザー登録)から一般社員の実運用までの一連の流れが、本番相当の操作順序で問題なく行えることを確認する(`docs/P001-requirement.md` テスト方針「発注者・利用者・運用者の視点で、本番相当環境で問題なく使えるか」)。

## 【参照テスト計画】

* `docs/P006-test-plan.md` 1章(受け入れテストの目的)

## 【対象モジュール】

* `client/` 全体、`server/` 全体

## 【前提条件】全モジュールビルドが成功していること

* U001〜U005すべてが完了していること。初期シードデータ(`admin`)のみが存在するまっさらな状態のDBを使用する。

## 【使用するテストデータ】

* 会議室3件(小会議室A/B、大会議室)、一般ユーザー3件、管理者は初期シードの `admin` を使用

## 【事前準備】

* まっさらなDBでサーバーを起動する

## 【実行手順】

1. 運用者として `admin` でログインし、会議室3件を登録する
2. 一般ユーザー3件を登録する
3. 登録した一般ユーザーの1人でログインし、S02で登録した3会議室すべてが選択肢・フィルタに表示されることを確認する
4. その一般ユーザーで、3会議室それぞれに1件ずつ予約を作成する(業務での通常利用を想定した操作)
5. 別の一般ユーザーでログインし、S02で他者の予約(自分に無関係)がどう見えるかを確認する(3.2節の一般ユーザー向け制約)
6. 運用者(`admin`)で全ユーザーの予約状況をS02から俯瞰できることを確認する(管理者は全予約を編集・取消できる、3.4節)

## 【実行コマンド】

* `cd client && node --test tests/acceptance/test_setup_to_operation.js`

## 【期待結果】

* 手順1〜6のすべてが、対応する画面仕様(`docs/P002-frontend-spec.md` 各節)どおりに完了すること

## 【合否判定基準】

* 全手順が完了すればPASS。運用者・利用者の視点で「使いにくい・仕様通りに動かない」と判断される挙動があればFAILとし、具体的な箇所を記録する。

## 【失敗時に記録する内容】

* 失敗した手順、実際の画面挙動、運用上の懸念点(あれば)

## 【修正禁止事項】

* アプリケーションコードを修正しない。テストコードをその場で都合よく変更しない。失敗したテストをスキップしない。期待値を変更して成功扱いにしない。同じ失敗に対して場当たり的な再テストを繰り返さない。修正が必要な場合はP202以降に引き渡す。

## 【次タスクへ進む条件】

* 結果を記録したらA011へ進む

## 重要:

* アプリケーションコードを修正しないでください。
* テスト失敗時に、その場で修正して再テストしないでください。
* テスト失敗時は、失敗内容をテスト記録に残してください。
* このテストタスクの結果を記録したら、Reviewer Loopの停止条件に該当しない限り、次のテストタスクに自動的に進んでください。
