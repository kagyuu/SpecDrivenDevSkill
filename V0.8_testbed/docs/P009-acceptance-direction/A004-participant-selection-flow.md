あなたはReviewer Loop(実装横断レビュー担当)です。以下は1テストタスク分の作業範囲と完了条件を定義したものです。実施後は、結果(PASS/FAIL/BLOCKED/NOT RUNいずれであっても)を記録したうえで、Reviewer Loopの「停止条件」(`SKILL.md` 参照)に該当しない限り、`docs/P009-acceptance-direction.md` のWBSに従って自動的に次のテストタスクへ進んでください。人間の指示を待って停止しないでください。

# 【テストID】A004

## 【目的】

* U005で登録されたユーザーが、U003で実装された参加者候補API(`GET /api/users/directory`)経由でS03の参加者選択肢に正しく表示され、予約作成後に参加者一覧として保存・表示されることを確認する(ユーザー管理と予約作成という異なるスプリントをまたぐ連携)。

## 【参照テスト計画】

* `docs/P006-test-plan.md` 2.4節

## 【対象モジュール】

* `server/app/routers/users.py`(directoryエンドポイント)、`server/app/repositories/reservation_repository.py`(participants)、`client/src/pages/ReservationCreatePage.tsx`, `ReservationDetailPage.tsx`

## 【前提条件】全モジュールビルドが成功していること

* U001〜U005すべてが完了していること

## 【使用するテストデータ】

* 一般ユーザー「鈴木花子」(社員ID `e2002`)を事前登録

## 【事前準備】

* 管理者で「鈴木花子」を登録しておく。予約者本人(別の一般ユーザー)でログインする。

## 【実行手順】

1. S03で会議室・日時・件名を入力し、参加者に「鈴木花子」を選択する
2. 登録する
3. S04で該当予約を開き、参加者一覧に「鈴木花子」が表示されることを確認する
4. SQLiteを直接クエリし `reservation_participants` テーブルを確認する

## 【実行コマンド】

* `cd client && node --test tests/acceptance/test_participant_selection_flow.js`

## 【期待結果】

* 手順1: 参加者候補一覧に「鈴木花子」が社員ID・氏名で表示される(役職・有効フラグ等の管理者専用情報は含まれない、`docs/P002-frontend-spec.md` 4.10.1節)
* 手順3: 参加者一覧に「鈴木花子」が表示される
* 手順4: `reservation_participants` に該当行が存在する

## 【合否判定基準】

* 上記すべてを満たせばPASS

## 【失敗時に記録する内容】

* 実際の候補一覧内容、実際の参加者一覧表示、DBの実際の内容

## 【修正禁止事項】

* アプリケーションコードを修正しない。テストコードをその場で都合よく変更しない。失敗したテストをスキップしない。期待値を変更して成功扱いにしない。同じ失敗に対して場当たり的な再テストを繰り返さない。修正が必要な場合はP202以降に引き渡す。

## 【次タスクへ進む条件】

* 結果を記録したらA005へ進む

## 重要:

* アプリケーションコードを修正しないでください。
* テスト失敗時に、その場で修正して再テストしないでください。
* テスト失敗時は、失敗内容をテスト記録に残してください。
* このテストタスクの結果を記録したら、Reviewer Loopの停止条件に該当しない限り、次のテストタスクに自動的に進んでください。
