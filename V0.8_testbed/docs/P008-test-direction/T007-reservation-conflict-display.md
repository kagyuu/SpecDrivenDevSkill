あなたはExecutor(実装担当)です。以下は1テストタスク分の作業範囲と完了条件を定義したものです。実施後は、結果(PASS/FAIL/BLOCKED/NOT RUNいずれであっても)を記録したうえで、Executor Stepの「停止条件」(`SKILL.md` 参照)に該当しない限り、`docs/P008-test-direction.md` のWBSに従って自動的に次のテストタスクへ進んでください。人間の指示を待って停止しないでください。

# 【テストID】T007

## 【目的】

* 既に予約済みの会議室・時間帯に対してS03から予約を試みた際、409レスポンスを受けてS03に重複エラーメッセージが表示され、入力内容が保持されることを確認する(画面+APIの結合、エラー系シナリオ)。

## 【参照テスト計画】

* `docs/P006-test-plan.md` 2.1〜2.3節

## 【対象モジュール】

* `client/src/pages/ReservationCreatePage.tsx`, `server/app/routers/reservations.py`, `server/app/services/reservation_service.py`

## 【前提条件】対象スプリントの全モジュールビルドが成功していること

* U003の全タスクが完了していること。T006で作成した「第一会議室、翌営業日10:00-11:00、週次定例会議」の予約が存在すること。

## 【使用するテストデータ】

* 会議室: 第一会議室、日付: 翌営業日、時間帯: 10:30-11:30(T006の予約と一部重複)、件名: 「臨時会議」

## 【事前準備】

* T006のテストを先に実行し予約を作成しておく。一般ユーザーでS03を開いた状態にする。

## 【実行手順】

1. テストデータでフォームを入力し登録を試みる
2. 画面のエラーメッセージ表示を確認する
3. フォームの入力内容が保持されていることを確認する

## 【実行コマンド】

* `cd client && node --import tsx --test tests/integration/reservationConflict.test.tsx`

## 【期待結果】

* 手順1: APIレスポンス409 `RESERVATION_CONFLICT`
* 手順2: 「選択した会議室・時間帯は既に予約されています」が画面上部に表示される
* 手順3: 会議室・日付・時刻・件名の入力値が変わらず表示されている

## 【合否判定基準】

* 上記すべてを満たせばPASS

## 【失敗時に記録する内容】

* 実際のレスポンス、実際に表示されたメッセージ、入力保持の実際の状態

## 【修正禁止事項】

* アプリケーションコードを修正しない。テストコードをその場で都合よく変更しない。失敗したテストをスキップしない。期待値を変更して成功扱いにしない。同じ失敗に対して場当たり的な再テストを繰り返さない。

## 【次タスクへ進む条件】

* 結果を記録したらT008へ進む

## 重要:

* アプリケーションコードを修正しないでください。
* テスト失敗時に、その場で修正して再テストしないでください。
* テスト失敗時は、失敗内容をテスト記録に残してください。
* このテストタスクの結果を記録したら、Executor Stepの停止条件に該当しない限り、次のテストタスクに自動的に進んでください。
