あなたはExecutor(実装担当)です。以下は1テストタスク分の作業範囲と完了条件を定義したものです。実施後は、結果(PASS/FAIL/BLOCKED/NOT RUNいずれであっても)を記録したうえで、Executor Stepの「停止条件」(`SKILL.md` 参照)に該当しない限り、`docs/P008-test-direction.md` のWBSに従って自動的に次のテストタスクへ進んでください。人間の指示を待って停止しないでください。

# 【テストID】T011

## 【目的】

* S04予約詳細・編集画面から予約を取消すると、DBから削除されS02カレンダー画面から消えることを確認する(画面+API+DBの結合)。

## 【参照テスト計画】

* `docs/P006-test-plan.md` 2.3〜2.4節

## 【対象モジュール】

* `client/src/pages/ReservationDetailPage.tsx`, `client/src/pages/CalendarPage.tsx`, `server/app/routers/reservations.py`, SQLite(`reservations`テーブル)

## 【前提条件】対象スプリントの全モジュールビルドが成功していること

* U004の全タスクが完了していること。T006の予約が存在すること。

## 【使用するテストデータ】

* T006の予約

## 【事前準備】

* 予約者本人でログインし、S05またはS02からS04を開いた状態にする

## 【実行手順】

1. 「取消」ボタンを押し、確認ダイアログでOKを押す
2. S02へ遷移後、該当セルを確認する
3. SQLiteを直接クエリし `reservations` テーブルを確認する

## 【実行コマンド】

* `cd client && node --import tsx --test tests/integration/reservationCancelReflect.test.tsx`

## 【期待結果】

* 手順1: 204、S02へ遷移
* 手順2: 該当セルが空きセルに戻っている
* 手順3: 該当 `reservation_id` の行が存在しない(物理削除、`docs/P002-frontend-spec.md` 4.9.2節)

## 【合否判定基準】

* 上記すべてを満たせばPASS

## 【失敗時に記録する内容】

* 実際の画面表示、DBの実際の状態

## 【修正禁止事項】

* アプリケーションコードを修正しない。テストコードをその場で都合よく変更しない。失敗したテストをスキップしない。期待値を変更して成功扱いにしない。同じ失敗に対して場当たり的な再テストを繰り返さない。

## 【次タスクへ進む条件】

* 結果を記録したらT012へ進む

## 重要:

* アプリケーションコードを修正しないでください。
* テスト失敗時に、その場で修正して再テストしないでください。
* テスト失敗時は、失敗内容をテスト記録に残してください。
* このテストタスクの結果を記録したら、Executor Stepの停止条件に該当しない限り、次のテストタスクに自動的に進んでください。
