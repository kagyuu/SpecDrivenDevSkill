あなたはExecutor(実装担当)です。以下は1テストタスク分の作業範囲と完了条件を定義したものです。実施後は、結果(PASS/FAIL/BLOCKED/NOT RUNいずれであっても)を記録したうえで、Executor Stepの「停止条件」(`SKILL.md` 参照)に該当しない限り、`docs/P008-test-direction.md` のWBSに従って自動的に次のテストタスクへ進んでください。人間の指示を待って停止しないでください。

# 【テストID】T008

## 【目的】

* S03予約作成画面で、選択した会議室の収容人数を超える参加予定人数を入力した場合に、エラーメッセージが表示され送信がブロックされることを確認する(画面+APIの結合、エラー系シナリオ)。

## 【参照テスト計画】

* `docs/P006-test-plan.md` 2.1〜2.3節

## 【対象モジュール】

* `client/src/pages/ReservationCreatePage.tsx`, `server/app/routers/reservations.py`, `server/app/services/reservation_service.py`

## 【前提条件】対象スプリントの全モジュールビルドが成功していること

* U003の全タスクが完了していること。会議室「第一会議室」(収容人数8)が登録済みであること。

## 【使用するテストデータ】

* 会議室: 第一会議室(収容人数8)、参加予定人数: 9(収容人数超過)

## 【事前準備】

* 一般ユーザーでS03を開いた状態にする

## 【実行手順】

1. 会議室に「第一会議室」を選択する
2. 参加予定人数に9を入力する
3. クライアント側で即時表示されるエラーを確認する(`docs/P002-frontend-spec.md` 3.3節、クライアント側でも即時検証)
4. クライアント側検証を意図的に無効化した状態(またはAPIを直接呼び出す形)で送信し、サーバー側のバリデーション応答を確認する

## 【実行コマンド】

* `cd client && node --import tsx --test tests/integration/capacityExceeded.test.tsx`

## 【期待結果】

* 手順3: 「選択した会議室の収容人数(8名)を超えています」がクライアント側で即時表示される
* 手順4: サーバーからも400 `VALIDATION_ERROR`(`fields.attendee_count`)が返る(クライアント側検証のバイパスに対してもサーバー側が正として機能することの確認、`docs/P002-frontend-spec.md` 3.3節末尾の方針)

## 【合否判定基準】

* 上記すべてを満たせばPASS

## 【失敗時に記録する内容】

* 実際に表示されたエラーメッセージ、サーバー側レスポンスの実際の内容

## 【修正禁止事項】

* アプリケーションコードを修正しない。テストコードをその場で都合よく変更しない。失敗したテストをスキップしない。期待値を変更して成功扱いにしない。同じ失敗に対して場当たり的な再テストを繰り返さない。

## 【次タスクへ進む条件】

* 結果を記録したらT009へ進む

## 重要:

* アプリケーションコードを修正しないでください。
* テスト失敗時に、その場で修正して再テストしないでください。
* テスト失敗時は、失敗内容をテスト記録に残してください。
* このテストタスクの結果を記録したら、Executor Stepの停止条件に該当しない限り、次のテストタスクに自動的に進んでください。
