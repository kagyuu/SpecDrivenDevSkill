あなたはExecutor(実装担当)です。以下は1テストタスク分の作業範囲と完了条件を定義したものです。実施後は、結果(PASS/FAIL/BLOCKED/NOT RUNいずれであっても)を記録したうえで、Executor Stepの「停止条件」(`SKILL.md` 参照)に該当しない限り、`docs/P008-test-direction.md` のWBSに従って自動的に次のテストタスクへ進んでください。人間の指示を待って停止しないでください。

# 【テストID】T010

## 【目的】

* 予約を自分自身の元の時間帯のまま(または一部を変更して)更新した際、自分自身の予約が重複チェックの対象から除外され、誤って409にならないことを確認する(API+DBの結合)。

## 【参照テスト計画】

* `docs/P006-test-plan.md` 2.1節

## 【対象モジュール】

* `server/app/routers/reservations.py`, `server/app/services/reservation_service.py`, `server/app/repositories/reservation_repository.py`

## 【前提条件】対象スプリントの全モジュールビルドが成功していること

* U004の全タスク(`docs/P007-impl-direction/U004-reservation-detail-and-mylist.md`)が完了していること。T006の予約が存在すること。

## 【使用するテストデータ】

* T006の予約(第一会議室、翌営業日10:00-11:00)を件名のみ「週次定例会議(更新)」に変更

## 【事前準備】

* T006の予約を作成したユーザーでログインする

## 【実行手順】

1. `PUT /api/reservations/{reservation_id}` に、時間帯は変更せず件名のみ変更したボディを送信する
2. レスポンスを確認する
3. 同じ予約を、時間帯を10:00-11:00のまま、参加予定人数のみ7に変更して再度PUTする

## 【実行コマンド】

* `cd server && python -m pytest tests/integration/test_reservation_update_self_exclude.py -v`

## 【期待結果】

* 手順2: 200(409にならない)、`title='週次定例会議(更新)'`
* 手順3: 200(409にならない)、`attendee_count=7`

## 【合否判定基準】

* 上記すべてを満たせばPASS。409が返った場合は自分自身除外ロジックの不具合としてFAIL。

## 【失敗時に記録する内容】

* 実際のレスポンスステータス・ボディ

## 【修正禁止事項】

* アプリケーションコードを修正しない。テストコードをその場で都合よく変更しない。失敗したテストをスキップしない。期待値を変更して成功扱いにしない。同じ失敗に対して場当たり的な再テストを繰り返さない。

## 【次タスクへ進む条件】

* 結果を記録したらT011へ進む

## 重要:

* アプリケーションコードを修正しないでください。
* テスト失敗時に、その場で修正して再テストしないでください。
* テスト失敗時は、失敗内容をテスト記録に残してください。
* このテストタスクの結果を記録したら、Executor Stepの停止条件に該当しない限り、次のテストタスクに自動的に進んでください。
