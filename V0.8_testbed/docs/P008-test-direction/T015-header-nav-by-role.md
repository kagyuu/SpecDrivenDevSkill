あなたはExecutor(実装担当)です。以下は1テストタスク分の作業範囲と完了条件を定義したものです。実施後は、結果(PASS/FAIL/BLOCKED/NOT RUNいずれであっても)を記録したうえで、Executor Stepの「停止条件」(`SKILL.md` 参照)に該当しない限り、`docs/P008-test-direction.md` のWBSに従って自動的に次のテストタスクへ進んでください。人間の指示を待って停止しないでください。

# 【テストID】T015

## 【目的】

* ヘッダーナビゲーションの表示が、一般ユーザーと管理者で `docs/P002-frontend-spec.md` の権限仕様どおりに出し分けられることを確認する(画面の結合、権限別表示)。

## 【参照テスト計画】

* `docs/P006-test-plan.md` 2.4節(権限まわりの確認)

## 【対象モジュール】

* `client/src/components/Header.tsx`, `client/src/pages/CalendarPage.tsx`

## 【前提条件】対象スプリントの全モジュールビルドが成功していること

* U005の全タスクが完了していること。一般ユーザーアカウント1件・管理者アカウント1件が用意されていること。

## 【使用するテストデータ】

* 一般ユーザー(T013で作成した `e1001` を有効な状態に戻して使用、または新規に一般ユーザーを1件作成)、管理者(`admin`)

## 【事前準備】

* それぞれのアカウントでログインし、S02(カレンダー画面)を開く

## 【実行手順】

1. 一般ユーザーでログインし、S02のヘッダーのリンク一覧を確認する
2. 管理者でログインし、S02のヘッダーのリンク一覧を確認する
3. 管理者で「会議室管理」リンクからS06に遷移し、S06のヘッダーのリンク一覧を確認する
4. 一般ユーザーで `/admin/rooms` および `/admin/users` に直接URLアクセスを試みる

## 【実行コマンド】

* `cd client && node --import tsx --test tests/integration/headerNavByRole.test.tsx`

## 【期待結果】(`docs/P002-frontend-spec.md` 3.0節の共通ヘッダー仕様、★P010初回レビュー後に新設)

* 手順1: 「マイ予約」「ログアウト」は表示され、「会議室管理」「ユーザー管理」は表示されない
* 手順2: 「マイ予約」「会議室管理」「ログアウト」は表示されるが、「ユーザー管理」はS02では表示されない
* 手順3: S06のヘッダーには「ユーザー管理」も表示される(S06画面表示中のみの限定表示)
* 手順4: いずれも403画面が表示される(`docs/P002-frontend-spec.md` 3.6〜3.7節のアクセス制御)

## 【合否判定基準】

* 上記すべてを満たせばPASS

## 【失敗時に記録する内容】

* 実際に表示されたリンク一覧、直接URLアクセス時の実際の画面

## 【修正禁止事項】

* アプリケーションコードを修正しない。テストコードをその場で都合よく変更しない。失敗したテストをスキップしない。期待値を変更して成功扱いにしない。同じ失敗に対して場当たり的な再テストを繰り返さない。

## 【次タスクへ進む条件】

* 結果を記録したら、本書(`docs/P008-test-direction.md`)の全テストが完了となる。P008完了後、`docs/P009-acceptance-direction.md` への移行を待つ。

## 重要:

* アプリケーションコードを修正しないでください。
* テスト失敗時に、その場で修正して再テストしないでください。
* テスト失敗時は、失敗内容をテスト記録に残してください。
* このテストタスクの結果を記録したら、Executor Stepの停止条件に該当しない限り、次のテストタスクに自動的に進んでください(本テストがP008最後のテストタスクである場合は、P103全体の完了条件を確認してください)。
