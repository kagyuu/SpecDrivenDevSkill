あなたはExecutor(実装担当)です。以下は1テストタスク分の作業範囲と完了条件を定義したものです。実施後は、結果(PASS/FAIL/BLOCKED/NOT RUNいずれであっても)を記録したうえで、Executor Stepの「停止条件」(`SKILL.md` 参照)に該当しない限り、`docs/P008-test-direction.md` のWBSに従って自動的に次のテストタスクへ進んでください。人間の指示を待って停止しないでください。

# 【テストID】T009

## 【目的】

* S02予約カレンダー画面が、`GET /api/reservations` の結果を会議室×時間帯グリッドの正しいセルに表示することを確認する(画面←APIの結合)。

## 【参照テスト計画】

* `docs/P006-test-plan.md` 2.3節

## 【対象モジュール】

* `client/src/pages/CalendarPage.tsx`, `client/src/components/RoomTimeGrid.tsx`, `server/app/routers/reservations.py`

## 【前提条件】対象スプリントの全モジュールビルドが成功していること

* U003の全タスクが完了していること。T006で作成した予約が存在すること。

## 【使用するテストデータ】

* T006の予約(第一会議室、翌営業日10:00-11:00、週次定例会議)

## 【事前準備】

* 一般ユーザーでログインし、該当週のS02を開く

## 【実行手順】

1. S02画面を表示する
2. 第一会議室の列、10:00-10:30および10:30-11:00のセル(30分刻み)を確認する
3. 会議室フィルタで第一会議室のみに絞り込む
4. 別の会議室のみに絞り込む

## 【実行コマンド】

* `cd client && node --import tsx --test tests/integration/calendarListDisplay.test.tsx`

## 【期待結果】

* 手順2: 両セルに「週次定例会議」(予約者名付き)が表示され、参加予定人数は表示されない
* 手順3: 第一会議室の列のみが表示され、該当予約が見える
* 手順4: 別会議室に絞り込むと該当予約は表示されない(別会議室には無関係のため)

## 【合否判定基準】

* 上記すべてを満たせばPASS

## 【失敗時に記録する内容】

* 実際の表示内容、APIレスポンスの実際の内容

## 【修正禁止事項】

* アプリケーションコードを修正しない。テストコードをその場で都合よく変更しない。失敗したテストをスキップしない。期待値を変更して成功扱いにしない。同じ失敗に対して場当たり的な再テストを繰り返さない。

## 【次タスクへ進む条件】

* 結果を記録したらT010へ進む(U004のテストタスクへ移行)

## 重要:

* アプリケーションコードを修正しないでください。
* テスト失敗時に、その場で修正して再テストしないでください。
* テスト失敗時は、失敗内容をテスト記録に残してください。
* このテストタスクの結果を記録したら、Executor Stepの停止条件に該当しない限り、次のテストタスクに自動的に進んでください。
