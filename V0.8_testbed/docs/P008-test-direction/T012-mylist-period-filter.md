あなたはExecutor(実装担当)です。以下は1テストタスク分の作業範囲と完了条件を定義したものです。実施後は、結果(PASS/FAIL/BLOCKED/NOT RUNいずれであっても)を記録したうえで、Executor Stepの「停止条件」(`SKILL.md` 参照)に該当しない限り、`docs/P008-test-direction.md` のWBSに従って自動的に次のテストタスクへ進んでください。人間の指示を待って停止しないでください。

# 【テストID】T012

## 【目的】

* S05マイ予約一覧画面の期間フィルタ(今後の予約/過去の予約/すべて)切り替えが、`GET /api/reservations/mine?period=...` を正しく呼び分け、結果が正しく表示されることを確認する(画面+APIの結合)。

## 【参照テスト計画】

* `docs/P006-test-plan.md` 2.3節

## 【対象モジュール】

* `client/src/pages/MyReservationsPage.tsx`, `server/app/routers/reservations.py`, `server/app/services/reservation_service.py`

## 【前提条件】対象スプリントの全モジュールビルドが成功していること

* U004の全タスクが完了していること

## 【使用するテストデータ】

* 未来の予約1件(第一会議室、翌営業日)、過去の予約1件(第一会議室、前営業日。テストデータ投入時にDBへ直接INSERTして用意する)

## 【事前準備】

* 上記2件の予約をいずれも同一ユーザーの予約として用意し、そのユーザーでログインする

## 【実行手順】

1. S05を既定(今後の予約)で開く
2. フィルタを「過去の予約」に切り替える
3. フィルタを「すべて」に切り替える

## 【実行コマンド】

* `cd client && node --import tsx --test tests/integration/mylistPeriodFilter.test.tsx`

## 【期待結果】

* 手順1: 未来の予約のみ表示
* 手順2: 過去の予約のみ表示
* 手順3: 両方表示

## 【合否判定基準】

* 上記すべてを満たせばPASS

## 【失敗時に記録する内容】

* 実際の表示内容、APIへの実際のクエリパラメータ

## 【修正禁止事項】

* アプリケーションコードを修正しない。テストコードをその場で都合よく変更しない。失敗したテストをスキップしない。期待値を変更して成功扱いにしない。同じ失敗に対して場当たり的な再テストを繰り返さない。

## 【次タスクへ進む条件】

* 結果を記録したらT013へ進む(U005のテストタスクへ移行)

## 重要:

* アプリケーションコードを修正しないでください。
* テスト失敗時に、その場で修正して再テストしないでください。
* テスト失敗時は、失敗内容をテスト記録に残してください。
* このテストタスクの結果を記録したら、Executor Stepの停止条件に該当しない限り、次のテストタスクに自動的に進んでください。
