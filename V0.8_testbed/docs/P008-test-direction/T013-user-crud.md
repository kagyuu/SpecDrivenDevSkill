あなたはExecutor(実装担当)です。以下は1テストタスク分の作業範囲と完了条件を定義したものです。実施後は、結果(PASS/FAIL/BLOCKED/NOT RUNいずれであっても)を記録したうえで、Executor Stepの「停止条件」(`SKILL.md` 参照)に該当しない限り、`docs/P008-test-direction.md` のWBSに従って自動的に次のテストタスクへ進んでください。人間の指示を待って停止しないでください。

# 【テストID】T013

## 【目的】

* S07ユーザー管理画面からユーザーを登録・編集・無効化すると、DBに反映され画面にも反映されることを確認する(画面+API+DBの結合)。

## 【参照テスト計画】

* `docs/P006-test-plan.md` 2.3節

## 【対象モジュール】

* `client/src/pages/UserManagementPage.tsx`, `server/app/routers/users.py`, `server/app/services/user_service.py`, SQLite(`users`テーブル)

## 【前提条件】対象スプリントの全モジュールビルドが成功していること

* U005の全タスク(`docs/P007-impl-direction/U005-user-management-and-hardening.md`)が完了していること

## 【使用するテストデータ】

* 社員ID `e1001`、氏名「山田太郎」、パスワード `Passw0rd!`、権限「一般」

## 【事前準備】

* 管理者アカウントでログインしS07を開く

## 【実行手順】

1. テストデータで新規登録する
2. 一覧に「山田太郎」が表示されることを確認する
3. 氏名を「山田次郎」に編集する(パスワードは空欄のまま)
4. 「無効」に切り替える
5. SQLiteを直接クエリし `users` テーブルの該当行を確認する

## 【実行コマンド】

* `cd client && node --import tsx --test tests/integration/userCrud.test.tsx`

## 【期待結果】

* 手順1: 201
* 手順2: 一覧に反映
* 手順3: 200、`name='山田次郎'`、`password_hash` が変更前と同一(パスワード未変更)
* 手順4: 200、`is_active=false`
* 手順5: DBの内容が上記と一致する

## 【合否判定基準】

* 上記すべてを満たせばPASS

## 【失敗時に記録する内容】

* 実際の画面表示、DBの実際の内容

## 【修正禁止事項】

* アプリケーションコードを修正しない。テストコードをその場で都合よく変更しない。失敗したテストをスキップしない。期待値を変更して成功扱いにしない。同じ失敗に対して場当たり的な再テストを繰り返さない。

## 【次タスクへ進む条件】

* 結果を記録したらT014へ進む

## 重要:

* アプリケーションコードを修正しないでください。
* テスト失敗時に、その場で修正して再テストしないでください。
* テスト失敗時は、失敗内容をテスト記録に残してください。
* このテストタスクの結果を記録したら、Executor Stepの停止条件に該当しない限り、次のテストタスクに自動的に進んでください。
