あなたはExecutor(実装担当)です。以下は1テストタスク分の作業範囲と完了条件を定義したものです。実施後は、結果(PASS/FAIL/BLOCKED/NOT RUNいずれであっても)を記録したうえで、Executor Stepの「停止条件」(`SKILL.md` 参照)に該当しない限り、`docs/P008-test-direction.md` のWBSに従って自動的に次のテストタスクへ進んでください。人間の指示を待って停止しないでください。

# 【テストID】T014

## 【目的】

* S07から自分自身を無効化しようとした場合、および最後の管理者を無効化しようとした場合に、それぞれ適切なエラーメッセージが画面に表示されることを確認する(画面+APIの結合、エラー系シナリオ)。

## 【参照テスト計画】

* `docs/P006-test-plan.md` 2.1〜2.3節

## 【対象モジュール】

* `client/src/pages/UserManagementPage.tsx`, `server/app/routers/users.py`, `server/app/services/user_service.py`

## 【前提条件】対象スプリントの全モジュールビルドが成功していること

* U005の全タスクが完了していること。管理者アカウントが1件のみ(シードデータの`admin`のみ)の状態であること。

## 【使用するテストデータ】

* シードデータの管理者アカウント(`admin`)自身

## 【事前準備】

* 管理者アカウント(`admin`)でログインしS07を開く

## 【実行手順】

1. 自分自身(`admin`)の行の「無効」への切り替えを試みる
2. 表示されるエラーを確認する
3. (自己無効化とは別の経路の確認として)`admin` の権限を「一般」に変更しようとする操作は行わず、`admin` を無効化しようとする操作のみで「最後の管理者保護」も同時に検証されることを確認する(`admin`が唯一の管理者であるため、本シナリオは自己無効化禁止と最後の管理者保護の両方に該当する。判定順序は`docs/P003-backend-spec.md` 4.11〜4.11.2節のとおり自己無効化禁止が先に評価されるため、エラーコードは`SELF_DEACTIVATION_FORBIDDEN`になることを確認する)

## 【実行コマンド】

* `cd client && node --import tsx --test tests/integration/userProtectionDisplay.test.tsx`

## 【期待結果】

* 手順2: 「自分自身は無効化できません」が表示される(`SELF_DEACTIVATION_FORBIDDEN`)
* 手順3: エラーコードが `SELF_DEACTIVATION_FORBIDDEN` であり `LAST_ADMIN_PROTECTED` ではないこと(判定順序の確認)

## 【合否判定基準】

* 上記すべてを満たせばPASS

## 【失敗時に記録する内容】

* 実際に表示されたメッセージ、実際のエラーコード

## 【修正禁止事項】

* アプリケーションコードを修正しない。テストコードをその場で都合よく変更しない。失敗したテストをスキップしない。期待値を変更して成功扱いにしない。同じ失敗に対して場当たり的な再テストを繰り返さない。

## 【次タスクへ進む条件】

* 結果を記録したらT015へ進む

## 重要:

* アプリケーションコードを修正しないでください。
* テスト失敗時に、その場で修正して再テストしないでください。
* テスト失敗時は、失敗内容をテスト記録に残してください。
* このテストタスクの結果を記録したら、Executor Stepの停止条件に該当しない限り、次のテストタスクに自動的に進んでください。
