あなたはExecutor(実装担当)です。以下は1テストタスク分の作業範囲と完了条件を定義したものです。実施後は、結果(PASS/FAIL/BLOCKED/NOT RUNいずれであっても)を記録したうえで、Executor Stepの「停止条件」(`SKILL.md` 参照)に該当しない限り、`docs/P008-test-direction.md` のWBSに従って自動的に次のテストタスクへ進んでください。人間の指示を待って停止しないでください。

# 【テストID】T002

## 【目的】

* 誤った社員ID・パスワードでのログイン試行時に、S01画面が `POST /api/auth/login` の401レスポンスを受けて正しくエラーメッセージを表示することを確認する(画面とAPIの結合)。

## 【参照テスト計画】

* `docs/P006-test-plan.md` 2.2節(フロントエンド画面のAPI呼び出し結果に応じた表示)

## 【対象モジュール】

* `client/src/pages/LoginPage.tsx`, `client/src/api/authApi.ts`, `server/app/routers/auth.py`

## 【前提条件】対象スプリントの全モジュールビルドが成功していること

* U001の全タスクが完了していること

## 【使用するテストデータ】

* 存在しない社員ID(`user_id=nonexistent`)、およびシードデータの管理者IDに誤ったパスワード(`user_id=admin`, `password=wrongpass`)の2パターン

## 【事前準備】

* テスト用サーバーを起動し、フロントエンドのテスト環境(jsdom)からAPIをモックせず実サーバーに対して呼び出す結合テストとして実装する

## 【実行手順】

1. `LoginPage` に存在しない社員IDでログインを試行する
2. 表示されるエラーメッセージを確認する
3. `LoginPage` に正しい社員ID・誤ったパスワードでログインを試行する
4. 表示されるエラーメッセージを確認する

## 【実行コマンド】

* `cd client && node --import tsx --test tests/integration/loginFailure.test.tsx`

## 【期待結果】

* 手順2・手順4いずれも画面に「社員IDまたはパスワードが正しくありません」が表示され、両ケースで同一のメッセージであること(`docs/P002-frontend-spec.md` 3.1節、アカウント存在の推測を防ぐ仕様)
* 画面遷移が発生しないこと(S01に留まる)

## 【合否判定基準】

* 上記をすべて満たせばPASS

## 【失敗時に記録する内容】

* 実際に表示されたメッセージ、2パターンのメッセージが異なっていた場合はその内容

## 【修正禁止事項】

* アプリケーションコードを修正しない。テストコードをその場で都合よく変更しない。失敗したテストをスキップしない。期待値を変更して成功扱いにしない。同じ失敗に対して場当たり的な再テストを繰り返さない。

## 【次タスクへ進む条件】

* 結果を記録したらT003へ進む

## 重要:

* アプリケーションコードを修正しないでください。
* テスト失敗時に、その場で修正して再テストしないでください。
* テスト失敗時は、失敗内容をテスト記録に残してください。
* このテストタスクの結果を記録したら、Executor Stepの停止条件に該当しない限り、次のテストタスクに自動的に進んでください。
