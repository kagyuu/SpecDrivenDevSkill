# 結合テスト実行記録 — 2026-08-05 11:52 (UTC)

> 本記録は `spec-driven-dev` Skill フェーズP103(結合テスト実行)の成果物です。
> 記載形式は `TEMPLATE-test-record.md` に従います。
> 対象は Sprint 1(U001)・Sprint 2(U002)に閉じた結合テスト **T001〜T010** です。
> **T011〜T018 は Sprint 3(U003)・Sprint 4(U004)の実装が未着手のため本回の対象外**であり、実行していません(`docs/P008-test-direction.md` の該当行は `[ ]` のまま残しています)。

## 共通の実行環境・前提

| 項目 | 値 |
| --- | --- |
| 実行者 | Executor (AI) |
| 実行環境 | Linux 6.18.5 / Python 3.11.15 / Node.js v22.22.2 / Starlette 1.0.0 / Pydantic 2.13.3 / httpx 0.28.1 / uvicorn 0.46.0 |
| 対象ブランチ/コミット | Gitリポジトリ未初期化(作業ツリー `/tmp/work/V0.7_testbed` の実行時点の内容) |
| 事前ビルド結果 | ビルド工程なし(Python・ESモジュールとも変換不要)。前提となる単体テストは**サーバー111件・クライアント46件すべてPASS**(下記) |
| サーバー単体テスト | `cd server && python3 -m unittest discover -s tests -t .` → **119 tests OK**(うち結合テスト8件を含む。単体テストのみでは111件) |
| クライアント単体テスト | `cd client && node --test 'tests/*.js'` → **46 tests / 46 pass / 0 fail** |
| DBの扱い | 各テストは開始時にDBファイルを作り直し、基準データセット(`server/tests/fixtures/seed.py`)を投入する。**T004のみ同一DBファイルを使い回す** |

* HTTPクライアントは `httpx` が利用可能だったため、`docs/P006-test-plan.md` 1.1 に従い `httpx`(Starlette `TestClient` 経由)を用いた。T003・T004・T010 は指示どおり **実際に `uvicorn` のサーバープロセスを起動**して実行した。
* `docs/P006-test-plan.md` 5章の基準データセットのうち **予約1件は投入していない**(`reservations` テーブルは Sprint 3 の担当で未実装のため)。T001〜T010 はいずれも予約データを必要としない。
* 基準データセットの `admin001` の氏名は、P006 5章では「管理 太郎」だが、実際にはアプリ起動時のシード(P003 3.6)が作る「初期管理者」の行がそのまま使われる。T001〜T010 の期待結果は `admin001` の氏名に依存しないため判定に影響しない(申し送り事項として記録する)。

---

## T001: ログインとセッションCookieの発行

- 実行日時: 2026-08-05 11:52 (UTC)
- 実行者: Executor (AI)
- 対象ブランチ/コミット: 作業ツリー(Git未初期化)
- 参照したテスト指示: docs/P008-test-direction/T001-login-session.md
- 実行環境: 上記「共通の実行環境」に同じ
- 事前ビルド結果: 成功(Sprint 1 単体テスト全件PASS)
- 実行コマンド: `cd server && python3 -m unittest tests.integration.test_t001_login_session -v`
- 結果: PASS
- 期待結果: 手順1=200かつ `{"user":{"user_id":"user001","name":"山田 太郎","role":"general"}}` / 手順2=`Set-Cookie` に `sid=` `HttpOnly` `SameSite=Lax` `Secure` `Path=/` / 手順3=200で `user_id` が `user001` / 手順4=`sessions` に1件 / 手順5=ログアウト204、その後 401 `UNAUTHENTICATED` かつ `sessions` 0件
- 実際の結果: 全項目が期待どおり。`Ran 1 test in 0.387s OK`
- 失敗ログ抜粋: -
- 再現手順: -
- 影響がありそうなモジュール: -
- 次工程への申し送り: なし

## T002: 認証失敗と未認証アクセス

- 実行日時: 2026-08-05 11:52 (UTC)
- 実行者: Executor (AI)
- 対象ブランチ/コミット: 作業ツリー(Git未初期化)
- 参照したテスト指示: docs/P008-test-direction/T002-auth-failure.md
- 実行環境: 上記「共通の実行環境」に同じ
- 事前ビルド結果: 成功
- 実行コマンド: `cd server && python3 -m unittest tests.integration.test_t002_auth_failure -v`
- 結果: PASS
- 期待結果: 手順1〜3=401 `AUTH_FAILED` かつ3件の `message` が完全に同一 / 手順4=401 `UNAUTHENTICATED` / 手順5=401 / 手順6=期限切れセッション行が削除 / 手順7=400 `VALIDATION_ERROR` で `details[0].field == "user_id"`、日本語メッセージ
- 実際の結果: 全項目が期待どおり。手順1〜3の `message` は3件とも「ユーザーIDまたはパスワードが正しくありません。」で一致。手順7のメッセージは「ユーザーIDは半角英数字4〜20文字で入力してください。」。`Ran 1 test in 0.545s OK`
- 失敗ログ抜粋: -
- 再現手順: -
- 影響がありそうなモジュール: -
- 次工程への申し送り: なし

## T003: S01ログイン画面とログインAPIの接続

- 実行日時: 2026-08-05 11:52 (UTC)
- 実行者: Executor (AI)
- 対象ブランチ/コミット: 作業ツリー(Git未初期化)
- 参照したテスト指示: docs/P008-test-direction/T003-s01-login-api.md
- 実行環境: 上記に同じ。**実サーバー(`uvicorn`)をサブプロセスで起動**し、`fetch` をそのサーバーに向けた(指示の「実サーバーに向ける方を優先する」に従う)。ブラウザのCookie保持は `client/tests/helpers/server.js` の `makeFetch` で模した
- 事前ビルド結果: 成功(クライアント単体テスト全件PASS)
- 実行コマンド: `cd client && node --test tests/integration/test_t003_s01_login_api.js`
- 結果: PASS
- 期待結果: 手順1=4要素が存在しパスワード欄が `type="password"` / 手順2・3=APIを呼ばずフィールド直下に日本語エラー / 手順4=APIが呼ばれエラー領域に「ユーザーIDまたはパスワードが正しくありません。」、画面はS01のまま / 手順5=APIが呼ばれ遷移先が `#/calendar`、送信中はボタン非活性
- 実際の結果: 5テストすべてPASS(`# tests 5 / # pass 5 / # fail 0`)。手順5では発行されたCookieで `GET /api/me` が200を返し `user001` を得られることも併せて確認した
- 失敗ログ抜粋: -
- 再現手順: -
- 影響がありそうなモジュール: -
- 次工程への申し送り: 結合テストの配置は `client/tests/integration/` で確定した(T003指示の★FIXME★を解消)。ただし**クライアントの単体テスト実行コマンド `node --test tests` は本環境の Node.js v22.22.2 では動作しない**(ディレクトリ指定がモジュール解決エラーになる)。単体テストは `node --test 'tests/*.js'` で実行した。詳細は `docs/P007-impl-direction.md` 5章 未解決事項#6

## T004: 既存DBに対する再起動とマイグレーション差分適用

- 実行日時: 2026-08-05 11:52 (UTC)
- 実行者: Executor (AI)
- 対象ブランチ/コミット: 作業ツリー(Git未初期化)
- 参照したテスト指示: docs/P008-test-direction/T004-restart-migration.md
- 実行環境: 上記に同じ。**同一DBファイルに対して `uvicorn` プロセスの起動・停止を3回**繰り返した(このテストのみDBを使い回す)
- 事前ビルド結果: 成功
- 実行コマンド: `cd server && python3 -m unittest tests.integration.test_t004_restart_migration -v`
- 結果: PASS
- 期待結果: 手順1=起動成功、`schema_migrations` の行数=マイグレーションファイル数、`users` に初期管理者1件 / 手順3=エラーなく再起動 / 手順4=行数が増えない / 手順5=初期管理者でログイン200 / 手順6=3回目も起動成功
- 実際の結果: 1回目 `schema_migrations` 2行(`001-init.sql` / `002-rooms.sql`= ファイル数2と一致)、`users` 1件(`admin001`)。2回目・3回目とも `duplicate column name` 等の異常終了なく起動し、`schema_migrations` と `users` の行数は1回目から**変化なし**。2回目起動後の初期管理者ログインは200。`Ran 1 test in 1.904s OK`
- 失敗ログ抜粋: -
- 再現手順: -
- 影響がありそうなモジュール: -
- 次工程への申し送り: 本テストがPASSしても、システムレベルの再起動耐性・データ永続は `docs/P009-acceptance-direction/A007-restart-resilience.md` で改めて確認すること(T004指示の「役割分担」のとおり、本テストはA007の代替ではない)

## T005: 会議室CRUDの一気通貫

- 実行日時: 2026-08-05 11:52 (UTC)
- 実行者: Executor (AI)
- 対象ブランチ/コミット: 作業ツリー(Git未初期化)
- 参照したテスト指示: docs/P008-test-direction/T005-room-crud.md
- 実行環境: 上記に同じ
- 事前ビルド結果: 成功(Sprint 2 単体テスト全件PASS)
- 実行コマンド: `cd server && python3 -m unittest tests.integration.test_t005_room_crud -v`
- 結果: PASS
- 期待結果: 手順1=201で `room_id` 採番・`description` 保存 / 手順2=200で1件 / 手順3=200で `capacity` 12 / 手順4=反映 / 手順5=204で `is_active`=0(行は残る) / 手順6=既定に出ず `include_inactive=true` に出る / 手順7=204(冪等)
- 実際の結果: 全項目が期待どおり。手順5では `rooms` の行が物理削除されていないことをSQLで直接確認した。`Ran 1 test in 0.414s OK`
- 失敗ログ抜粋: -
- 再現手順: -
- 影響がありそうなモジュール: -
- 次工程への申し送り: なし

## T006: 会議室名の一意性と無効化後の再登録

- 実行日時: 2026-08-05 11:52 (UTC)
- 実行者: Executor (AI)
- 対象ブランチ/コミット: 作業ツリー(Git未初期化)
- 参照したテスト指示: docs/P008-test-direction/T006-room-name-uniqueness.md
- 実行環境: 上記に同じ
- 事前ビルド結果: 成功
- 実行コマンド: `cd server && python3 -m unittest tests.integration.test_t006_room_name_uniqueness -v`
- 結果: PASS
- 期待結果: 手順2=409 `DUPLICATE_KEY`「同じ名前の会議室がすでに登録されています。」 / 手順3=409 / 手順4=204 / 手順5=**201** / 手順6=`会議室A` が2件(`is_active` が 0 と 1)
- 実際の結果: 全項目が期待どおり。手順5は201で新しい `room_id` が採番され、部分ユニークインデックス `uq_rooms_name_active` が意図どおり有効な行のみを対象にしていることを確認した。`Ran 1 test in 0.409s OK`
- 失敗ログ抜粋: -
- 再現手順: -
- 影響がありそうなモジュール: -
- 次工程への申し送り: なし

## T007: 管理者専用APIの認可

- 実行日時: 2026-08-05 11:52 (UTC)
- 実行者: Executor (AI)
- 対象ブランチ/コミット: 作業ツリー(Git未初期化)
- 参照したテスト指示: docs/P008-test-direction/T007-admin-authz.md
- 実行環境: 上記に同じ
- 事前ビルド結果: 成功
- 実行コマンド: `cd server && python3 -m unittest tests.integration.test_t007_admin_authz -v`
- 結果: PASS
- 期待結果: 手順1〜6の8本すべてが 403 `FORBIDDEN`(404にマスクしない)、DBの状態が不変 / 手順7=200
- 実際の結果: 8本すべて 403 `FORBIDDEN`。実行前後で `rooms` / `users` の件数、および対象会議室の `is_active` が不変であることを確認した。手順7の `GET /api/rooms`(既定)は200。`Ran 1 test in 0.404s OK`
- 失敗ログ抜粋: -
- 再現手順: -
- 影響がありそうなモジュール: -
- 次工程への申し送り: なし

## T008: ユーザーCRUDと最後の管理者の保護

- 実行日時: 2026-08-05 11:52 (UTC)
- 実行者: Executor (AI)
- 対象ブランチ/コミット: 作業ツリー(Git未初期化)
- 参照したテスト指示: docs/P008-test-direction/T008-user-crud-last-admin.md
- 実行環境: 上記に同じ
- 事前ビルド結果: 成功
- 実行コマンド: `cd server && python3 -m unittest tests.integration.test_t008_user_crud_last_admin -v`
- 結果: PASS
- 期待結果: 手順1=201でレスポンスに `password` なし・`password_hash` が `scrypt$` 始まり / 手順2=409 / 手順3=200 / 手順4=200かつ元のパスワードでログイン可 / 手順5=409 `CONSTRAINT_VIOLATION` / 手順6=409(自分自身) / 手順7=204かつ無効化ユーザーのCookieで401 / 手順8=204(冪等)
- 実際の結果: 全項目が期待どおり。手順6のメッセージは「自分自身を無効化することはできません。」。手順7では無効化と同時に当該ユーザーの `sessions` が削除され、既存Cookieでの `GET /api/me` が401になることを確認した。`Ran 1 test in 0.630s OK`
- 失敗ログ抜粋: -
- 再現手順: -
- 影響がありそうなモジュール: -
- 次工程への申し送り: 最後の有効な管理者を保護するときの `message` はP002・P003に規定が無く、実装側で「最後の有効な管理者を無効化または一般に変更することはできません。」と補った(★FIXME★。`docs/P007-impl-direction.md` 5章 未解決事項#8)。文言を確定する場合はこのテストの期待値も併せて見直すこと

## T009: 参加者候補スコープの権限と射影

- 実行日時: 2026-08-05 11:52 (UTC)
- 実行者: Executor (AI)
- 対象ブランチ/コミット: 作業ツリー(Git未初期化)
- 参照したテスト指示: docs/P008-test-direction/T009-attendee-candidates-scope.md
- 実行環境: 上記に同じ
- 事前ビルド結果: 成功
- 実行コマンド: `cd server && python3 -m unittest tests.integration.test_t009_attendee_candidates_scope -v`
- 結果: PASS
- 期待結果: 手順1=200 / 手順2=各要素のキーが `user_id` と `name` の2つだけ / 手順3=3件で `user003` を含まない / 手順4=403 `FORBIDDEN` / 手順5=200
- 実際の結果: 全項目が期待どおり。返却は `admin001` / `user001` / `user002` の3件で、`role` `is_active` `created_at` `updated_at` `password_hash` のいずれも含まれない(SELECT句のレベルで除外)。`Ran 1 test in 0.441s OK`
- 失敗ログ抜粋: -
- 再現手順: -
- 影響がありそうなモジュール: -
- 次工程への申し送り: なし

## T010: S06・S07画面とマスタAPIの接続

- 実行日時: 2026-08-05 11:52 (UTC)
- 実行者: Executor (AI)
- 対象ブランチ/コミット: 作業ツリー(Git未初期化)
- 参照したテスト指示: docs/P008-test-direction/T010-master-screens-api.md
- 実行環境: 上記に同じ。**実サーバー(`uvicorn`)をサブプロセスで起動**して実行。DOMは外部パッケージを取得できないため `client/tests/helpers/dom-env.js`(本プロジェクト内の最小DOM実装)を用いた
- 事前ビルド結果: 成功
- 実行コマンド: `cd client && node --test tests/integration/test_t010_master_screens_api.js`
- 結果: PASS
- 期待結果: 手順1=会議室2件描画・無効行に `is-inactive`・説明文の列あり / 手順2=一覧に1件追加 / 手順3=変更が反映 / 手順4=会議室名欄直下に「同じ名前の会議室がすでに登録されています。」 / 手順5=S07にユーザー4件と「戻る」リンク / 手順6=画面上部に「自分自身を無効化することはできません。」、一覧は不変 / 手順7=表を描画せず権限メッセージのみ、ヘッダーに管理リンクなし
- 実際の結果: 7テストすべてPASS(`# tests 7 / # pass 7 / # fail 0`)
- 失敗ログ抜粋: -
- 再現手順: -
- 影響がありそうなモジュール: -
- 次工程への申し送り: 手順4の期待文言(APIの `message`)は、P002 2.4 が定める 409 `DUPLICATE_KEY` の既定文言「同じ値がすでに登録されています。」と食い違う。実装は「APIが `message` を返す場合はそれを優先し、無い場合のみP002 2.4の既定文言を使う」として両立させた(`docs/P007-impl-direction.md` 5章 未解決事項#7)。P002 2.4 の記述を更新するかは人間の判断が必要

---

## 集計

| 項目 | 値 |
| --- | --- |
| 実行したテストタスク | T001〜T010(10件) |
| PASS | 10件 |
| FAIL | 0件 |
| BLOCKED | 0件 |
| NOT RUN | T011〜T018(8件。Sprint 3・4 が未実装のため本回の対象外) |
| Reviewer Loop(P201〜)への引き渡し要否 | **本回の範囲(T001〜T010)については不要**(FAIL・BLOCKEDなし)。ただしT011〜T018が未実施のため、P008全体としては未完了 |
