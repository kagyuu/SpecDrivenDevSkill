# 結合テスト実行記録(P103) — T011〜T018

> 本書は `spec-driven-dev` Skill フェーズP103の成果物です。形式は `TEMPLATE-test-record.md` に従います。
> 対象: `docs/P008-test-direction.md` の Sprint 3(T011〜T014)・Sprint 4(T015〜T018)。
> T001〜T010 の記録は `docs/test-records/20260805-1152-test-record.md` にあります(本実行でも退行確認のため再実行し、全件PASSであることを確認済み。第2章参照)。

## 1. テストタスク別の記録

## T011: 予約の登録→一覧→詳細→更新→取消

- 実行日時: 2026-08-05 12:22
- 実行者: Executor Agent (P103)
- 対象ブランチ/コミット: 作業ツリー(Gitリポジトリ管理外)/ U003・U004 完了時点
- 参照したテスト指示: docs/P008-test-direction/T011-reservation-lifecycle.md
- 実行環境: Python 3.11 / Starlette 1.0.0 / Pydantic 2.13.3 / SQLite(標準 `sqlite3`)/ Starlette TestClient、一時ディレクトリの新規DBファイル
- 事前ビルド結果: 成功(Sprint 3 の単体テストは `python3 -m unittest discover -s tests -t .` で全件PASS)
- 実行コマンド: `cd server && python3 -m unittest tests.integration.test_t011_reservation_lifecycle -v`
- 結果: PASS
- 期待結果: 手順1=201かつ `reservation.user_id == "user001"` / 手順2=200・1件・`room_name`/`user_name` あり・`attendees` は空配列 / 手順3=200・`attendees` に `user002` 1件 / 手順4=200・1件 / 手順5=200・件名変更 / 手順6=`attendees` が空配列 / 手順7=204→404 / 手順8=`reservation_attendees` 0件
- 実際の結果: 上記すべて一致(Ran 1 test, OK)
- 失敗ログ抜粋: なし
- 再現手順: なし
- 影響がありそうなモジュール: なし
- 次工程への申し送り: なし

## T012: 予約の重複検出と境界時刻

- 実行日時: 2026-08-05 12:22
- 実行者: Executor Agent (P103)
- 対象ブランチ/コミット: 作業ツリー / U003・U004 完了時点
- 参照したテスト指示: docs/P008-test-direction/T012-reservation-conflict.md
- 実行環境: T011に同じ
- 事前ビルド結果: 成功
- 実行コマンド: `cd server && python3 -m unittest tests.integration.test_t012_reservation_conflict -v`
- 結果: PASS
- 期待結果: 手順1〜3=409 `RESERVATION_CONFLICT`(`error.conflicts` に既存予約の `reservation_id`/`start_time`/`end_time`)/ 手順4・5=201(境界接触)/ 手順6・7=201 / 手順8=200(自己除外)
- 実際の結果: 上記すべて一致(Ran 1 test, OK)。境界接触(11:00-12:00 / 09:00-10:00)がいずれも201であり、判定SQLに `<=` が使われていないこと、および更新時の `exclude_reservation_id` による自己除外が効いていることを確認した
- 失敗ログ抜粋: なし
- 再現手順: なし
- 影響がありそうなモジュール: なし
- 次工程への申し送り: なし

## T013: 収容人数超過と予約の認可

- 実行日時: 2026-08-05 12:22
- 実行者: Executor Agent (P103)
- 対象ブランチ/コミット: 作業ツリー / U003・U004 完了時点
- 参照したテスト指示: docs/P008-test-direction/T013-capacity-and-authz.md
- 実行環境: T011に同じ
- 事前ビルド結果: 成功
- 実行コマンド: `cd server && python3 -m unittest tests.integration.test_t013_capacity_and_authz -v`
- 結果: PASS
- 期待結果: 手順1=400 `CAPACITY_EXCEEDED`(メッセージに「4」)/ 手順2=201 / 手順3=`0` は400・未指定は201 / 手順4=400(`field == "room_id"`)/ 手順5=400(`field == "reserved_date"`)/ 手順6・7=403 `FORBIDDEN` かつDB不変 / 手順8=200 / 手順9=200・204
- 実際の結果: 上記すべて一致(Ran 1 test, OK)
- 失敗ログ抜粋: なし
- 再現手順: なし
- 影響がありそうなモジュール: なし
- 次工程への申し送り: なし

## T014: 今後の予約がある会議室の無効化拒否

- 実行日時: 2026-08-05 12:22
- 実行者: Executor Agent (P103)
- 対象ブランチ/コミット: 作業ツリー / U003・U004 完了時点
- 参照したテスト指示: docs/P008-test-direction/T014-room-deactivate-blocked.md
- 実行環境: T011に同じ
- 事前ビルド結果: 成功
- 実行コマンド: `cd server && python3 -m unittest tests.integration.test_t014_room_deactivate_blocked -v` / `grep -rn "TODO(U003-T2)" server/src || true`
- 結果: PASS
- 期待結果: 手順1=409 `CONSTRAINT_VIOLATION`(メッセージに件数「1」)かつ会議室Aは `is_active=1` のまま / 手順2=204 / 手順3=204(過去の予約は妨げない)/ 手順4=204 / 手順5=`TODO(U003-T2)` の該当なし
- 実際の結果: 上記すべて一致(Ran 2 tests, OK)。`grep -rn "TODO(U003-T2)" server/src` は該当0件(終了コード1、マッチなし)であり、Sprint 2 の暫定実装が本実装に差し替わっていることを確認した
- 失敗ログ抜粋: なし
- 再現手順: なし
- 影響がありそうなモジュール: なし
- 次工程への申し送り: なし

## T015: S02カレンダーと予約APIの接続

- 実行日時: 2026-08-05 12:24
- 実行者: Executor Agent (P103)
- 対象ブランチ/コミット: 作業ツリー / U003・U004 完了時点
- 参照したテスト指示: docs/P008-test-direction/T015-calendar-grid-api.md
- 実行環境: Node.js v22.22.2 / `node --test` / 実サーバー(`python3 -m uvicorn meeting_room.main:app`、一時DBファイル)/ 最小DOM実装 `tests/helpers/dom-env.js`(ADR-001によりjsdomは使用不可)
- 事前ビルド結果: 成功(Sprint 4 のクライアント単体テストは全件PASS)
- 実行コマンド: `cd client && node --test tests/integration/test_t015_calendar_grid_api.js`
- 結果: PASS
- 期待結果: 手順1=各1回 / 手順2=有効な会議室2列のみ / 手順3=10:00・10:30 に「山田 太郎 / 定例会議」、11:00 は空き / 手順4=参加予定人数の非表示 / 手順5=列1つ・API追加呼び出し0回 / 手順6=`date_from` が+7日 / 手順7=`#/reservations/new?room_id={B}&date={本日+1日}&start=14:00` / 手順8=`#/reservations/{reservation_id}` / 手順9=遷移せずメッセージ表示
- 実際の結果: 上記すべて一致(9 tests / 9 pass / 0 fail)。手順7の遷移先は `end=14:30` を含む形(`#/reservations/new?room_id={B}&date={本日+1日}&start=14:00&end=14:30`)であり、指示の「終了は開始+30分として扱われる」を満たす
- 失敗ログ抜粋: なし
- 再現手順: なし
- 影響がありそうなモジュール: なし
- 次工程への申し送り: なし

## T016: S02→S03の値の引き継ぎと登録完了

- 実行日時: 2026-08-05 12:26
- 実行者: Executor Agent (P103)
- 対象ブランチ/コミット: 作業ツリー / U003・U004 完了時点
- 参照したテスト指示: docs/P008-test-direction/T016-create-flow.md
- 実行環境: T015に同じ
- 事前ビルド結果: 成功
- 実行コマンド: `cd client && node --test tests/integration/test_t016_create_flow.js`
- 結果: **FAIL**(8手順中7手順がPASS、手順7がFAIL)
- 期待結果:
  - 手順1: 会議室A・日付「本日+1日」・開始 14:00・終了 14:30 が初期値 → 一致(PASS)
  - 手順2: 開始 09:00 / 終了 18:00 が自動入力 → 一致(PASS)
  - 手順3: 開始が 10:00 のまま保持され、チェックは自動で外れない → 一致(PASS)
  - 手順4: APIが呼ばれず、件名欄の直下に「件名を入力してください。」 → 一致(PASS)
  - 手順5: `GET /api/users?scope=attendee_candidates` の結果が候補として描画 → 一致(PASS)
  - 手順6: 409 が返り、重複エラー領域に「選択した時間帯はすでに予約されています。」と競合時間帯(10:00-11:00)。画面はS03のまま → 一致(PASS)
  - **手順7: 400 `CAPACITY_EXCEEDED` が返り、収容人数超過エラー領域に表示される → 不一致(FAIL)**
  - 手順8: 201。`#/calendar` へ戻り「予約を登録しました。」が表示され、S02のグリッドに新しい予約が現れる → 一致(PASS)
- 実際の結果: 手順7で `POST /api/reservations` が**そもそも呼ばれない**。参加予定人数 11 は会議室A(収容10)の収容人数を超えるため、送信前のクライアント側バリデーション(P002 3.3「会議室選択の変更時と送信時にクライアント側で検証する」/ U004-T2 の単体テスト指示「参加予定人数が収容人数+1 → APIを呼ばずフィールド直下にエラー表示」)が先に成立し、メッセージは**参加予定人数欄の直下**(`[data-error-for="attendee_count"]`)に「参加予定人数が会議室の収容人数(10名)を超えています。」として表示される。結果として、収容人数超過エラー領域(`[data-role="capacity-error"]`)は空のままとなる
- 失敗ログ抜粋:

```
not ok 7 - 手順7: 14:00-15:00 / 参加予定人数11 は 400 CAPACITY_EXCEEDED になり所定領域に出る
  failureType: 'testCodeFailure'
  error: |-
    POST /api/reservations が呼ばれること
    1 !== 2
  code: 'ERR_ASSERTION'
  expected: 2
  actual: 1
  operator: 'strictEqual'
  location: 'client/tests/integration/test_t016_create_flow.js:141:1'
```

- 再現手順: 上記の実行コマンドを再実行すると同じ手順7で同じ差異が再現する(1回だけ再実行して確認済み。3回以上は繰り返していない)。手動での再現手順は、S03で会議室A(収容10)を選び参加予定人数に 11 を入力して「登録」を押す → APIは呼ばれず、参加予定人数欄の直下にエラーが出る
- 影響がありそうなモジュール: `client/src/components/reservation-form.js`(`validate` / `showCapacityError`)、`client/src/views/s03-reservation-new.js`(`submit`)、`client/src/lib/validation.js`(`validateAttendeeCount`)。サーバー側 `services/reservation_service.py` は正しく 400 `CAPACITY_EXCEEDED` を返す(T013 手順1でPASS済み)ため、サーバー側の欠陥ではない
- 次工程への申し送り: **本件は実装の欠陥ではなく、設計文書間の矛盾である可能性が高い。** 関連する仕様箇所は次のとおりで、Reviewer Loop(P202)ではまずどちらを正とするかの決定が必要である。
  - `docs/P002-frontend-spec.md` 3.3「参加予定人数の検証タイミング: 会議室選択の変更時と送信時にクライアント側で検証する。…サーバー側でも同じ検証を行い、超過時は 400 `CAPACITY_EXCEEDED` を返す」+ 同 2.4「400 `VALIDATION_ERROR` → 該当入力欄の直下」
  - `docs/P007-impl-direction/U004-reservation-ui.md` U004-T2【Unit Test内容】異常系「参加予定人数が収容人数+1 → **APIを呼ばず**フィールド直下にエラー表示」
  - `docs/P008-test-direction/T016-create-flow.md` 手順7・期待結果「400 `CAPACITY_EXCEEDED` が返り、収容人数超過エラー領域に表示される」
  - クライアント側検証を仕様どおり残す限り、会議室が選択済みのケースでは 400 `CAPACITY_EXCEEDED` に到達できない(収容人数超過エラー領域は、会議室未選択のまま送信した場合など、クライアント側で判定できない経路でのみ表示される)。想定される解消案は (a) クライアント側の収容人数超過メッセージも収容人数超過エラー領域に表示する、(b) T016 手順7の期待結果をフィールド直下表示に改める、のいずれか。詳細は `docs/P007-impl-direction.md` 5章「未解決事項」#18 にも記載した
- 備考: 本フェーズ(P103)の規定に従い、アプリケーションコード・テストコードの修正、期待値の変更、スキップは一切行っていない

## T017: S04の編集・取消と権限による表示制御

- 実行日時: 2026-08-05 12:26
- 実行者: Executor Agent (P103)
- 対象ブランチ/コミット: 作業ツリー / U003・U004 完了時点
- 参照したテスト指示: docs/P008-test-direction/T017-detail-edit-cancel.md
- 実行環境: T015に同じ
- 事前ビルド結果: 成功
- 実行コマンド: `cd client && node --test tests/integration/test_t017_detail_edit_cancel.js`
- 結果: PASS
- 期待結果: 手順1=表示項目が揃い編集・取消ボタンあり(参加者「鈴木 花子」・参加予定人数「-」)/ 手順2=`PUT` 後に `#/calendar` へ戻り「予約を更新しました。」/ 手順3=`DELETE` が呼ばれない / 手順4=`DELETE` 後に `#/calendar` へ戻り「予約を取り消しました。」/ 手順5=編集・取消ボタンが無い / 手順6=ボタンが表示される / 手順7=「対象のデータが見つかりません。削除された可能性があります。」
- 実際の結果: 上記すべて一致(7 tests / 7 pass / 0 fail)。重大度「高」の観点である手順5(第三者にボタンが出ないこと)もPASS
- 失敗ログ抜粋: なし
- 再現手順: なし
- 影響がありそうなモジュール: なし
- 次工程への申し送り: なし

## T018: S05の期間フィルタ

- 実行日時: 2026-08-05 12:26
- 実行者: Executor Agent (P103)
- 対象ブランチ/コミット: 作業ツリー / U003・U004 完了時点
- 参照したテスト指示: docs/P008-test-direction/T018-my-reservations-filter.md
- 実行環境: T015に同じ。過去日の予約は指示どおりリポジトリ経由(`reservations_repo.insert`)で直接INSERTした
- 事前ビルド結果: 成功
- 実行コマンド: `cd client && node --test tests/integration/test_t018_my_reservations_filter.js`
- 結果: PASS
- 期待結果: 手順1=`period=upcoming` が呼ばれる / 手順2=2行(日付昇順)・列4つ・`user002` の予約を含まない / 手順3=`period=past` で再取得 / 手順4=1行 / 手順5=`#/reservations/{reservation_id}` / 手順6=「該当する予約はありません。」
- 実際の結果: 上記すべて一致(5 tests / 5 pass / 0 fail)。手順6は、テスト指示の★FIXME★に従い「予約を持たない**有効**ユーザー」として `user004` を別途作成して実施した(基準データセットの `user003` は無効ユーザーのためログインできない)
- 失敗ログ抜粋: なし
- 再現手順: なし
- 影響がありそうなモジュール: なし
- 次工程への申し送り: なし

## 2. 退行確認(T001〜T010 の再実行)

Sprint 3・4 の実装によって Sprint 1・2 の結合テストが壊れていないことを確認するため、既に `[x]` の T001〜T010 も同じ実行コマンドで再実行した。

| テストID | 実行コマンド | 結果 |
| --- | --- | --- |
| T001 | `cd server && python3 -m unittest tests.integration.test_t001_login_session -v` | PASS (Ran 1 test, OK) |
| T002 | `cd server && python3 -m unittest tests.integration.test_t002_auth_failure -v` | PASS (Ran 1 test, OK) |
| T003 | `cd client && node --test tests/integration/test_t003_s01_login_api.js` | PASS (5 tests / 5 pass) |
| T004 | `cd server && python3 -m unittest tests.integration.test_t004_restart_migration -v` | PASS (Ran 1 test, OK) |
| T005 | `cd server && python3 -m unittest tests.integration.test_t005_room_crud -v` | PASS (Ran 1 test, OK) |
| T006 | `cd server && python3 -m unittest tests.integration.test_t006_room_name_uniqueness -v` | PASS (Ran 1 test, OK) |
| T007 | `cd server && python3 -m unittest tests.integration.test_t007_admin_authz -v` | PASS (Ran 1 test, OK) |
| T008 | `cd server && python3 -m unittest tests.integration.test_t008_user_crud_last_admin -v` | PASS (Ran 1 test, OK) |
| T009 | `cd server && python3 -m unittest tests.integration.test_t009_attendee_candidates_scope -v` | PASS (Ran 1 test, OK) |
| T010 | `cd client && node --test tests/integration/test_t010_master_screens_api.js` | PASS (7 tests / 7 pass) |

* T004(同一DBファイルを使い回す再起動確認)がPASSであることにより、`003-reservations.sql` を追加した後も差分適用型マイグレーション(ADR-009)が冪等であることを確認できている。
* 併せて、同一DBファイルに対して `db.apply_migrations()` を連続2回実行する確認も行った。1回目の戻り値は `['001-init.sql', '002-rooms.sql', '003-reservations.sql']`、2回目は `[]` であり、`schema_migrations` の行数は3のまま増えなかった。

## 3. 単体テストの状況(参考)

| 対象 | 実行コマンド | 結果 |
| --- | --- | --- |
| バックエンド単体+結合(discover) | `cd server && python3 -m unittest discover -s tests -t .` | 226 tests / OK(内訳: 単体213件・結合13件。`discover` は `tests/integration/` も走査するため両方を含む) |
| フロントエンド単体 | `cd client && node --test 'tests/*.js'` | 126 tests / 126 pass / 0 fail |

* フロントエンド単体テストの実行コマンドは、`docs/P007-impl-direction.md` 5章「未解決事項」#6 のとおり `node --test 'tests/*.js'` に読み替えて実行している(実行環境の Node.js v22.22.2 ではディレクトリ指定が動作しないため)。

## 4. 集計と引き渡し

| 項目 | 値 |
| --- | --- |
| 実施件数 | 18件(T001〜T018。うち本フェーズの新規実施は T011〜T018 の8件) |
| PASS | 17件 |
| FAIL | **1件(T016)** |
| BLOCKED | 0件 |
| NOT RUN | 0件 |
| Reviewer Loopへの引き渡し要否 | **要**(T016 のFAILが未解消のため) |

* P103の規定(`SKILL-P103-integration-test.md`)に従い、FAILとなったT016についてアプリケーションコード・テストコードの修正は行っていない。修正はReviewer Loop(P201〜P205)で行う。
