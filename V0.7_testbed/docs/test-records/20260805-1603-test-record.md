# 受け入れ結合テスト実行記録(P201 第1回 / A001〜A012)

> 記録形式は `TEMPLATE-test-record.md` に従う。実行フェーズ: P201(実装横断レビュー)。
> P009 2章の共通指示に従い、全モジュールのビルドを先に行い、失敗時に修正しない方針で実行した。

## 事前ビルド(P009 2章)

| コマンド | 終了コード |
| --- | --- |
| `cd server && python3 -m compileall -q src` | 0 |
| `cd client && node --check` を `src/**/*.js` 全17ファイルに対して実行(★FIXME★ の読み替え指示に従い、ESモジュールを個別に構文検査した) | すべて 0 |

* 実行環境: Linux 6.18.5 / CPU 2コア / Python 3.11.15 / Node v22.22.2 / uvicorn 0.46.0 / starlette 1.0.0 / httpx 0.28.1
* テスト実行方式: **実サーバープロセス(uvicorn)+ 実HTTPクライアント**。
  * サーバー側: `server/tests/acceptance/support.py`(`subprocess` で uvicorn を起動、`urllib.request` でHTTP)。P008の `TestClient`(同一プロセス内呼び出し)ではA007・A009・A012の確認対象(停止・再起動・同時接続)を満たせないため、新たに用意した。
  * クライアント側: `client/tests/helpers/server.js`(既存)+ `client/tests/acceptance/support.js`(新規。基準データセットを実APIで投入)。画面は `dom-env.js` の最小DOM上で描画関数・イベントハンドラを実サーバーに接続して実行(P006 1.1 / P009 3章)。

---

## A001: 予約業務フロー全体

- 実行日時: 2026-08-05T16:03Z
- 実行者: Reviewer Loop (P201, 1回目)
- 対象ブランチ/コミット: Git管理外(ワークツリー直接。`docs/.inprogress` = P201)
- 参照したテスト指示: docs/P009-acceptance-direction/A001-reservation-business-flow.md
- 実行環境: 上記共通
- 事前ビルド結果: 成功(終了コード0)
- 実行コマンド: `cd client && node --test tests/acceptance/test_a001_business_flow.js`
- 結果: **FAIL**
- 期待結果: 手順1〜10のすべてがエラーなく順に完了する。手順5「S02に戻り、13:00・13:30 のセルに予約が表示されることを確認する(終了時刻は 13:30 とする)」。
- 実際の結果: 手順1〜4・6〜10 はすべて期待どおり(9/10 PASS)。**手順5のみFAIL**。13:00 のセルには「山田 太郎 / 定例会議」が表示されるが、**13:30 のセルは空き(`+`)** である。
- 失敗ログ抜粋:

```text
not ok 5 - 手順5: S02に戻り 13:00・13:30 のセルに予約が表示される(終了時刻は13:30)
  error: |-
    13:30 のセルにも予約が表示されること(A001 手順5)
    + actual - expected
    + '+'
    - '山田 太郎 / 定例会議'
  operator: 'strictEqual'
```

- 再現手順: `user001` でログイン → S02(本日+1日の週)→ 会議室A 13:00 の空きセルをクリック → S03で件名「定例会議」/ 参加者 user002 / 参加予定人数6 / 備考を入力して登録(終了時刻 13:30)→ S02を再描画 → 会議室A 13:30 のセルの表示を確認する。1回だけ再実行して同じ結果を確認した(再現する)。
- 影響がありそうなモジュール: `client/src/lib/grid.js`(`occupiedSlots` / `buildOccupancy`)、`client/src/views/s02-calendar.js`、および **テスト指示 `docs/P009-acceptance-direction/A001-reservation-business-flow.md` 手順5そのもの**。
- 次工程への申し送り: **これはコードの欠陥ではなく、テスト指示(A001 手順5)と設計判断(ADR-007)の矛盾である可能性が高い。**
  * ADR-007「決定内容」: 「重複の定義: … 半開区間 `[start, end)` の交差」「境界一致(09:00-10:00 と 10:00-11:00)は重複しない」。
  * `client/src/lib/grid.js` の `occupiedSlots` は同じ規約(`at >= start && at < end`)で占有スロットを決めており、13:00-13:30 の予約が占有するのは **13:00 のみ**である。
  * 同じA001の**手順8は「15:00・15:30 に予約が表示される」(15:00-16:00 の予約)と書かれており、半開区間と整合している**。したがって手順5の「13:30」の記述だけが、同一文書内でも整合していない。
  * 判断はP202(修正計画)に引き渡す。関連仕様箇所: `docs/ADR.md` ADR-007、`docs/P002-frontend-spec.md` 3.2、`docs/P009-acceptance-direction/A001-*.md` 手順5。

---

## A002: 二重予約の防止

- 実行日時: 2026-08-05T16:03Z
- 実行者: Reviewer Loop (P201, 1回目)
- 対象ブランチ/コミット: Git管理外(ワークツリー直接)
- 参照したテスト指示: docs/P009-acceptance-direction/A002-double-booking-prevention.md
- 実行環境: 上記共通(実プロセス + `threading.Barrier` による送信タイミング同期)
- 事前ビルド結果: 成功
- 実行コマンド: `cd server && python3 -m unittest tests.acceptance.test_a002_double_booking -v`
- 結果: **PASS**
- 期待結果: 手順1=201 / 手順2=409 `RESERVATION_CONFLICT` / 手順3=1件 / 手順6=一方201・他方409でDB1件(500不可) / 手順7=5本中ちょうど1本が201・4本が409でDB1件。
- 実際の結果: すべて期待どおり。同時2本 → `[201, 409]`、DB1件。同時5本 → 201が1本・409が4本、DB1件。5xxは発生しなかった。
- 失敗ログ抜粋: (なし)
- 再現手順: (なし)
- 影響がありそうなモジュール: (なし)
- 次工程への申し送り: `BEGIN IMMEDIATE`(ADR-007)による排他が実プロセス・実HTTPの同時リクエストでも成立していることを確認した。

---

## A003: 権限分離の横断確認

- 実行日時: 2026-08-05T16:03Z
- 実行者: Reviewer Loop (P201, 1回目)
- 対象ブランチ/コミット: Git管理外(ワークツリー直接)
- 参照したテスト指示: docs/P009-acceptance-direction/A003-role-separation.md
- 実行環境: 上記共通
- 事前ビルド結果: 成功
- 実行コマンド:
  * `cd server && python3 -m unittest tests.acceptance.test_a003_role_separation -v`(手順3〜7)
  * `cd client && node --test tests/acceptance/test_a003_role_separation_ui.js`(手順1・2)
- 結果: **PASS**
- 期待結果: 手順1=2リンクなし / 手順2=権限メッセージのみで一覧を取得しない / 手順3=8本すべて403 / 手順4=403 / 手順5=200 / 手順6=すべて401 / 手順7=8本すべて成功。
- 実際の結果: すべて期待どおり。手順2では `GET /api/rooms*` / `GET /api/users*` の呼び出しが0件であること(権限判定を画面側で先に行っていること)も確認した。
- 失敗ログ抜粋: (なし)
- 再現手順: (なし)
- 影響がありそうなモジュール: (なし)
- 次工程への申し送り: **手順7の実施上の注記(合否には影響しない)**。A003の【使用するテストデータ】は「会議室1件、`user001` 以外の利用者の予約1件」だが、その1室に今後の予約がある状態で管理者が `DELETE /api/rooms/{id}` を呼ぶと、認可は通るものの業務制約により 409 `CONSTRAINT_VIOLATION` になる(実測確認済み)。手順7の目的は「管理者が認可で弾かれないこと」なので、破壊系(PUT/DELETE)は**今後の予約を持たない会議室B**を対象として実施した。テスト指示の記述を厳密に1室で読むと自己矛盾になるため、P202で指示の明確化を検討してよい(重大度「低」)。

---

## A004: 会議室マスタ変更の予約への波及

- 実行日時: 2026-08-05T16:03Z
- 実行者: Reviewer Loop (P201, 1回目)
- 対象ブランチ/コミット: Git管理外(ワークツリー直接)
- 参照したテスト指示: docs/P009-acceptance-direction/A004-room-master-impact.md
- 実行環境: 上記共通
- 事前ビルド結果: 成功
- 実行コマンド:
  * `cd server && python3 -m unittest tests.acceptance.test_a004_room_master_impact -v`(手順1〜5・7)
  * `cd client && node --test tests/acceptance/test_a004_room_master_impact_ui.js`(手順6・8・9)
- 結果: **PASS**
- 期待結果: 手順1=409 `CONSTRAINT_VIOLATION` / 手順2=200 / 手順3=`attendee_count` 8のまま取得可 / 手順4=記録のみ(合否対象外) / 手順5=400 `CAPACITY_EXCEEDED` / 手順6=収容人数5で検証 / 手順7=204 / 手順8・9=会議室Aが選択肢・列に現れない。
- 実際の結果: すべて期待どおり。
  * **手順4(合否対象外・記録): `status=400 code=CAPACITY_EXCEEDED`**(`{"error":{"code":"CAPACITY_EXCEEDED","message":"参加予定人数が会議室の収容人数(5名)を超えています。"}}`)。A004の★FIXME★の想定どおり、更新時は新しい収容人数で再検証されるため、件名だけの変更でも 400 になる。人間の確認事項として `docs/P302-deliver.md` へ引き継ぐ候補。
  * 手順6: S03のクライアント側検証が収容人数5で動作し、参加予定人数6で「参加予定人数が会議室の収容人数(5名)を超えています。」を `[data-error-for="attendee_count"]` に表示。5(境界値)は登録成功。
- 失敗ログ抜粋: (なし)
- 再現手順: (なし)
- 影響がありそうなモジュール: (なし)
- 次工程への申し送り: 手順4の挙動(既存予約の件名変更が収容人数変更後に400になる)は仕様未明示のため、P204/P302で人間への申し送りとして扱う。

---

## A005: マイ予約とカレンダーの整合

- 実行日時: 2026-08-05T16:03Z
- 実行者: Reviewer Loop (P201, 1回目)
- 対象ブランチ/コミット: Git管理外(ワークツリー直接)
- 参照したテスト指示: docs/P009-acceptance-direction/A005-my-reservations-consistency.md
- 実行環境: 上記共通(過去予約は指示どおりDBへ直接INSERT)
- 事前ビルド結果: 成功
- 実行コマンド: `cd client && node --test tests/acceptance/test_a005_consistency.js`
- 結果: **PASS**
- 期待結果: 手順1=3件すべて表示 / 手順2=`is-mine` が予約者に応じて分かれる / 手順3=**2件のみ**・日付昇順 / 手順4=S02のセルと一致 / 手順5=過去予約1件 / 手順6=S02・S05の両方から消える。
- 実際の結果: すべて期待どおり。S05(`upcoming`)は `user002` の予約と過去予約を含まず2件、日付昇順。取消後はS02のセルが `+` に戻り、S05は1件になった。
- 失敗ログ抜粋: (なし)
- 再現手順: (なし)
- 影響がありそうなモジュール: (なし)
- 次工程への申し送り: なし。

---

## A006: 認証セッションのライフサイクル

- 実行日時: 2026-08-05T16:03Z
- 実行者: Reviewer Loop (P201, 1回目)
- 対象ブランチ/コミット: Git管理外(ワークツリー直接)
- 参照したテスト指示: docs/P009-acceptance-direction/A006-session-lifecycle.md
- 実行環境: 上記共通
- 事前ビルド結果: 成功
- 実行コマンド:
  * `cd server && python3 -m unittest tests.acceptance.test_a006_session_lifecycle -v`(手順1〜4・6〜8)
  * `cd client && node --test tests/acceptance/test_a006_session_ui.js`(手順5)
- 結果: **PASS**
- 期待結果: 手順1=`HttpOnly` `SameSite=Lax` `Secure` `Path=/` / 手順2=200(スライディング) / 手順3=401 / 手順4=401(絶対期限) / 手順5=`#/login` へ遷移しセッション切れメッセージ / 手順6=401(無効化) / 手順7=401(権限変更) / 手順8=401(ログアウト後)。
- 実際の結果: すべて期待どおり。
- 失敗ログ抜粋: (なし)
- 再現手順: (なし)
- 影響がありそうなモジュール: (なし)
- 次工程への申し送り: 手順5の実測所見(合否には影響しない): 401時に `api.js` は `#/login` への遷移とメッセージ設定を行ったうえで例外を**再送出**する。S02の「翌週」操作のように呼び出し側が捕捉していない経路では、遷移は成立するが未処理のPromise拒否が残る。P002 2.4は遷移とメッセージのみを規定しており仕様違反ではないため、所見として記録する。

---

## A007: 再起動耐性とデータ永続(運用観点 V-O-01 / V-O-02。必須)

- 実行日時: 2026-08-05T16:03Z
- 実行者: Reviewer Loop (P201, 1回目)
- 対象ブランチ/コミット: Git管理外(ワークツリー直接)
- 参照したテスト指示: docs/P009-acceptance-direction/A007-restart-resilience.md
- 実行環境: 上記共通。**同一のDBファイルを使い回し**、uvicorn プロセスを SIGTERM で停止して合計4回起動した。
- 事前ビルド結果: 成功
- 実行コマンド: `cd server && python3 -m unittest tests.acceptance.test_a007_restart_resilience -v`
- 結果: **PASS**
- 期待結果: 手順4・7=エラーなく起動 / 手順5=`schema_migrations` の行数が増えず初期管理者も二重に作られない / 手順6=データがすべて残る / 手順8=WAL残存下でも起動成功。
- 実際の結果: すべて期待どおり。
  * 1回目起動で `schema_migrations` に3件(001-init / 002-rooms / 003-reservations)が適用され、2回目・3回目・4回目の起動で行数は増えなかった。`users` の行数・`role='admin'` の件数も不変で、初期管理者の二重作成はない。
  * 起動ログに `Traceback` / `duplicate column name` / `already exists` / `OperationalError` / `IntegrityError` はいずれも出現しなかった。
  * 再起動後も `user001` でログインでき、`GET /api/reservations/mine` で再起動前に作成した予約を取得できた。有効な会議室2件も保持されていた。
  * 手順8: `PRAGMA journal_mode` が `wal` であることを確認したうえで、`*.db-wal` / `*.db-shm` が存在する状態で停止・起動しても正常に起動し、予約詳細を200で取得できた。
- 失敗ログ抜粋: (なし)
- 再現手順: (なし)
- 影響がありそうなモジュール: (なし)
- 次工程への申し送り: 実施上の注記。SQLiteは**最終接続のクローズ時にWALをチェックポイントして `-wal`/`-shm` を削除する**ため、サーバー停止後に何もしないとWALファイルは残らない。手順8の「WALファイルが残っている状態」を作るには、テスト側で接続を1本開いたまま保持する必要がある(そのように実施した)。テスト指示に手順の前提を1行補うとよい(重大度「低」)。

---

## A008: カレンダー表示の性能(V-N-01)

- 実行日時: 2026-08-05T16:03Z
- 実行者: Reviewer Loop (P201, 1回目)
- 対象ブランチ/コミット: Git管理外(ワークツリー直接)
- 参照したテスト指示: docs/P009-acceptance-direction/A008-calendar-performance.md
- 実行環境: 上記共通。会議室10室 / ユーザー10名 / 予約200件(平日5日 × 10室 × 4件)。CPU 2コア。
- 事前ビルド結果: 成功
- 実行コマンド:
  * `cd server && python3 -m unittest tests.acceptance.test_a008_calendar_performance -v`(手順1・2・4・5・6)
  * `cd client && node --test tests/acceptance/test_a008_calendar_render.js`(手順3・4)
- 結果: **PASS**
- 期待結果: 手順2=中央値1秒以内 / 手順4=中央値3秒以内 / 手順6=API-12の予約取得SQLが1回だけ。
- 実際の結果:

```json
API側: {"rooms_ms": [2.6, 2.5, 2.5, 2.4, 2.3], "reservations_ms": [4.6, 5.0, 4.6, 4.5, 4.5],
        "rooms_median_ms": 2.5, "reservations_median_ms": 4.6, "api_total_median_ms": 7.0,
        "server_duration_ms_median": 3.5355, "select_count_api12": 1,
        "rooms": 10, "reservations": 200, "cpu_count": 2}
画面側: {"render_only_ms": [25.7, 10.4, 11.5, 9.3, 9.6], "render_only_median_ms": 10.4,
        "total_ms": [56.1, 28.3, 26.7, 34.1, 35.6], "total_median_ms": 34.1,
        "rooms": 10, "reservations": 200}
```

  * 手順2: `GET /api/reservations`(1週間・200件)の中央値 **4.6ms**(1秒以内)。
  * 手順4: API取得 + S02描画までの中央値 **34.1ms**(3秒以内)。
  * 手順5: サーバーログの `duration_ms` 中央値 3.54ms。
  * 手順6: `set_trace_callback` で計測し、API-12相当の呼び出しで発行される `SELECT` は **1回のみ**(N+1ではない)。
- 失敗ログ抜粋: (なし)
- 再現手順: (なし)
- 影響がありそうなモジュール: (なし)
- 次工程への申し送り: 判定値に対して2桁以上の余裕があるため、環境依存の懸念(A008の★FIXME★)は該当しない。

---

## A009: 同時接続の負荷(V-N-02)

- 実行日時: 2026-08-05T16:03Z
- 実行者: Reviewer Loop (P201, 1回目)
- 対象ブランチ/コミット: Git管理外(ワークツリー直接)
- 参照したテスト指示: docs/P009-acceptance-direction/A009-concurrent-load.md
- 実行環境: 上記共通。標準ライブラリ `threading` + `Barrier` による30スレッド同時実行。CPU 2コア。
- 事前ビルド結果: 成功
- 実行コマンド: `cd server && python3 -m unittest tests.acceptance.test_a009_concurrent_load -v`
- 結果: **PASS**
- 期待結果: 手順2=5xxが0件・すべて200 / 手順4=30件すべて201でDBが30件増える / 手順5=`DB_LOCK_TIMEOUT` が0件。
- 実際の結果: すべて期待どおり。参照系90リクエスト(30スレッド×3回)はすべて200(中央値 95.2ms / 最大 121.3ms)。更新系は30スレッド同時POSTで**30件すべて201**、DBの予約件数は 100 → 130。アクセスログに5xxはなく、`DB_LOCK_TIMEOUT` の記録もなかった。
- 失敗ログ抜粋: (なし)
- 再現手順: (なし)
- 影響がありそうなモジュール: (なし)
- 次工程への申し送り: 30スレッドは `Barrier` で同時に解放しており、同時実行は成立している(A009の★FIXME★の懸念は該当しない)。

---

## A010: セキュリティの横断確認(V-N-03〜06)

- 実行日時: 2026-08-05T16:03Z
- 実行者: Reviewer Loop (P201, 1回目)
- 対象ブランチ/コミット: Git管理外(ワークツリー直接)
- 参照したテスト指示: docs/P009-acceptance-direction/A010-security-checks.md
- 実行環境: 上記共通。サーバーの標準出力はファイルへ退避して検査した。
- 事前ビルド結果: 成功
- 実行コマンド: `cd server && python3 -m unittest tests.acceptance.test_a010_security -v`
- 結果: **PASS**
- 期待結果: 手順1=全件 `scrypt$` 始まりでユーザーごとに異なる / 手順2=`password` を含むキーが0 / 手順3=Cookie属性4種 / 手順4=8本すべて403 / 手順5=5xx本文に内部情報なし / 手順6=ログに秘密なし・5xxログには `stack` あり / 手順7=`role` を含まない。
- 実際の結果: すべて期待どおり。
  * 手順1: 全ユーザーの `password_hash` が `scrypt$` 始まりで、同じパスワードでも値が重複しない。平文を含まない。
  * 手順2: 14種のAPI応答(`/api/me`・`/api/rooms`・`/api/users`(両scope)・ユーザー作成/更新・会議室作成/更新・予約一覧/マイ予約/詳細/更新・ログイン)の本文に `password` の文字列は1件もなかった。
  * 手順5: DBファイルを壊して内部例外を誘発した結果 500 が返り、本文は `{"error":{"code":"INTERNAL_ERROR","message":...}}` のみ。`Traceback` / `SELECT` / `sqlite3` / `.py` / ファイルパスはいずれも含まれない。
  * 手順6: ログに平文パスワード・`password` 文字列・`sid` の値はなく、5xxのログ行には `stack` が含まれ `level` は `ERROR` だった。
  * 手順7: `scope=attendee_candidates` の応答は `user_id` / `name` の2キーのみ。
- 失敗ログ抜粋: (なし)
- 再現手順: (なし)
- 影響がありそうなモジュール: (なし)
- 次工程への申し送り: TLS(HTTPS)はアプリケーション外(リバースプロキシ)の責務であり本テストの対象外(A010の記載どおり)。`docs/P302-deliver.md` で確認する。

---

## A011: 利用者視点の受け入れシナリオ

- 実行日時: 2026-08-05T16:03Z
- 実行者: Reviewer Loop (P201, 1回目)
- 対象ブランチ/コミット: Git管理外(ワークツリー直接)
- 参照したテスト指示: docs/P009-acceptance-direction/A011-user-acceptance.md
- 実行環境: 上記共通
- 事前ビルド結果: 成功
- 実行コマンド: `cd client && node --test tests/acceptance/test_a011_user_acceptance.js`(手順1〜6)/ 手順7は手動確認
- 結果: **PASS**(手順1〜6。手順7は **NOT RUN**)
- 期待結果: 手順1=空き/予約済みが視覚的に区別でき予約済みには予約者名と件名 / 手順2=S03だけで完結 / 手順3=競合時間帯付きのメッセージ / 手順4〜6=2画面に矛盾なく反映 / 手順7=崩れ・無反応がない。
- 実際の結果:
  * 手順1: 予約済みセルは「鈴木 花子 / 朝会」+ `is-reserved`、空きセルは `+` で `is-reserved` なし。会議室Aの空き時間帯を画面のセルだけから列挙できた(10:00・14:00 等が空き、09:00 は予約済み)。
  * 手順2: 空きセルクリック → S03 → 件名・参加予定人数の入力 → 登録まで、S03の中で完結した。
  * 手順3: 「選択した時間帯はすでに予約されています。(09:00-10:00)」が重複エラー領域に表示され、画面はS03のまま。次に何をすればよいか(別の時間帯を選ぶ)が分かる。
  * 手順4〜6: S05から予約を特定 → S04で 16:00-17:00 に変更 → S02に反映(14:00が空きに戻り16:00に表示)→ 取消でS02のセルが `+`、S05が「該当する予約はありません。」になった。
  * **手順7(NOT RUN・理由)**: 実ブラウザ・ブラウザ自動化ツールを取得できない実行環境であり(P006 1.1 の★ACCEPTED★、ADR-001)、CSSレイアウトの崩れ・実クリックの反応・エラーメッセージの物理的な表示位置は自動でも手動でも確認できない。A011の指示「実施できない環境の場合は **NOT RUN** として理由を記録する」に従って記録する。なお静的配信は実HTTPで確認済み(`/` `→` 200 text/html、`/src/main.js` → 200 `text/javascript`、`/src/styles.css` → 200 `text/css`)であり、ブラウザがアプリを読み込めること自体は成立している。
  * P001の課題との対応: 二重予約=手順3で防止を確認 / 空き状況が分からない=手順1で解消を確認 / 変更・取消の連絡漏れ=手順5・6でS02・S05の最新状態から確認できることを確認(通知機能は要件外。A011の★FIXME★どおり代替確認)。
- 失敗ログ抜粋: (なし)
- 再現手順: (なし)
- 影響がありそうなモジュール: (なし)
- 次工程への申し送り: 手順7(実ブラウザでの目視確認)を人間の確認事項として `docs/P302-deliver.md` に引き継ぐ。A011の【合否判定基準】により、手順1〜6が完了しているため本テストはPASSとする。

---

## A012: 運用者視点の受け入れシナリオ

- 実行日時: 2026-08-05T16:03Z
- 実行者: Reviewer Loop (P201, 1回目)
- 対象ブランチ/コミット: Git管理外(ワークツリー直接)
- 参照したテスト指示: docs/P009-acceptance-direction/A012-operator-acceptance.md
- 実行環境: 上記共通。`INITIAL_ADMIN_ID=ops0001` / `INITIAL_ADMIN_PASSWORD=OpsPassw0rd!` を明示指定。標準出力はファイルへ退避。
- 事前ビルド結果: 成功
- 実行コマンド: `cd server && python3 -m unittest tests.acceptance.test_a012_operator_acceptance -v`
- 結果: **PASS**
- 期待結果: 手順1=`ops0001` が作られる / 手順2=200 / 手順5=1リクエスト1行のJSONで必要項目あり・秘密なし / 手順6=`error_code` に `NOT_FOUND` / `FORBIDDEN` / 手順7=データ保持 / 手順8=起動失敗。
- 実際の結果: すべて期待どおり。
  * 手順1: `users` の `role='admin'` は `['ops0001']` のみ(既定値 `admin001` は作られない)。
  * 手順3・4: 会議室「第1会議室」1件、ユーザー `ops1002` 1件を登録し、そのユーザーで予約1件を作成できた。
  * 手順5: ログ行はすべてJSONとしてパースでき、`ts` `level` `method` `path` `status` `duration_ms` `user_id` を含む。パスワード・`password` 文字列・`sid` の値は含まれない。ログイン2回に対しログ2行、予約登録1回に対しログ1行(1リクエスト1行)。
  * 手順6: `error_code` に `NOT_FOUND` と `FORBIDDEN` が記録された。
  * 手順7: SIGTERM停止 → 再起動後も会議室・ユーザー・予約がすべて残っていた。
  * 手順8: `DB_PATH` の親にディレクトリでない通常ファイルを置いた不正パスで起動 → プロセスは非0で終了し、リクエストを受け付けなかった(中途半端に起動しない)。
- 失敗ログ抜粋: (なし)
- 再現手順: (なし)
- 影響がありそうなモジュール: (なし)
- 次工程への申し送り: 運用の未確定事項(TLS終端・プロセス監視・ログ集約先)は `docs/P302-deliver.md` へ委譲(A012の記載どおり。合否対象外)。

---

## 集計(本記録分)

| テストID | 結果 | 備考 |
| --- | --- | --- |
| A001 | **FAIL** | 手順5のみ。テスト指示 vs ADR-007 の矛盾の疑い |
| A002 | PASS | |
| A003 | PASS | 手順7の実施上の注記あり |
| A004 | PASS | 手順4は合否対象外・実測を記録 |
| A005 | PASS | |
| A006 | PASS | |
| A007 | PASS | 運用観点 V-O-01 / V-O-02 充足 |
| A008 | PASS | |
| A009 | PASS | |
| A010 | PASS | |
| A011 | PASS | 手順7は NOT RUN(実ブラウザなし) |
| A012 | PASS | |

* 自動テストの内訳: サーバー側 `python3 -m unittest discover -s tests/acceptance -t .` → **14 tests / OK**、クライアント側 `node --test 'tests/acceptance/test_*.js'` → **31 tests / 30 pass / 1 fail**(A001 手順5)。
