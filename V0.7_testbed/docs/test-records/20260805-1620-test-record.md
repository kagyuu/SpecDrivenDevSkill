# 結合テスト再実行記録(P205 / F001・F002 の修正後)

> 記録形式は `TEMPLATE-test-record.md` に従う。実行フェーズ: P205(結合テスト再実施)。
> 再実行の対象は `SKILL-P205-integration-retest.md` の規定どおり、**直近のテスト記録でFAILだったテスト**(T016 / A001)、および **`docs/P204-impact-analysis.md` で影響が疑われたテスト**に限定した(全件の無条件再実行はしない)。
> 過去の記録(`20260805-1152` / `20260805-1226` / `20260805-1603`)は上書き・削除していない。

## 事前ビルド

| コマンド | 終了コード |
| --- | --- |
| `cd server && python3 -m compileall -q src` | 0 |
| `cd client && node --check <src配下の全ESモジュール>` | すべて 0 |

* 実行環境: Linux 6.18.5 / CPU 2コア / Python 3.11.15 / Node v22.22.2 / uvicorn 0.46.0
* 実行方式はP201と同一(実サーバープロセス + 実HTTPクライアント)。

## 再実行対象の選定

| 区分 | 対象 | 選定理由 |
| --- | --- | --- |
| 直近FAIL | T016 / A001 | `docs/test-records/20260805-1226-test-record.md` / `20260805-1603-test-record.md` でFAIL |
| P204で影響が疑われた範囲 | T003 / T010 / T015 / T017 / T018(クライアント結合)、A003 / A004 / A005 / A006 / A008 / A011(クライアント受け入れ)、T013(サーバー側 `CAPACITY_EXCEEDED` の代替被覆)、T012 / A002(半開区間の重複判定) | 予約フォーム部品の共有(S03/S04)、占有スロット判定の共有(S02/A005/A011)、および被覆の担当移管先の確認 |
| 退行確認 | サーバー全体(単体213 + 結合13 + 受入14)、クライアント単体126 | アプリケーションコードに差分がないことの裏取り |

---

## T016: S02→S03の値の引き継ぎと登録完了(再実行)

- 実行日時: 2026-08-05T16:20Z
- 実行者: Reviewer Loop (P205)
- 対象ブランチ/コミット: Git管理外(作業前退避 `meeting-room-20260805161135.zip` からの差分は `docs/*` とテストコード2件のみ)
- 参照したテスト指示: docs/P008-test-direction/T016-create-flow.md(F001で手順7の期待結果を修正済み)
- 実行環境: 上記共通
- 事前ビルド結果: 成功(終了コード0)
- 実行コマンド: `cd client && node --test tests/integration/test_t016_create_flow.js`
- 結果: **PASS**(8 tests / 8 pass / 0 fail)
- 期待結果: 手順1〜8。手順7 は「`POST /api/reservations` は呼ばれず、参加予定人数欄の直下に『参加予定人数が会議室の収容人数(10名)を超えています。』が表示される。画面はS03のまま」(F001で修正した期待結果)。
- 実際の結果: 8手順すべて期待どおり。手順7では POST の発行回数が増えず、`[data-error-for="attendee_count"]` の文言が仕様の文言と完全一致し、`[data-role="capacity-error"]` は空、画面遷移も発生しなかった。
- 失敗ログ抜粋: (なし)
- 再現手順: (なし)
- 影響がありそうなモジュール: (なし)
- 次工程への申し送り: サーバー側 400 `CAPACITY_EXCEEDED` の被覆は T013 手順1 が担当(同日再実行でPASS)。

## A001: 予約業務フロー全体(再実行)

- 実行日時: 2026-08-05T16:20Z
- 実行者: Reviewer Loop (P205)
- 対象ブランチ/コミット: 同上
- 参照したテスト指示: docs/P009-acceptance-direction/A001-reservation-business-flow.md(F002で手順5の期待結果を修正済み)
- 実行環境: 上記共通
- 事前ビルド結果: 成功
- 実行コマンド: `cd client && node --test tests/acceptance/test_a001_business_flow.js`
- 結果: **PASS**(10 tests / 10 pass / 0 fail)
- 期待結果: 手順1〜10。手順5 は「13:00 のセルに予約が表示され、13:30 のセルは空きのまま」(ADR-007 の半開区間。F002で修正した期待結果)。
- 実際の結果: 10手順すべて期待どおり。手順5では 13:00 に「山田 太郎 / 定例会議」が表示され `is-reserved` / `is-mine` が付き、13:30 は `+` で `is-reserved` なし。手順8(15:00-16:00 → 15:00・15:30 占有)も従来どおりPASS。
- 失敗ログ抜粋: (なし)
- 再現手順: (なし)
- 影響がありそうなモジュール: (なし)
- 次工程への申し送り: なし。

## 影響が疑われたテストの再実行(まとめ)

- 実行日時: 2026-08-05T16:20Z
- 実行者: Reviewer Loop (P205)
- 対象ブランチ/コミット: 同上
- 参照したテスト指示: docs/P008-test-direction/T003・T010・T012・T013・T015・T017・T018-*.md / docs/P009-acceptance-direction/A002〜A012-*.md
- 実行環境: 上記共通
- 事前ビルド結果: 成功
- 実行コマンド:
  * `cd client && node --test 'tests/integration/*.js'`(T003・T010・T015・T016・T017・T018)
  * `cd client && node --test 'tests/acceptance/test_*.js'`(A001・A003UI・A004UI・A005・A006UI・A008描画・A011)
  * `cd server && python3 -m unittest discover -s tests/integration -t .`(T001・T002・T004〜T009・T011〜T014)
  * `cd server && python3 -m unittest discover -s tests/acceptance -t .`(A002・A003・A004・A006・A007・A008・A009・A010・A012)
  * `cd client && node --test 'tests/*.js'`(クライアント単体126件)
  * `cd server && python3 -m unittest discover -s tests -t .`(サーバー全体240件)
- 結果: **すべて PASS**
- 期待結果: いずれのテストも従来どおりの期待結果を満たすこと(退行がないこと)。
- 実際の結果:

| スイート | 結果 |
| --- | --- |
| クライアント結合 `tests/integration/*.js` | 41 tests / **41 pass / 0 fail** |
| クライアント受け入れ `tests/acceptance/test_*.js` | 31 tests / **31 pass / 0 fail** |
| サーバー結合 `tests/integration` | Ran 13 tests / **OK** |
| サーバー受け入れ `tests/acceptance` | Ran 14 tests / **OK** |
| クライアント単体 `tests/*.js` | 126 tests / **126 pass / 0 fail** |
| サーバー全体 `discover -s tests` | Ran 240 tests / **OK** |

- 失敗ログ抜粋: (なし)
- 再現手順: (なし)
- 影響がありそうなモジュール: (なし)
- 次工程への申し送り: 退行なし。P201(実装横断レビュー)の再判定に進む。

---

## 集計(P205 再実行分)

| テストID | 種別 | 結果 | 前回結果 |
| --- | --- | --- | --- |
| T016 | 結合(P008) | **PASS** | FAIL(P103) |
| A001 | 受け入れ結合(P009) | **PASS** | FAIL(P201 第1回) |
| T001〜T015・T017・T018 | 結合(P008) | PASS(退行なし) | PASS |
| A002〜A012 | 受け入れ結合(P009) | PASS(退行なし) | PASS |

* **T001〜T018(18件)+ A001〜A012(12件)= 30件すべて PASS。FAIL 0 / BLOCKED 0 / NOT RUN 0。**
* A011 手順7 は引き続き手順単位で NOT RUN(実ブラウザ不在。A011の【合否判定基準】によりテストIDとしてはPASS)。
