あなたはExecutor(実装担当)です。以下は Sprint 4 `reservation-ui` の作業範囲と完了条件を、タスク単位に分けて定義したものです。各タスクを記載順に実施し、実施後は、そのタスクの完了条件を満たしたことを確認したうえで、Executor Stepの「停止条件」(`SKILL.md` 参照)に該当しない限り、`docs/P007-impl-direction.md` のWBSに従って自動的に次のタスクへ進んでください。人間の指示を待って停止しないでください。

# スプリント U004 — reservation-ui(予約画面)

* 位置づけ: 予約系4画面を実装し、P001の画面遷移図に沿った操作をブラウザ上で完結させる。`docs/P005-impl-plan.md` 2.4 を参照。
* 実装対象: S02 予約カレンダー、S03 予約作成、S04 予約詳細・編集、S05 マイ予約一覧。**新規APIはない**(Sprint 1〜3で実装済みのAPIを利用する)。
* 前提: Sprint 1〜3(U001〜U003)が完了していること。
* このスプリント全体の共通の禁止事項: サーバー側のAPI・テーブルを追加・変更しないこと。P002にない画面機能(ドラッグ&ドロップ、予約のコピー、印刷用表示など)を追加しないこと。

---

# 【タスクID】U004-T1 — S02 予約カレンダー画面

## 【目的】

* 会議室×時間帯のグリッドを週単位で描画し、予約作成・予約詳細への起点とする。

## 【作成・編集対象ファイル】

* `client/src/views/s02-calendar.js`(編集: U001-T6で作ったプレースホルダを本実装に置き換える)
* `client/src/lib/grid.js`(新規: 予約配列から占有マップを構築する純粋関数)
* `client/src/styles.css`(編集)
* `client/tests/test_grid.js` / `client/tests/test_s02_calendar.js`(新規)

## 【参照すべき仕様箇所】

* P002 **3.2(S02の表示仕様・項目・操作)**、2.3(認証ガードと管理者リンク)、2.4、4(遷移図)、5.5 API-04、5.7 API-12

## 【実装内容】

* 表示対象: 選択日を含む週(**月曜起点の7日間**)。日付ごとに1つのグリッドを描画し、**会議室を列、時間帯(30分刻み、08:00〜20:00)を行**とする。
* 上部に「前週」「翌週」「今日」ボタンと日付選択、会議室フィルタ(チェックボックス、未選択なら全件)を置く。**フィルタはクライアント側で絞り込む**(再取得しない。P002 3.2)。
* データ取得: `GET /api/rooms`(有効な会議室のみ)と `GET /api/reservations?date_from={週初}&date_to={週末}` を各1回だけ呼ぶ。
* `grid.js`: 予約配列と時刻スロット列から「(日付, 会議室, スロット) → 予約 or null」の占有マップを作る純粋関数。**予約の終了時刻のスロットは占有しない**(09:00-10:00 の予約は 09:00 と 09:30 のスロットだけを占有する)。
* セルの描画: 空きセルは「+」、予約済みセルは「予約者の氏名 / 件名」。**参加予定人数は表示しない**(P002 3.2)。自分の予約は別配色(CSSクラス `is-mine`)。
* 操作: 空きセルのクリックで `#/reservations/new?room_id=..&date=..&start=..`(終了時刻は開始+30分)へ遷移。予約済みセルのクリックで `#/reservations/{reservation_id}` へ遷移。**過去日のセルはクリックしても遷移せず**「過去の日付には予約できません。」を表示する。
* 共通ヘッダーを描画する(管理者リンクの出し分けは `header.js` が担当)。

## 【実装してはいけないこと】

* 予約データの追加取得(セルごとのAPI呼び出し)。
* 参加予定人数の表示。
* 会議室フィルタ変更のたびの再取得。

## 【Unit Test内容】

* テスト対象: `grid.js`、`s02-calendar.js` の描画関数
* 正常系: 09:00-10:00 の予約が 09:00 / 09:30 の2スロットを占有し、**10:00 スロットは空きになる**。複数会議室・複数日で正しい位置に入る。フィルタ適用で列が減る。自分の予約に `is-mine` が付く。「翌週」で表示週が+7日になる。予約済みセルのクリックで遷移先が `#/reservations/{id}` になる。
* 異常系: 予約0件で全セルが空き表示。過去日の空きセルをクリックしてもS03へ遷移せずメッセージが出る。`GET /api/reservations` が401を返したときにS01へ戻る導線が働く。
* 実行コマンド: `cd client && node --test tests`
* 合格条件: 上記すべてPASS。

## 【実行コマンド】

* `cd client && node --test tests`

## 【完了条件】

* 単体テストが全件PASS。

## 【次タスクに進む前の停止条件】

* 単体テストが3回自己修正しても合格しない場合は、処理を停止して人間に報告する。

---

# 【タスクID】U004-T2 — S03 予約作成画面

## 【目的】

* 予約の新規登録フォームを実装する。終日チェック、収容人数チェック、重複エラー表示を含む。

## 【作成・編集対象ファイル】

* `client/src/views/s03-reservation-new.js`(新規)
* `client/src/components/reservation-form.js`(新規: S03とS04で共有する入力フォーム)
* `client/src/lib/router.js`(編集)
* `client/tests/test_reservation_form.js` / `client/tests/test_s03_reservation_new.js`(新規)

## 【参照すべき仕様箇所】

* P002 **3.3(S03の全項目・バリデーション・終日チェックの挙動・エラー表示)**、2.4、5.5 API-04、5.6 API-08(`scope=attendee_candidates`)、5.7 API-15

## 【実装内容】

* `reservation-form.js`: 会議室(プルダウン、`GET /api/rooms` の有効な会議室)、日付、終日チェック、開始時刻・終了時刻(30分刻みプルダウン)、件名、参加者(複数選択。候補は `GET /api/users?scope=attendee_candidates`)、参加予定人数、備考。**S04の編集モードでも同じ部品を使う。**
  * **終日チェック**: チェックすると開始 `09:00` / 終了 `18:00` を自動入力する。自動入力後も手動で上書きでき、手動変更でチェックは自動的に外れない。チェックを外しても時刻は元に戻さない(P002 3.3)。
  * **参加予定人数**: 会議室選択の変更時と送信時に、選択中の会議室の `capacity` と比較する。会議室未選択のあいだは上限検証を行わない。
* `s03-reservation-new.js`: URLのクエリ(`room_id` / `date` / `start`)を初期値として反映する(S02の空きセルからの引き継ぎ)。
  * 「登録」→ クライアント側バリデーション → `POST /api/reservations` → 成功で `#/calendar` に戻り「予約を登録しました。」を表示。
  * 409 `RESERVATION_CONFLICT` → 重複エラー領域に「選択した時間帯はすでに予約されています。」と `error.conflicts` の時間帯を表示する。
  * 400 `CAPACITY_EXCEEDED` → 収容人数超過エラー領域に表示する。
  * 「キャンセル」→ 確認なしで `#/calendar` へ。
  * 送信中は登録ボタンを非活性にする。

## 【実装してはいけないこと】

* 予約者を画面から指定させること(サーバーがセッションから決める)。
* 重複時に自動で別時間帯を提案すること。
* 30分刻み以外の時刻入力を許すこと。

## 【Unit Test内容】

* テスト対象: `reservation-form.js`、`s03-reservation-new.js`
* 正常系: クエリの初期値がフォームに反映される。終日チェックで 09:00 / 18:00 が入る。終日チェック後に開始を 10:00 に手動変更しても保持される。正しい入力で `POST /api/reservations` が期待するボディ(`attendee_user_ids` を含む)で1回だけ呼ばれる。成功後に `#/calendar` へ遷移する。
* 異常系: 件名未入力 / 101文字、終了 <= 開始、参加予定人数が収容人数+1 → APIを呼ばずフィールド直下にエラー表示。409応答 → 重複エラー領域に競合時間帯が表示される。400 `CAPACITY_EXCEEDED` → 収容人数超過領域に表示。送信中はボタンが非活性。
* 実行コマンド: `cd client && node --test tests`
* 合格条件: 上記すべてPASS。

## 【実行コマンド】

* `cd client && node --test tests`

## 【完了条件】

* 単体テストが全件PASS。

## 【次タスクに進む前の停止条件】

* 単体テストが3回自己修正しても合格しない場合は、処理を停止して人間に報告する。

---

# 【タスクID】U004-T3 — S04 予約詳細・編集画面

## 【目的】

* 予約の内容確認、編集、取消を、権限に応じた表示制御とともに実装する。

## 【作成・編集対象ファイル】

* `client/src/views/s04-reservation-detail.js`(新規)
* `client/src/lib/router.js`(編集)
* `client/tests/test_s04_reservation_detail.js`(新規)

## 【参照すべき仕様箇所】

* P002 **3.4(S04の表示項目・編集可否・過去予約の扱い)**、2.4、5.7 API-14 / API-16 / API-17

## 【実装内容】

* 閲覧モード: 会議室名 / 日付 / 開始・終了時刻 / 件名 / 参加者の氏名一覧 / 参加予定人数(未入力は「-」) / 備考 / 予約者の氏名 / 登録日時 / 更新日時 を表示する。
* 編集・取消ボタンは、`me.user_id === reservation.user_id` または `me.role === 'admin'` のときのみ表示する。それ以外は閲覧のみ。
* 予約日が過去日の場合は、権限にかかわらず編集・取消ボタンを表示せず「過去の予約は編集できません。」を表示する(P002 3.4)。
* 編集モード: `reservation-form.js`(U004-T2で作成)を再利用し、既存値を初期値として設定する。「更新」で `PUT`、成功で `#/calendar` へ戻り「予約を更新しました。」を表示。
* 「取消」: 「この予約を取り消します。よろしいですか?」の確認ダイアログ → `DELETE` → 成功で `#/calendar` へ戻り「予約を取り消しました。」を表示。
* 「キャンセル」: 遷移元(S02またはS05)へ戻る。遷移元が不明なら `#/calendar`。
* 403 / 404 は P002 2.4 の共通規則で表示する。

## 【実装してはいけないこと】

* 入力フォームをS03と別実装で作ること(`reservation-form.js` を再利用する)。
* 予約者以外にも編集ボタンを表示すること(管理者を除く)。

## 【Unit Test内容】

* テスト対象: `s04-reservation-detail.js`
* 正常系: 予約者本人のとき編集・取消ボタンが出る。管理者のときも出る。参加予定人数が未設定なら「-」。更新で `PUT` が正しいボディで呼ばれる。取消確認でOKなら `DELETE`、キャンセルなら呼ばれない。
* 異常系: 第三者のとき編集・取消ボタンが出ない。過去日の予約のとき、予約者本人でもボタンが出ずメッセージが出る。404応答で「対象のデータが見つかりません。削除された可能性があります。」が出る。409 `RESERVATION_CONFLICT` で重複エラーが表示される。
* 実行コマンド: `cd client && node --test tests`
* 合格条件: 上記すべてPASS。

## 【実行コマンド】

* `cd client && node --test tests`

## 【完了条件】

* 単体テストが全件PASS。

## 【次タスクに進む前の停止条件】

* 単体テストが3回自己修正しても合格しない場合は、処理を停止して人間に報告する。

---

# 【タスクID】U004-T4 — S05 マイ予約一覧画面

## 【目的】

* ログインユーザー自身の予約を、期間フィルタ付きで一覧表示する。

## 【作成・編集対象ファイル】

* `client/src/views/s05-my-reservations.js`(新規)
* `client/src/lib/router.js`(編集)
* `client/tests/test_s05_my_reservations.js`(新規)

## 【参照すべき仕様箇所】

* P002 **3.5(S05の項目・並び順・0件表示)**、5.7 API-13、4(S05 → S04 の遷移)

## 【実装内容】

* 期間フィルタ(ラジオ: 今後の予約 / 過去の予約。初期値=今後の予約)を描画し、変更時に `GET /api/reservations/mine?period=upcoming|past` を呼び直す。
* 一覧の列は「日付 / 会議室名 / 時間帯 / 件名」。並び順はAPIの返却順をそのまま使う(`upcoming` は昇順、`past` は降順)。
* 0件のときは「該当する予約はありません。」を表示する。
* 行のクリックで `#/reservations/{reservation_id}` へ遷移する。

## 【実装してはいけないこと】

* クライアント側での並べ替え・再ソート(APIの順序を正とする)。
* 他人の予約の表示。

## 【Unit Test内容】

* テスト対象: `s05-my-reservations.js`
* 正常系: 3件を渡すと3行描画され、列が4つ。フィルタを「過去の予約」に変えると `period=past` でAPIが呼ばれる。行クリックで遷移先が `#/reservations/{id}`。
* 異常系: 0件で「該当する予約はありません。」が出る。401でS01へ戻る導線が働く。
* 実行コマンド: `cd client && node --test tests`
* 合格条件: 上記すべてPASS。

## 【実行コマンド】

* `cd client && node --test tests`

## 【完了条件】

* 単体テストが全件PASS。

## 【次タスクに進む前の停止条件】

* 単体テストが3回自己修正しても合格しない場合は、処理を停止して人間に報告する。

---

# 【タスクID】U004-T5 — 画面遷移の通し確認(薄い結合確認タスク)

## 【目的】

* 個々の画面が揃った状態で、ルーティング・共通ヘッダー・認証ガードが P002 4章の遷移図どおりに繋がっていることを、単体テストのレベルで確認する。

## 【作成・編集対象ファイル】

* `client/src/lib/router.js`(編集: 全7ルートの登録を確定)
* `client/src/components/header.js`(編集: 全リンクの遷移先を確定)
* `client/tests/test_router.js`(新規)

## 【参照すべき仕様箇所】

* P002 2.2(ルーティング表)、2.3(認証ガード・権限による表示制御)、4(画面遷移図)

## 【実装内容】

* ルーターに7ルート(`#/login` `#/calendar` `#/reservations/new` `#/reservations/{id}` `#/my-reservations` `#/admin/rooms` `#/admin/users`)がすべて登録されていることを確定させる。
* 共通ヘッダーのリンク先: 「カレンダー」→ `#/calendar`、「マイ予約」→ `#/my-reservations`、「会議室管理」→ `#/admin/rooms`(管理者のみ描画)、「ユーザー管理」→ `#/admin/users`(管理者のみ描画)、ログアウト → `POST /api/auth/logout` 後に `#/login`。
* S01以外の全画面が、描画前に `GET /api/me` を呼ぶこと(または共通の前処理で1回呼ぶこと)を確定させる。

## 【実装してはいけないこと】

* 新しい画面・ルートの追加(P002 2.2 の7ルートのみ)。
* この段階でのAPI・仕様の変更。

## 【Unit Test内容】

* テスト対象: `router.js`、`header.js`
* 正常系: 7つのハッシュそれぞれで、対応する描画関数が1回呼ばれる。パラメータ付きルート(`#/reservations/12`)で `reservation_id=12` が渡る。管理者の `me` でヘッダーに4リンク、一般ユーザーで2リンク。ログアウトでAPIが呼ばれ `#/login` へ遷移する。
* 異常系: 未知のハッシュ(`#/unknown`)で `#/calendar` にリダイレクトされる。`GET /api/me` が401のとき、どのルートでも `#/login` へ遷移する。一般ユーザーで `#/admin/rooms` を直接開くと権限メッセージのみが表示される。
* 実行コマンド: `cd client && node --test tests`
* 合格条件: 上記すべてPASS。

## 【実行コマンド】

* `cd client && node --test tests`
* `cd server && python3 -m unittest discover -s tests -t .`(退行がないことの確認)

## 【完了条件】

* クライアント・サーバー双方の単体テストが全件PASS。
* Sprint 4 の全タスク(T1〜T5)が完了したら、`docs/P007-impl-direction.md` の `U004` 行のチェックボックスを `[x]` に更新する。
* 4スプリントすべてが `[x]` になったことをもって、P007の実行対象であるスプリント群が完了する。

## 【次タスクに進む前の停止条件】

* 単体テストが3回自己修正しても合格しない場合は、処理を停止して人間に報告する。

# 【タスクID】U004-T6 — ※CR-001 S03・S04のオンライン会議URL入力とリンク表示

## 【目的】

* CR-001(`docs/P901-cr-direction/CR-001.md`)にもとづき、S03(予約作成)とS04(予約詳細・編集)でオンライン会議URLを任意入力できるようにし、S04の閲覧モードでクリック可能なリンクとして表示する。**S02は対象外。**

## 【作成・編集対象ファイル】

* `client/src/lib/validation.js`(`validateMeetingUrl` を追加)
* `client/src/components/reservation-form.js`(入力欄・`setValues`/`readValues`/`validate`/`toBody`)
* `client/src/views/s04-reservation-detail.js`(閲覧モードのリンク表示、編集モードの初期値)
* `client/tests/test_validation.js`・`client/tests/test_reservation_form.js`・`client/tests/test_s03_reservation_new.js`・`client/tests/test_s04_reservation_detail.js`(単体テストの追加)

## 【参照すべき仕様箇所】

* P002 3.3(入力項目表・オンライン会議URLの検証仕様)、3.4(閲覧モードのリンク表示・編集モード)、3.2(S02には表示しない)、5.7 API-15/16

## 【実装内容】

* `validation.js` に `validateMeetingUrl(value)` を追加する。空欄は `null`(OK)。500文字超・スキーム不一致はP002 3.3の文言を返す。判定順序は **P002 3.3 の規定**(「文字数 → スキーム」。両方に違反する場合は文字数のメッセージ)に従い、サーバー側(U003-T5)と一致させる。
* `reservation-form.js`:
  * ラベル「オンライン会議URL」、`data-role="meeting_url"` の `input[type=text]` を「参加予定人数」と「備考」の間に追加する(P002 3.3 の項目表の並び順)。
  * `setValues` / `readValues` / `toBody` に `meeting_url` を通す。`toBody` は空欄を `""` として送る(`null` にしない。P002 5.3)。
  * `validate` の戻り値に `meeting_url: validateMeetingUrl(...)` を追加する。
* `s04-reservation-detail.js`:
  * 閲覧モードに「オンライン会議URL」の行を「参加予定人数」と「備考」の間に追加する。値があれば `a` 要素(`href` = URL、表示文字列 = URL、`target="_blank"`、`rel="noopener noreferrer"`)、無ければ `-` のテキスト。
  * 編集モードの `setValues` に `meeting_url: reservation.meeting_url` を渡す。
* S02(`s02-calendar.js` / `grid.js`)は**変更しない**。

## 【実装してはいけないこと】

* S02のセル表示にURLを出すこと(CR-001が明示的に除外)。
* URLの短縮・整形・自動リンク化のための正規表現置換(表示するのは登録値そのもの)。
* `innerHTML` によるリンク生成(既存の `el()` ヘルパで要素を組み立てる。文字列連結でHTMLを作らない)。

## 【Unit Test内容】

* テスト対象: `validateMeetingUrl`、予約フォーム、S03、S04
* 正常系: 空欄でエラーにならない。`https://example.com/x` / `http://example.com/x` が通る。500文字ちょうどが通る。フォームの `setValues`→`readValues` で値が往復する。`toBody()` に `meeting_url` が含まれ、空欄なら `""` になる。S04閲覧モードで `a[href]` が生成され `href` が登録URLと一致する。S04閲覧モードでURL未登録なら `-` が表示され `a` 要素が無い。S04編集モードで既存URLが初期値に入る。
* 異常系: 501文字・スキームなし・`ftp://` でP002 3.3の文言が返る。S03で不正なURLを入れて送信するとAPIを呼ばずにフィールドエラーが表示される。
* 退行確認: S02のセル表示文言にURLが混入しないこと。
* 実行コマンド: `cd client && node --test 'tests/*.js'`
* 合格条件: 上記すべてPASS。既存の126件の単体テストが1件も退行しないこと。

## 【実行コマンド】

* `cd client && node --test 'tests/*.js'`
* `cd server && python3 -m unittest discover -s tests -t .`(退行がないことの確認)

## 【完了条件】

* クライアント・サーバー双方の単体テストが全件PASS。
* CR-001の「期待する振る舞い」のうち画面に関する項目が満たされていること。

## 【次タスクに進む前の停止条件】

* 単体テストが3回自己修正しても合格しない場合は、処理を停止して人間に報告する。

---

---

## 重要

* このスプリントの範囲外のファイルは編集しないでください。
* 実装後、実行したテストコマンドと結果を報告してください。
* 全スプリントが完了したら、`docs/P008-test-direction.md` の指示に従って結合テスト(P103)に進んでください。1タスクごとに人間の指示を待つ必要はありません。
