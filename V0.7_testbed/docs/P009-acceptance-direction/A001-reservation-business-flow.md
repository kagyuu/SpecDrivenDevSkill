あなたはReviewer Loop(実装横断レビュー担当)です。以下は1テストタスク分の作業範囲と完了条件を定義したものです。実施後は、結果(PASS/FAIL/BLOCKED/NOT RUNいずれであっても)を記録したうえで、Reviewer Loopの「停止条件」(`SKILL.md` 参照)に該当しない限り、`docs/P009-acceptance-direction.md` のWBSに従って自動的に次のテストタスクへ進んでください。人間の指示を待って停止しないでください。

# 【テストID】A001 — 予約業務フロー全体

## 【目的】

* P001が解決すべきとした「空き状況の確認から予約・変更・取消までをオンラインで完結させる」という業務フローが、スプリントをまたいで一連の操作として成立することを確認する。

## 【参照テスト計画】

* `docs/P006-test-plan.md` 3.1 V-F-15、4.3(システムテスト)
* 仕様: `docs/P001-requirement.md` アプリケーションの概要・画面遷移図、`docs/P002-frontend-spec.md` 4

## 【対象モジュール】

* 全モジュール(S01・S02・S03・S04、API-01・03・04・08・12・14・15・16・17)

## 【前提条件】全モジュールビルドが成功していること

* 第2章のビルドコマンドが終了コード0で完了していること。

## 【使用するテストデータ】

* `user001` / `Passw0rd!23`、会議室A(収容10)、日付「本日+1日」。

## 【事前準備】

1. DBファイルを削除し、基準データセットを投入してサーバーを起動する。

## 【実行手順】

1. S01で `user001` としてログインする。
2. S02で「本日+1日」の週が表示され、会議室A・Bの列があることを確認する。
3. 会議室A 13:00 の空きセルをクリックしてS03へ進む。
4. 件名「定例会議」、参加者 `user002`、参加予定人数 6、備考「資料は事前配布」、**オンライン会議URL `https://example.com/meet/a001`(※CR-001)** で登録する。
5. S02に戻り、**13:00 のセルに予約が表示され、13:30 のセルは空きのまま**であることを確認する(終了時刻は 13:30 とする)。占有スロットの判定は半開区間 `[start, end)` に従うため、終了時刻のスロットは占有しない(`docs/ADR.md` ADR-007)。
6. 予約セルをクリックしてS04を開き、入力した内容がすべて表示されることを確認する。
7. S04で件名を「定例会議(変更)」、時間帯を 15:00-16:00 に変更して更新する。
   * ※CR-001: このとき、オンライン会議URLは編集モードの初期値として引き継がれ、更新後も `https://example.com/meet/a001` のまま保持されることを確認する。
8. S02で 13:00 が空きに戻り、15:00・15:30 に予約が表示されることを確認する。
9. S04を開いて取消する。
10. S02で当該時間帯が空きに戻ることを確認する。

## 【実行コマンド】

* `cd client && node --test tests/acceptance/test_a001_business_flow.js`(★FIXME★ 受け入れテストの配置は `client/tests/acceptance/` を想定。サーバー側で完結する部分は `server/tests/acceptance/` に置いてよい。実装時に確定すること)

## 【期待結果】

* 手順1〜10のすべてが、エラーなく順に完了する。
* 手順6: 会議室名・日付・時間帯・件名・参加者「鈴木 花子」・参加予定人数 6・備考・予約者「山田 太郎」が表示される。※CR-001: オンライン会議URLが**クリック可能なリンク**(`a` 要素、`href` = `https://example.com/meet/a001`)として表示される。
* 手順5・8・10: グリッドの占有状態が更新後の内容と一致する。**占有スロットの判定は `docs/ADR.md` ADR-007 の半開区間 `[start, end)` に従い、終了時刻のスロットは占有しない**(13:00-13:30 の予約は 13:00 のみ、15:00-16:00 の予約は 15:00・15:30 を占有する)。
  * ★手順5の期待結果は Reviewer Loop の修正タスク F002(`docs/P202-fix-plan/fixed/F002-a001-half-open-slot-expectation.md`)で修正した。修正前は「13:00・13:30 のセルに予約が表示される」であり、ADR-007 および本書手順8(15:00-16:00 → 15:00・15:30)と整合していなかった。

## 【合否判定基準】

* 全手順が期待どおりに完了すればPASS。途中で1つでも期待と異なればFAILとし、どの手順で止まったかを記録する。

## 【失敗時に記録する内容】

* テストID、実行日時、実行コマンド、終了コード
* 期待結果と実際の結果(HTTPステータス、レスポンス本文、DBの該当行、画面の描画結果、計測値のうち該当するもの)
* サーバーの標準出力ログのうち当該操作に対応する行(`sid`・パスワードは伏せる)
* 失敗が再現するかどうか(同一手順を1回だけ再実行して確認する。3回以上繰り返さない)
* 関連しそうな仕様箇所(`docs/P001-requirement.md` / `docs/P002-frontend-spec.md` / `docs/P003-backend-spec.md` の節番号)

## 【修正禁止事項】

* アプリケーションコードを修正しない
* テストコードをその場で都合よく変更しない
* 失敗したテストをスキップしない
* 期待値を変更して成功扱いにしない
* 同じ失敗に対して場当たり的な再テストを繰り返さない
* 修正が必要な場合はP202(修正計画)以降に引き渡す

## 【次タスクへ進む条件】

* 本テストの結果(PASS / FAIL / BLOCKED のいずれか)が `docs/test-records/YYYYMMDD-HHMM-test-record.md` に記録されていること。
* `docs/P009-acceptance-direction.md` の該当行のチェックボックスが `[x]` になっていること。
* **FAILであっても次のテストタスクへ進む**(修正はP202以降で行う)。

## 重要:

* アプリケーションコードを修正しないでください。
* テスト失敗時に、その場で修正して再テストしないでください。
* テスト失敗時は、失敗内容をテスト記録に残してください。
* このテストタスクの結果を記録したら、Reviewer Loopの停止条件に該当しない限り、次のテストタスクに自動的に進んでください。1テストタスクごとに人間の指示を待つ必要はありません。
