# 実行時に必要な外部パッケージ(配布用のピン留め)。
# 本ファイルはP302(納品物作成)で作成した。バージョンは、全テスト(バックエンド240件 /
# フロントエンド198件)を合格させた検証環境に実際にインストールされていたものと同一である。
#
# 注意: `server/pyproject.toml` の `[project].dependencies` にも同じ内容を宣言している。
# 本ファイルは「配布・コンテナビルド時のピン留め」を目的とし、pyproject 側は
# パッケージとしてのメタデータを目的とする(`docs/P302-deliver.md` 5章・10章 参照)。
starlette==1.0.0
pydantic==2.13.3
uvicorn==0.46.0
