# T003: 同時実行時の排他制御(二重予約防止)

> V0.3ルールで再生成。IDは本ファイル名・目次と一致する T003 表記を最初から使用している。

あなたはテスト担当です。以下の1テストタスクだけを実施してください。

【テストID】T003
【目的】`docs/03-backend-spec.md` 4章で定めた `BEGIN IMMEDIATE` による排他制御が、同一会議室・同一時間帯への同時リクエストで実際に機能し、二重予約が発生しないことを確認する(非機能要件: 同時実行時の排他制御)。
【参照テスト計画】`docs/05-test-plan.md` 4章(非機能観点: 同時実行時の排他制御)
【対象モジュール】`server/app/repositories/reservations.py`(create関数)
【前提条件】ビルド成功。サーバー起動済み。
【使用するテストデータ】同一会議室・同一日付・完全に同一の時間帯を指定した予約リクエスト2件。
【事前準備】u001でログインしCookieを取得する。
【実行手順】
1. 同一のCookie・同一の会議室/日付/時間帯で、`POST /api/reservations` をPythonの`threading`で2スレッド同時に送信する。
2. 2件のレスポンスのステータスコードを集計する。
【実行コマンド】`cd server && python3 -m unittest tests.system.test_t003_concurrent_booking -v`
【期待結果】2件のうち1件が`201`、もう1件が`409`になること(2件とも201になってはいけない)。
【合否判定基準】201が1件かつ409が1件であること。
【失敗時に記録する内容】実際に得られたステータスコードの組み合わせ。
【修正禁止事項】T001と同様。
【次タスクへ進む条件】結果がテスト記録に記載された時点。

重要:
- アプリケーションコードを修正しないでください。
- テスト失敗時に、その場で修正して再テストしないでください。
- テスト失敗時は、失敗内容をテスト記録に残してください。
- 次のテストタスクには進まないでください。
