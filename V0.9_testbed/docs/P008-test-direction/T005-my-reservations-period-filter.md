あなたはExecutor(実装担当)です。以下は1テストタスク分の作業範囲と完了条件を定義したものです。実施後は、結果(PASS/FAIL/BLOCKED/NOT RUNいずれであっても)を記録したうえで、Executor Stepの「停止条件」(`SKILL.md` 参照)に該当しない限り、`docs/P008-test-direction.md` のWBSに従って自動的に次のテストタスクへ進んでください。人間の指示を待って停止しないでください。

# 【テストID】T005

## 【目的】

* S05(マイ予約一覧画面)の期間フィルタが `GET /api/reservations/mine?period=` と正しく連携し、DB上の予約データを正しく分類して表示することを確認する。

## 【参照テスト計画】

* `docs/P006-test-plan.md` §2(機能観点)

## 【対象モジュール】

* `client/src/pages/MyReservationsPage.tsx`, `server/app/api/routers/reservations.py`, `server/app/repositories/reservations.py`

## 【前提条件】対象スプリントの全モジュールビルドが成功していること

* U002範囲の単体テストが全PASSであること

## 【使用するテストデータ】

* シードユーザー `user001`、会議室1件、予約2件(過去の予約1件・未来の予約1件を直接SQLでINSERT)

## 【事前準備】

* 上記データをシードした状態でバックエンド・フロントエンドを起動する。

## 【実行手順】

1. `user001` でログインしS05を開く。
2. 既定表示(「今後の予約」)で未来の予約のみ表示されることを確認する。
3. フィルタを「過去の予約」に切り替え、過去の予約のみ表示されることを確認する。

## 【実行コマンド】

* `cd client && npx playwright test tests/integration/t005_my_reservations_period_filter.spec.ts`

## 【期待結果】

* 各フィルタ状態で、対応する期間の予約のみが一覧に表示される。

## 【合否判定基準】

* 上記が満たされればPASS。

## 【失敗時に記録する内容】

* 実際に表示された一覧内容、APIリクエストのクエリパラメータとレスポンス。

## 【修正禁止事項】

* アプリケーションコードを修正しない。テストコードをその場で都合よく変更しない。失敗したテストをスキップしない。期待値を変更して成功扱いにしない。同じ失敗に対して場当たり的な再テストを繰り返さない。失敗内容をテスト記録に残し、次工程に引き渡す。

## 【次タスクへ進む条件】

* 結果を記録した時点で次へ進んでよい。

## 重要:

* アプリケーションコードを修正しないでください。
* テスト失敗時に、その場で修正して再テストしないでください。
* テスト失敗時は、失敗内容をテスト記録に残してください。
* このテストタスクの結果を記録したら、Executor Stepの停止条件に該当しない限り、次のテストタスクに自動的に進んでください。1テストタスクごとに人間の指示を待つ必要はありません。
