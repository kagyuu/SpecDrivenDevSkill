あなたはExecutor(実装担当)です。以下は1テストタスク分の作業範囲と完了条件を定義したものです。実施後は、結果(PASS/FAIL/BLOCKED/NOT RUNいずれであっても)を記録したうえで、Executor Stepの「停止条件」(`SKILL.md` 参照)に該当しない限り、`docs/P008-test-direction.md` のWBSに従って自動的に次のテストタスクへ進んでください。人間の指示を待って停止しないでください。

# 【テストID】T004

## 【目的】

* 会議室・時間帯が重複する予約をS03から登録しようとした際、バックエンドの409 `RESERVATION_CONFLICT` がフロントエンドに正しく伝わり、`docs/P002-frontend-spec.md` §3 S03のメッセージが表示されることを確認する。

## 【参照テスト計画】

* `docs/P006-test-plan.md` §2(業務ルール違反観点)

## 【対象モジュール】

* `client/src/pages/ReservationCreatePage.tsx`, `server/app/api/routers/reservations.py`, `server/app/services/reservations.py`

## 【前提条件】対象スプリントの全モジュールビルドが成功していること

* U002範囲の単体テストが全PASSであること

## 【使用するテストデータ】

* シードユーザー `user001`、会議室1件(直接SQLでINSERT)、既存予約1件(`会議室A`, 本日 `10:00-11:00`, 直接SQLでINSERT)

## 【事前準備】

* 上記データをシードした状態でバックエンド・フロントエンドを起動する。

## 【実行手順】

1. `user001` でログインする。
2. S03で `会議室A`、本日、`10:30-11:30` を入力して登録する。
3. 画面上のエラーメッセージを確認する。

## 【実行コマンド】

* `cd client && npx playwright test tests/integration/t004_reservation_conflict_display.spec.ts`

## 【期待結果】

* API呼び出しは409 `RESERVATION_CONFLICT` を返し、画面に「選択した会議室・時間帯は既に予約されています」が表示され、画面はS03に留まる(S02へ遷移しない)。

## 【合否判定基準】

* 上記がすべて満たされればPASS。

## 【失敗時に記録する内容】

* 実際に表示されたメッセージ文言、APIレスポンス。

## 【修正禁止事項】

* アプリケーションコードを修正しない。テストコードをその場で都合よく変更しない。失敗したテストをスキップしない。期待値を変更して成功扱いにしない。同じ失敗に対して場当たり的な再テストを繰り返さない。失敗内容をテスト記録に残し、次工程に引き渡す。

## 【次タスクへ進む条件】

* 結果を記録した時点で次へ進んでよい。

## 重要:

* アプリケーションコードを修正しないでください。
* テスト失敗時に、その場で修正して再テストしないでください。
* テスト失敗時は、失敗内容をテスト記録に残してください。
* このテストタスクの結果を記録したら、Executor Stepの停止条件に該当しない限り、次のテストタスクに自動的に進んでください。1テストタスクごとに人間の指示を待つ必要はありません。
