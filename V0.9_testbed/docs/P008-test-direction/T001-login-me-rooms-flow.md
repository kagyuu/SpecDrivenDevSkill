あなたはExecutor(実装担当)です。以下は1テストタスク分の作業範囲と完了条件を定義したものです。実施後は、結果(PASS/FAIL/BLOCKED/NOT RUNいずれであっても)を記録したうえで、Executor Stepの「停止条件」(`SKILL.md` 参照)に該当しない限り、`docs/P008-test-direction.md` のWBSに従って自動的に次のテストタスクへ進んでください。人間の指示を待って停止しないでください。

# 【テストID】T001

## 【目的】

* U001で実装したバックエンド(認証・ルーム読み取りAPI)とフロントエンド(S01ログイン画面)が、ブラウザ相当の環境で実際に連携して動作することを確認する。

## 【参照テスト計画】

* `docs/P006-test-plan.md` §1(結合テストレベル)、§2(機能観点)

## 【対象モジュール】

* `server/app/api/routers/auth.py`, `server/app/api/routers/rooms.py`, `client/src/pages/LoginPage.tsx`, `client/src/router.tsx`

## 【前提条件】対象スプリントの全モジュールビルドが成功していること

* `cd server && uv run python -m pytest tests/ -v` が全PASSであること(U001範囲の単体テスト)
* `cd client && npm run build` が成功していること

## 【使用するテストデータ】

* `docs/P006-test-plan.md` §5のシードユーザー `user001` / パスワード `TestPassw0rd!`

## 【事前準備】

* 一時DBファイルにマイグレーション(001・002)を適用する。
* `cd server && uv run uvicorn app.main:app --port 8001` でバックエンドを起動する。
* `cd client && npm run dev -- --port 5174` でフロントエンドを起動し、APIのベースURLをバックエンドのポートに向ける。

## 【実行手順】

1. ブラウザ(または `client` のE2Eテストランナー、例: Playwright)でS01を開く。
2. `user001` / `TestPassw0rd!` を入力してログインする。
3. トップ画面遷移後、ヘッダー等に表示されるユーザー名が `GET /api/me` の結果と一致することを確認する。
4. 会議室一覧(S02のフィルタ選択肢、または本テスト用の簡易確認画面)が `GET /api/rooms` の結果と一致することを確認する。

## 【実行コマンド】

* `cd client && npx playwright test tests/integration/t001_login_flow.spec.ts`(E2Eテストとして実装する場合)

## 【期待結果】

* ログイン後、画面上にエラーが表示されず、ユーザー名・会議室一覧が正しく表示される。

## 【合否判定基準】

* 上記【期待結果】をすべて満たせばPASS。1つでも満たさなければFAIL。

## 【失敗時に記録する内容】

* 失敗したステップ番号、実際のレスポンス(ステータスコード・ボディ)、スクリーンショットまたはコンソールログ。

## 【修正禁止事項】

* アプリケーションコードを修正しない。テストコードをその場で都合よく変更しない。失敗したテストをスキップしない。期待値を変更して成功扱いにしない。同じ失敗に対して場当たり的な再テストを繰り返さない。失敗内容をテスト記録に残し、次工程(Reviewer Loop)の修正フェーズに引き渡す。

## 【次タスクへ進む条件】

* 本テストの結果(PASS/FAIL/BLOCKEDいずれか)を `docs/test-records/YYYYMMDD-HHMM-test-record.md` に記録した時点で次のテストタスクへ進んでよい。

## 重要:

* アプリケーションコードを修正しないでください。
* テスト失敗時に、その場で修正して再テストしないでください。
* テスト失敗時は、失敗内容をテスト記録に残してください。
* このテストタスクの結果を記録したら、Executor Stepの停止条件に該当しない限り、次のテストタスクに自動的に進んでください。1テストタスクごとに人間の指示を待つ必要はありません。
