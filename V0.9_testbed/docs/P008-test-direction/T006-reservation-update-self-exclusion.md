あなたはExecutor(実装担当)です。以下は1テストタスク分の作業範囲と完了条件を定義したものです。実施後は、結果(PASS/FAIL/BLOCKED/NOT RUNいずれであっても)を記録したうえで、Executor Stepの「停止条件」(`SKILL.md` 参照)に該当しない限り、`docs/P008-test-direction.md` のWBSに従って自動的に次のテストタスクへ進んでください。人間の指示を待って停止しないでください。

# 【テストID】T006

## 【目的】

* S04(予約詳細・編集画面)で自分の予約を、時間帯を変えずに他の項目(件名など)だけ編集した際、自分自身の現在の予約枠が重複判定の対象から正しく除外され、誤って409にならないことを確認する(`docs/P003-backend-spec.md` §5.9のexclude_idの結合確認)。

## 【参照テスト計画】

* `docs/P006-test-plan.md` §2(機能観点、境界値)

## 【対象モジュール】

* `client/src/pages/ReservationDetailPage.tsx`, `server/app/api/routers/reservations.py`, `server/app/repositories/reservations.py`

## 【前提条件】対象スプリントの全モジュールビルドが成功していること

* U003範囲の単体テストが全PASSであること

## 【使用するテストデータ】

* シードユーザー `user001`、会議室1件、予約1件(`user001` 自身、本日 `10:00-11:00`、件名「旧件名」)

## 【事前準備】

* 上記データをシードした状態でバックエンド・フロントエンドを起動する。

## 【実行手順】

1. `user001` でログインし、S05経由でS04を開く。
2. 時間帯は `10:00-11:00` のまま、件名を「新件名」に変更して更新する。
3. 200が返り、S02で「新件名」が表示されることを確認する。

## 【実行コマンド】

* `cd client && npx playwright test tests/integration/t006_reservation_update_self_exclusion.spec.ts`

## 【期待結果】

* 更新が409にならず成功し、件名が更新される。

## 【合否判定基準】

* 上記が満たされればPASS。

## 【失敗時に記録する内容】

* APIレスポンス(特にステータスコードが409だった場合はその旨)、画面の表示内容。

## 【修正禁止事項】

* アプリケーションコードを修正しない。テストコードをその場で都合よく変更しない。失敗したテストをスキップしない。期待値を変更して成功扱いにしない。同じ失敗に対して場当たり的な再テストを繰り返さない。失敗内容をテスト記録に残し、次工程に引き渡す。

## 【次タスクへ進む条件】

* 結果を記録した時点で次へ進んでよい。

## 重要:

* アプリケーションコードを修正しないでください。
* テスト失敗時に、その場で修正して再テストしないでください。
* テスト失敗時は、失敗内容をテスト記録に残してください。
* このテストタスクの結果を記録したら、Executor Stepの停止条件に該当しない限り、次のテストタスクに自動的に進んでください。1テストタスクごとに人間の指示を待つ必要はありません。
