あなたはReviewer Loop(実装横断レビュー担当)です。以下は1テストタスク分の作業範囲と完了条件を定義したものです。実施後は、結果(PASS/FAIL/BLOCKED/NOT RUNいずれであっても)を記録したうえで、Reviewer Loopの「停止条件」(`SKILL.md` 参照)に該当しない限り、`docs/P009-acceptance-direction.md` のWBSに従って自動的に次のテストタスクへ進んでください。人間の指示を待って停止しないでください。

# 【テストID】A002

## 【目的】

* 管理者が行う標準的な運用操作(会議室登録、ユーザー登録、一般ユーザーが作成した予約の代理編集・取消)が、S06・S07・関連APIを通して問題なく完結することを確認する(受け入れテスト相当)。

## 【参照テスト計画】

* `docs/P006-test-plan.md` §1(受け入れテストレベル)

## 【対象モジュール】

* `client/`(S06, S07, S04)、`server/`(会議室・ユーザー・予約API)

## 【前提条件】全モジュールビルドが成功していること

* `cd server && uv run python -m pytest tests/ -v` が全PASS
* `cd client && npm run build` が成功

## 【使用するテストデータ】

* シードユーザー `admin001` / `TestPassw0rd!`(管理者)、`user001`(一般)

## 【事前準備】

* 永続DBファイルにマイグレーションを適用し、上記テストデータをシードする。
* `user001` の予約を1件、事前にAPI経由で作成しておく(`会議室C`、本日 `09:00-10:00`、件名「一般ユーザー予約」)。ただし `会議室C` はこの時点で存在しないため、本手順の直前に管理者が作成する(下記手順1)。

## 【実行手順】

1. `admin001` でログインしS06を開き、`会議室C`(収容6名)を新規登録する。
2. S07を開き、新規ユーザー `user003`(一般、初期パスワード `NewUser1234`)を登録する。
3. API経由(またはS03を使い `user001` で再ログインして)`会議室C` に `user001` の予約(本日 `09:00-10:00`、件名「一般ユーザー予約」)を作成する。
4. `admin001` で再ログインし、S02から `user001` の予約セルを開き(S04)、件名を「管理者確認済み」に変更して更新する。
5. 同じ予約をS04から取消す。

## 【実行コマンド】

* `cd client && npx playwright test tests/acceptance/a002_admin_journey.spec.ts`

## 【期待結果】

* 手順1〜5すべてがエラーなく完了する。手順4・5では、管理者が予約者本人でなくても編集・取消操作が許可される(`docs/P002-frontend-spec.md` §6のとおり)。

## 【合否判定基準】

* 上記【期待結果】をすべて満たせばPASS。

## 【失敗時に記録する内容】

* 失敗したステップ、画面のスクリーンショット、APIレスポンス。

## 【修正禁止事項】

* アプリケーションコードを修正しない。テストコードをその場で都合よく変更しない。失敗したテストをスキップしない。期待値を変更して成功扱いにしない。同じ失敗に対して場当たり的な再テストを繰り返さない。失敗内容をテスト記録に残し、修正が必要な場合はP202以降に引き渡す。

## 【次タスクへ進む条件】

* 結果を記録した時点で次へ進んでよい。

## 重要:

* アプリケーションコードを修正しないでください。
* テスト失敗時に、その場で修正して再テストしないでください。
* テスト失敗時は、失敗内容をテスト記録に残してください。
* このテストタスクの結果を記録したら、Reviewer Loopの停止条件に該当しない限り、次のテストタスクに自動的に進んでください。1テストタスクごとに人間の指示を待つ必要はありません。
