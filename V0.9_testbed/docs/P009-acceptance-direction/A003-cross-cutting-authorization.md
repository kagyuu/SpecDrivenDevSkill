あなたはReviewer Loop(実装横断レビュー担当)です。以下は1テストタスク分の作業範囲と完了条件を定義したものです。実施後は、結果(PASS/FAIL/BLOCKED/NOT RUNいずれであっても)を記録したうえで、Reviewer Loopの「停止条件」(`SKILL.md` 参照)に該当しない限り、`docs/P009-acceptance-direction.md` のWBSに従って自動的に次のテストタスクへ進んでください。人間の指示を待って停止しないでください。

# 【テストID】A003

## 【目的】

* `docs/P002-frontend-spec.md` §6の認可の外部契約表を、実装全体(U001〜U004)を通してシステムテスト相当で再確認する。`docs/P008-test-direction/T009-admin-screen-guard.md` はU004時点の画面ガードのみを対象としていたが、本テストはスプリントをまたいだ全リソース(予約・会議室・ユーザー)に対する認可を一括で確認する。

## 【参照テスト計画】

* `docs/P006-test-plan.md` §2(認可エラー観点)、`docs/P001-requirement.md`「権限まわり(...)は結合テストで必ず確認する」

## 【対象モジュール】

* `server/` 全API、`client/` 全画面

## 【前提条件】全モジュールビルドが成功していること

* `cd server && uv run python -m pytest tests/ -v` が全PASS

## 【使用するテストデータ】

* 一般ユーザー `user001`・`user002`、管理者 `admin001`、`user001` が作成した予約1件

## 【事前準備】

* 永続DBファイルにマイグレーションを適用し、上記データをシードする。

## 【実行手順】

1. `user002` でログインし、`user001` が作成した予約に対して `PUT`/`DELETE` を試みる → 403を確認する。
2. `user002` でログインし、`POST/PUT/DELETE /api/rooms/*`、`GET/POST/PUT/DELETE /api/users/*` をすべて試みる → 403を確認する。
3. `admin001` でログインし、手順1・2と同じ操作をすべて試みる → いずれも成功(200/201/204)することを確認する。
4. 未認証(Cookie無し)で全保護APIを試みる → 401を確認する。

## 【実行コマンド】

* `cd server && uv run python -m pytest tests/acceptance/test_a003_cross_cutting_authorization.py -v`

## 【期待結果】

* 手順1・2・4で一貫して403/401、手順3で一貫して成功する。

## 【合否判定基準】

* 上記がすべて満たされればPASS。1件でも一致しない組み合わせがあればFAIL。

## 【失敗時に記録する内容】

* 一致しなかった具体的なAPI・ユーザー・期待/実際のステータスコード。

## 【修正禁止事項】

* アプリケーションコードを修正しない。テストコードをその場で都合よく変更しない。失敗したテストをスキップしない。期待値を変更して成功扱いにしない。同じ失敗に対して場当たり的な再テストを繰り返さない。失敗内容をテスト記録に残し、修正が必要な場合はP202以降に引き渡す。

## 【次タスクへ進む条件】

* 結果を記録した時点で次へ進んでよい。

## 重要:

* アプリケーションコードを修正しないでください。
* テスト失敗時に、その場で修正して再テストしないでください。
* テスト失敗時は、失敗内容をテスト記録に残してください。
* このテストタスクの結果を記録したら、Reviewer Loopの停止条件に該当しない限り、次のテストタスクに自動的に進んでください。1テストタスクごとに人間の指示を待つ必要はありません。
