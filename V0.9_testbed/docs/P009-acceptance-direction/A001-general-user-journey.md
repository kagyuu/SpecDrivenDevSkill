あなたはReviewer Loop(実装横断レビュー担当)です。以下は1テストタスク分の作業範囲と完了条件を定義したものです。実施後は、結果(PASS/FAIL/BLOCKED/NOT RUNいずれであっても)を記録したうえで、Reviewer Loopの「停止条件」(`SKILL.md` 参照)に該当しない限り、`docs/P009-acceptance-direction.md` のWBSに従って自動的に次のテストタスクへ進んでください。人間の指示を待って停止しないでください。

# 【テストID】A001

## 【目的】

* 一般ユーザーが会議室予約システムを実際に使う際の標準的な一連の操作(ログイン→カレンダー確認→予約作成→マイ予約確認→予約編集→予約取消→ログアウト)が、S01〜S05・関連APIすべてを通して問題なく完結することを確認する(受け入れテスト相当の主要シナリオ)。

## 【参照テスト計画】

* `docs/P006-test-plan.md` §1(受け入れテストレベル)、§2(機能観点)

## 【対象モジュール】

* `client/` 全画面(S01, S02, S03, S04, S05)、`server/` 認証・予約系API全体

## 【前提条件】全モジュールビルドが成功していること

* `cd server && uv run python -m pytest tests/ -v` が全PASS
* `cd client && npm run build` が成功
* `docs/P008-test-direction.md` の全テストが実行済み(FAIL/BLOCKEDが残っていても本テストの実施自体は妨げない。ただし関連する既知の失敗があれば事前に確認する)

## 【使用するテストデータ】

* シードユーザー `user001` / `TestPassw0rd!`
* 会議室2件(`会議室A` 収容10名、`会議室B` 収容4名)

## 【事前準備】

* 永続DBファイル(本番相当パス)にマイグレーションを適用し、上記テストデータをシードする。
* バックエンド・フロントエンドを起動する。

## 【実行手順】

1. S01で `user001` としてログインする。
2. S02で今週のカレンダーを表示し、空きセルをクリックしてS03へ遷移する。
3. S03で `会議室A`、本日、`14:00-15:00`、件名「定例MTG」、参加予定人数3で登録する。
4. S02に戻り、該当セルに予約が表示されることを確認する。
5. ヘッダーから「マイ予約」を開き(S05)、作成した予約が一覧に表示されることを確認する。
6. 一覧行をクリックしS04を開き、件名を「定例MTG(会議室変更前)」に変更して更新する。
7. 再度S04を開き、取消ボタンで予約を取り消す。
8. S02に戻り、該当セルが空きに戻っていることを確認する。
9. ログアウトし、S01に戻ることを確認する。

## 【実行コマンド】

* `cd client && npx playwright test tests/acceptance/a001_general_user_journey.spec.ts`

## 【期待結果】

* 手順1〜9のすべてがエラーなく完了し、各ステップの画面表示が仕様(`docs/P002-frontend-spec.md`)どおりであること。

## 【合否判定基準】

* 上記【期待結果】をすべて満たせばPASS。1つでも満たさなければFAIL。

## 【失敗時に記録する内容】

* 失敗したステップ番号、画面のスクリーンショット、関連するAPIリクエスト/レスポンス。

## 【修正禁止事項】

* アプリケーションコードを修正しない。テストコードをその場で都合よく変更しない。失敗したテストをスキップしない。期待値を変更して成功扱いにしない。同じ失敗に対して場当たり的な再テストを繰り返さない。失敗内容をテスト記録に残し、修正が必要な場合はP202(修正計画)以降に引き渡す。

## 【次タスクへ進む条件】

* 結果を `docs/test-records/YYYYMMDD-HHMM-test-record.md` に記録した時点で次のテストタスクへ進んでよい。

## 重要:

* アプリケーションコードを修正しないでください。
* テスト失敗時に、その場で修正して再テストしないでください。
* テスト失敗時は、失敗内容をテスト記録に残してください。
* このテストタスクの結果を記録したら、Reviewer Loopの停止条件に該当しない限り、次のテストタスクに自動的に進んでください。1テストタスクごとに人間の指示を待つ必要はありません。
