# CR(変更要求)の扱いに関する変更仕様

作成日: 2026-08-04
位置づけ: V0.6 に対する変更仕様。**本書は仕様のまとめであり、まだ V0.7 として実装していない。**

## 1. 変更の目的

V0.5 → V0.6 で 2 バージョンにわたって修正を重ねた「完了した CR を `docs/CR.md` から削除するのか残すのか」という問題は、個別の不備ではなく、**`docs/CR.md` が『状態の台帳』と『CR 本文の置き場』という 2 つの役割を兼ねていた**ことに起因していた。本変更は、この 2 つの役割を分離し、問題が発生する構造そのものを無くす。

副次的に、次の点も解消される。

* `TEMPLATE-CR.md` が「CR.md 全体の構造」と「CR 1 件の書式」を 1 ファイルに詰め込んでいた二重役割。
* `TEMPLATE-CR.md`(削除すると指示)と `SKILL-P904-cr-verify.md`(残すと指示)の矛盾。
* CR だけが、他の成果物(`U0NN`/`T0NN`/`A0NN`/`F0NN`)と異なり「目次ファイル+1 件 1 ファイル」の構造になっていなかった非一貫性。

## 2. 新しいファイル構成

| ファイル | 役割 | 作成 | 更新 |
| --- | --- | --- | --- |
| `docs/P901-cr-direction/CR-NNN.md` | 変更要求(人間が何をなぜ変えてほしいか) | P901 | 人間(起票後、着手前まで) |
| `docs/P903-cr-records/CR-NNN.md` | 対処の記録(AI が何をどう変えたか) | P903 | P903 → P904 → P905 |
| `docs/CR.md` | 状態の台帳(一覧と状態管理) | P901 | P901 / P903 / P904 |

* CR 番号は既存の ID 体系(`U001`/`T001`/`A001`/`F001`/`ADR-001`)に合わせて **3 桁**(`CR-001`)とする。
* 要求ファイルと記録ファイルは**同じ番号で 1 対 1 に対応**させる。番号だけで両者を突き合わせられる。
* 他の `P0NN-*-direction` は目次ファイル(例: `docs/P007-impl-direction.md`)を別に持つが、**CR では `docs/CR.md` が目次の役割を兼ねる**ため、`docs/P901-cr-direction.md` という目次ファイルは作らない。

### 2.1 ディレクトリ名を `P903-cr-records` とした理由

当初案では対処の記録を `docs/P904-cr-records/` としていたが、**`P903-cr-records` に改めた**。

P903 のスコープ決定(対象/対象外フェーズとその理由の記録。V0.5 で必須化)は、作業を**始める前**に行う判断である。記録ファイルの作成を P904(修正・テスト完了後)まで遅らせると、この判断を最後に思い出しながら書くことになり、事前の判断ではなく事後の正当化に変質してしまう。実際 CR-002 では、事前に「対象外」と見込んでいた P201〜P205 が作業中に「対象」へ変わっており、事前記録が無ければこの変化自体が残らない。

したがって記録ファイルは **P903 が起票し、P904・P905 が追記して完成させる**。ファイルを作成したフェーズの番号を名前に持たせる方針は、`docs/P202-fix-plan/F0NN-*.md` が P202 で作成され P203 で更新される既存の前例と同じである。

## 3. 各ファイルの内容

### 3.1 `docs/P901-cr-direction/CR-NNN.md`(変更要求)

P901 が、人間から会話・issue・メールなど**任意の形式**で示された要望をもとに作成する。人間がファイルを自分で書く必要はない。

記載する項目:

* タイトル
* 起票日、起票者(氏名・役職、チケット/issue リンク、議事録参照など。特定できない場合は不明である旨を明記)
* 背景・課題(なぜこの変更が必要か)
* 変更要求の内容(何をどう変えてほしいか。画面・API・データモデル・非機能要件のどこに関わるか)
* 期待する振る舞い(変更後の受け入れ条件。P904 がこれを 1 項目ずつ確認する)
* 影響が想定される範囲(画面/API/データモデル/関連 ADR)— P901 時点の見積もり
* 既存 ADR に抵触する要望(採用済みの決定を覆す要望)であれば、その旨
* 優先度の判断理由(影響度×緊急度ルーブリックの適用結果と、そう判断した理由。機械的に定まらない場合は ★FIXME★ を付す)

**記載しないもの**: 状態(→ `docs/CR.md`)、優先度の現在値(→ `docs/CR.md`)、対処内容(→ 記録ファイル)。

**人間による編集**: AI が作成した後、人間が内容を修正してよい。ただし**状態が `対応中` に移った以降は変更しない**(作業の前提が動くため)。修正したい場合は新しい CR を起票する。

### 3.2 `docs/P903-cr-records/CR-NNN.md`(対処の記録)

P903 が作業開始時に作成し、以降 P904・P905 が追記する。**どの節をどのフェーズが書くかを固定する**。

**P903 が書く(作業開始時、実装に着手する前)**

* CR の影響カテゴリの分類(UI 表示文言のみ/画面項目追加・変更/API 契約変更/データモデル変更/非機能・インフラ変更。複数該当可)
* 対象とするフェーズと、その理由
* 対象外としたフェーズと、再オープン不要と判断した理由
* 「データモデル変更」カテゴリに該当する場合は、マイグレーション方式の確認結果と冪等性の判断(V0.6 で追加したルール)

**P903 が書く(作業完了時)**

* 実際に変更したドキュメント・ソースコードの一覧
* **事前のスコープ想定と実際の差異**(作業前に「対象外」としたフェーズが実際には必要になった、など)。差異が無かった場合もその旨を記載する
* 実行したテストと結果(`docs/test-records/*.md` への参照)

**P904 が書く**

* 「期待する振る舞い」の各項目と、それを確認したテスト ID・確認方法の対応表
* 反映確認の結論(すべて確認できたか、不足があるか)

**P905 が書く**

* ADR 更新の要否と、その理由
* 更新した場合は該当 ADR 番号

**却下する場合**: 却下を決めたフェーズが記録ファイルを作成し、却下理由を書く(P901 時点で却下が決まる場合は P901 が作成する)。

### 3.3 `docs/CR.md`(状態の台帳)

CR 本文は一切持たず、一覧表のみを持つ。

| CR | タイトル | 状態 | 優先度 | 変更要求 | 対処記録 |
| --- | --- | --- | --- | --- | --- |
| CR-001 | ... | 完了 | 低 | [リンク](./P901-cr-direction/CR-001.md) | [リンク](./P903-cr-records/CR-001.md) |

* **完了した CR の行も削除せず、状態列を更新して残し続ける**。これが「状態管理」であり、これを規定することで「残すのか削除するのか」という判断そのものが発生しなくなる。
* 状態は従来どおり `未対応` / `対応中` / `反映確認中` / `完了` / `却下` の 5 値。**状態の唯一の正はこの台帳**とし、要求ファイル・記録ファイル側には状態を持たせない(二重管理による不整合を防ぐ)。
* 優先度も台帳側に持つ(運用の中で人間が変更しうるため)。P901 が判断した理由は要求ファイル側に残る。
* 更新のタイミング: P901 が行を追加(`未対応`)、P903 が着手時に `対応中`、P904 が確認時に `反映確認中` → 完了時に `完了`。
* 状態が 5 値あり、OKF 形式のチェックボックス(`[ ]`/`[~]`/`[x]`)では表現できないため、**台帳は表形式のままとする**(OKF 形式には寄せない)。

## 4. 各フェーズの変更点

| フェーズ | 変更内容 |
| --- | --- |
| P901 | 出力先が `docs/CR.md` から `docs/P901-cr-direction/CR-NNN.md` + 台帳への 1 行追加に変わる。番号の採番(`docs/P901-cr-direction/` を見て次番号)、複数要望が混ざっている場合の分割、優先度ルーブリックの適用は従来どおり |
| P902 | 参照先が `docs/CR.md` の本文から `docs/P901-cr-direction/CR-NNN.md` に変わる。要件定義書のバックアップ命名(`P001-requirement_{YYYYMMDD}_{CR番号}.md`)は変更なし |
| P903 | 記録ファイルを作成し、スコープ決定を**着手前に**書く。作業完了時に変更内容・差異・テスト結果を追記。台帳を `対応中` に更新 |
| P904 | **大幅に簡素化される**。CR 本文の退避が不要になり、V0.6 で追加した「既存の記法を現物で確認して複製する」手順も不要になる。受け入れ条件の確認結果を記録ファイルに書き、台帳の状態を更新するだけになる |
| P905 | ADR 判定結果の記録先が、記録ファイル内の自身の節に固定される |

## 5. テンプレートの変更

`TEMPLATE-CR.md` は 2 つの役割を兼ねていたため、3 つに分割する。

* `TEMPLATE-P901-cr-direction.md`(新規)— 変更要求 1 件の書式
* `TEMPLATE-P903-cr-record.md`(新規)— 対処記録の書式(P903/P904/P905 の各節を含む)
* `TEMPLATE-CR.md`(改訂)— 台帳の書式のみ。CR 本文のサンプルは持たない

## 6. この変更で解消される既知の問題

* **V0.6 の 1-1(完了 CR の記法)**: CR.md に本文が存在しなくなるため、「退避」という手順自体が消える。完了行は常に残すと規定されるため、二値の判断も発生しない。
* **V0.6_testbed で見つかった「低」の指摘**(`docs/CR.md` 冒頭の説明文が現行の記法と食い違う): 台帳の説明文が「本文は持たない・完了行は残す」で確定するため、記法とずれる余地が無くなる。
* **`TEMPLATE-CR.md` の二重役割**: 上記のとおり分割される。

## 7. 既存プロジェクトへの影響

`V0.4_testbed` / `V0.5_testbed` / `V0.6_testbed` は旧構造(`docs/CR.md` に本文、`docs/cr-records/` に完了分)のまま作られている。本変更を適用する際は、次のいずれかを選ぶ必要がある。

* 新規プロジェクトから適用し、既存プロジェクトは旧構造のまま運用する(Skill のルール変更は既存の生成物に遡及しない、という V0.6 で確認済みの方針に沿う)。
* 移行手順を用意する(`docs/cr-records/CR-NNN.md` を要求部分と対処部分に分割し、それぞれ新しいディレクトリに配置し直す)。

移行を行う場合、既存の CR-001〜CR-003 は要求と対処が 1 ファイルに混在しているため、機械的な分割はできず内容を読んだうえでの振り分けが必要になる。
