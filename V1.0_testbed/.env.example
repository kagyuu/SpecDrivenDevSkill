# .env.example — docker-compose.yml で参照する環境変数の例。
# 現時点のdocker-compose.ymlはDATABASE_PATHをコンテナ内固定値で設定しており、
# 追加の環境変数は必須ではない。将来的に外部設定が必要になった場合(例: 本番相当の
# BASE_URLやログレベル)はこのファイルに追記する。

# バックエンドのSQLiteファイルパス(コンテナ内、通常は変更不要)
DATABASE_PATH=/app/data/app.db
