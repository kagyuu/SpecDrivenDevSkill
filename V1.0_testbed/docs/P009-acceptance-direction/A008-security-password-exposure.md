あなたはReviewer Loop(実装横断レビュー担当)です。以下は1テストタスク分の作業範囲と完了条件を定義したものです。実施後は、結果(PASS/FAIL/BLOCKED/NOT RUNいずれであっても)を記録したうえで、Reviewer Loopの「停止条件」(`SKILL.md` 参照)に該当しない限り、`docs/P009-acceptance-direction.md` のWBSに従って自動的に次のテストタスクへ進んでください。人間の指示を待って停止しないでください。

# 【テストID】A008

## 【目的】

* `docs/P001-requirement.md` の非機能要件「パスワードはハッシュ化して保存する」を踏まえ、パスワード(平文・ハッシュ値とも)がAPIレスポンス・アプリケーションログに露出しないことを確認する。

## 【参照テスト計画】

* `docs/P006-test-plan.md` §3(非機能観点・セキュリティ)

## 【対象モジュール】

* `server/app/api/routers/users.py`, `server/app/api/routers/auth.py`, ログ出力全般

## 【前提条件】全モジュールビルドが成功していること

* `cd server && uv run python -m pytest tests/ -v` が全PASS

## 【使用するテストデータ】

* 管理者 `admin001`

## 【事前準備】

* バックエンドを標準出力ログを収集できる状態(ファイルへのリダイレクト等)で起動する。

## 【実行手順】

1. `admin001` でログインし、S07から新規ユーザーを初期パスワード付きで登録する。
2. `GET /api/users` のレスポンス全体をダンプし、`password`/`password_hash`/入力した初期パスワード文字列のいずれも含まれないことを確認する。
3. `POST /api/auth/login` のレスポンスにパスワード関連の値が含まれないことを確認する。
4. 手順1〜3で出力されたアプリケーションログ(標準出力)を確認し、平文パスワード・bcryptハッシュ値のいずれも含まれないことを確認する。

## 【実行コマンド】

* `cd server && uv run python -m pytest tests/acceptance/test_a008_password_exposure.py -v`

## 【期待結果】

* レスポンスボディ・ログのいずれにもパスワード関連の値(平文・ハッシュとも)が含まれない。

## 【合否判定基準】

* 上記が満たされればPASS。1箇所でも露出があればFAIL(重大度の高い不具合として扱う)。

## 【失敗時に記録する内容】

* 露出箇所(レスポンスかログか)、該当する具体的な出力内容(このテスト記録自体に平文パスワードを転記しないよう、露出した事実とフィールド名のみを記録し、値はマスクする)。

## 【修正禁止事項】

* アプリケーションコードを修正しない。テストコードをその場で都合よく変更しない。失敗したテストをスキップしない。期待値を変更して成功扱いにしない。同じ失敗に対して場当たり的な再テストを繰り返さない。失敗内容をテスト記録に残し、修正が必要な場合はP202以降に引き渡す。

## 【次タスクへ進む条件】

* 結果を記録した時点で次へ進んでよい。

## 重要:

* アプリケーションコードを修正しないでください。
* テスト失敗時に、その場で修正して再テストしないでください。
* テスト失敗時は、失敗内容をテスト記録に残してください。
* このテストタスクの結果を記録したら、Reviewer Loopの停止条件に該当しない限り、次のテストタスクに自動的に進んでください。1テストタスクごとに人間の指示を待つ必要はありません。
