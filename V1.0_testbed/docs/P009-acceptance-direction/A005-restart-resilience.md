あなたはReviewer Loop(実装横断レビュー担当)です。以下は1テストタスク分の作業範囲と完了条件を定義したものです。実施後は、結果(PASS/FAIL/BLOCKED/NOT RUNいずれであっても)を記録したうえで、Reviewer Loopの「停止条件」(`SKILL.md` 参照)に該当しない限り、`docs/P009-acceptance-direction.md` のWBSに従って自動的に次のテストタスクへ進んでください。人間の指示を待って停止しないでください。

# 【テストID】A005

## 【目的】

* `docs/P003-backend-spec.md` §4のマイグレーション方式が実際に冪等であり、**アプリケーションを停止・再起動しても正常に起動すること**、かつ既存データが失われないことを確認する。
* この観点は、`docs/P006-test-plan.md` §4で明記したとおり、単体テスト(P007)・スプリント内結合テスト(P008)がテストごとに新しい一時データストアを使う構成のため検出できず、本フェーズ(P009、受け入れ結合テスト)が担当すると定めた観点である。永続化された同一のSQLiteファイルに対して実際にプロセスを2回以上起動することで初めて検証できる。

## 【参照テスト計画】

* `docs/P006-test-plan.md` §4(運用観点・再起動耐性)
* `docs/P003-backend-spec.md` §4(マイグレーション方式)

## 【対象モジュール】

* `server/app/db/migrate.py`, `server/app/main.py`(起動時マイグレーション実行、`lifespan`)

## 【前提条件】全モジュールビルドが成功していること

* `cd server && uv run python -m pytest tests/ -v` が全PASS

## 【使用するテストデータ】

* 新規に用意する永続DBファイル(例: `server/data/a005_restart_test.db`。他のP009テストケースとは共有しない専用ファイルとする)

## 【事前準備】

* `server/data/a005_restart_test.db` が存在しないことを確認する(存在する場合は本テスト専用ファイルであることを確認したうえで削除してから開始する)。

## 【実行手順】

1. `DATABASE_PATH=./data/a005_restart_test.db` を指定して `cd server && uv run uvicorn app.main:app --port 8002` でアプリケーションを起動する(1回目の起動)。
2. 起動ログにマイグレーションエラーが無いこと、プロセスが正常に待受状態になることを確認する。
3. `admin001`(シード済み管理者)でログインし、会議室 `会議室Z` を1件登録する。続けて `会議室Z` に予約1件(本日 `15:00-16:00`、件名「再起動確認用」)を登録する。
4. アプリケーションプロセスを通常のシグナル(`SIGTERM`、Ctrl+C相当)で停止する。
5. 同じ `DATABASE_PATH=./data/a005_restart_test.db` を指定して、同じコマンドでアプリケーションを再起動する(2回目の起動)。
6. 起動ログにマイグレーションエラー(`duplicate column name` 等)が無いこと、プロセスが正常に待受状態になることを確認する。
7. 再起動後のプロセスに対し、`admin001` で再ログインし、`GET /api/rooms` に `会議室Z` が含まれること、`GET /api/reservations` に手順3で作成した予約が含まれることを確認する(データが失われていないこと)。
8. 同じ手順1〜7をもう一度繰り返し、3回目の起動でも同様にエラーなく起動し、データが保持されていることを確認する(2回だけでなく3回目でも問題が起きないことを確認し、必要十分性の限界はあるが実施可能な範囲でより高い確度を得る)。

## 【実行コマンド】

* `cd server && DATABASE_PATH=./data/a005_restart_test.db uv run python -m pytest tests/acceptance/test_a005_restart_resilience.py -v`(起動・停止・再起動を自動化したテストとして実装する場合。プロセスの起動・停止は `subprocess` でuvicornを制御する)

## 【期待結果】

* 1回目・2回目・3回目いずれの起動でもマイグレーションエラーが発生せず、プロセスが正常に起動する。
* 2回目・3回目の起動後も、1回目に作成した会議室・予約データが失われずに参照できる。

## 【合否判定基準】

* 上記【期待結果】をすべて満たせばPASS。いずれかの起動でエラーが発生した場合、またはデータが失われていた場合はFAILとする。

## 【失敗時に記録する内容】

* 失敗した起動回(1〜3回目のいずれか)、起動時のエラーログ全文、`schema_migrations` テーブルの内容(`SELECT * FROM schema_migrations`)、データ欠落があった場合はどのデータが欠落したか。

## 【修正禁止事項】

* アプリケーションコードを修正しない。テストコードをその場で都合よく変更しない。失敗したテストをスキップしない。期待値を変更して成功扱いにしない。同じ失敗に対して場当たり的な再テストを繰り返さない。失敗内容をテスト記録に残し、修正が必要な場合はP202以降に引き渡す。特に、マイグレーションが2回目以降に失敗する不具合が見つかった場合、その場でSQLを書き換えて回避しない(重大度の高い不具合として記録し、P202に引き渡す)。

## 【次タスクへ進む条件】

* 結果を記録した時点で次へ進んでよい。テスト専用DBファイル(`server/data/a005_restart_test.db`)はテスト記録にパスを残したうえで、次のテストに影響しないよう後片付け(削除)してよい。

## 重要:

* アプリケーションコードを修正しないでください。
* テスト失敗時に、その場で修正して再テストしないでください。
* テスト失敗時は、失敗内容をテスト記録に残してください。
* このテストタスクの結果を記録したら、Reviewer Loopの停止条件に該当しない限り、次のテストタスクに自動的に進んでください。1テストタスクごとに人間の指示を待つ必要はありません。
