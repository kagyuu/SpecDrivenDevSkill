あなたはReviewer Loop(実装横断レビュー担当)です。以下は1テストタスク分の作業範囲と完了条件を定義したものです。実施後は、結果(PASS/FAIL/BLOCKED/NOT RUNいずれであっても)を記録したうえで、Reviewer Loopの「停止条件」(`SKILL.md` 参照)に該当しない限り、`docs/P009-acceptance-direction.md` のWBSに従って自動的に次のテストタスクへ進んでください。人間の指示を待って停止しないでください。

# 【テストID】A007

## 【目的】

* `docs/P001-requirement.md` の非機能要件「カレンダー画面の表示は3秒以内」を、実行可能な範囲(開発環境上の測定、本番相当の負荷試験ではない)で確認する。本番相当環境での正式な性能検証は `docs/P302-deliver.md` に委譲する(`docs/P003-backend-spec.md` §7)。

## 【参照テスト計画】

* `docs/P006-test-plan.md` §3(非機能観点・性能)

## 【対象モジュール】

* `client/src/pages/CalendarPage.tsx`, `server/app/api/routers/reservations.py`

## 【前提条件】全モジュールビルドが成功していること

* `cd server && uv run python -m pytest tests/ -v` が全PASS

## 【使用するテストデータ】

* 会議室10件、1週間分・全会議室に対して1日3件程度の予約(合計約210件)を投入した状態

## 【事前準備】

* 上記規模のデータをシードする。

## 【実行手順】

1. `user001` でログインし、S02の初期表示にかかる時間(ログイン後、カレンダーグリッドが描画完了するまで)をブラウザの計測機能(Performance API等)で測定する。
2. 3回計測し、いずれも目標値内であることを確認する。

## 【実行コマンド】

* `cd client && npx playwright test tests/acceptance/a007_performance_calendar.spec.ts`

## 【期待結果】

* 3回の計測すべてで3秒以内に描画が完了する(開発環境上の参考値であり、本番相当環境でのSLA保証ではないことをテスト記録に明記する)。

## 【合否判定基準】

* 3回とも3秒以内であればPASS。1回でも超えた場合はFAILとし、原因(データ量、クエリ、フロントエンド描画のいずれか)をテスト記録に記載する。

## 【失敗時に記録する内容】

* 実測時間、ブラウザの開発者ツールで確認したネットワーク待ち時間とレンダリング時間の内訳。

## 【修正禁止事項】

* アプリケーションコードを修正しない。テストコードをその場で都合よく変更しない。失敗したテストをスキップしない。期待値を変更して成功扱いにしない。同じ失敗に対して場当たり的な再テストを繰り返さない。失敗内容をテスト記録に残し、修正が必要な場合はP202以降に引き渡す。

## 【次タスクへ進む条件】

* 結果を記録した時点で次へ進んでよい。

## 重要:

* アプリケーションコードを修正しないでください。
* テスト失敗時に、その場で修正して再テストしないでください。
* テスト失敗時は、失敗内容をテスト記録に残してください。
* このテストタスクの結果を記録したら、Reviewer Loopの停止条件に該当しない限り、次のテストタスクに自動的に進んでください。1テストタスクごとに人間の指示を待つ必要はありません。
