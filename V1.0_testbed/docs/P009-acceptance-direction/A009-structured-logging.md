あなたはReviewer Loop(実装横断レビュー担当)です。以下は1テストタスク分の作業範囲と完了条件を定義したものです。実施後は、結果(PASS/FAIL/BLOCKED/NOT RUNいずれであっても)を記録したうえで、Reviewer Loopの「停止条件」(`SKILL.md` 参照)に該当しない限り、`docs/P009-acceptance-direction.md` のWBSに従って自動的に次のテストタスクへ進んでください。人間の指示を待って停止しないでください。

# 【テストID】A009

## 【目的】

* 運用者視点で、`docs/P003-backend-spec.md` §7に定めた「標準出力への構造化ログ(JSON Lines)出力」が実際に行われていることを確認する(本番のログ集約基盤への転送確認自体は `docs/P302-deliver.md` に委譲するため対象外)。

## 【参照テスト計画】

* `docs/P006-test-plan.md` §3(非機能観点・運用)

## 【対象モジュール】

* `server/` 全体のログ出力

## 【前提条件】全モジュールビルドが成功していること

* `cd server && uv run python -m pytest tests/ -v` が全PASS

## 【使用するテストデータ】

* 一般ユーザー `user001`

## 【事前準備】

* バックエンドを標準出力をファイルにリダイレクトして起動する(例: `uv run uvicorn app.main:app > server.log 2>&1`)。

## 【実行手順】

1. `user001` でログイン(成功)、続けて誤ったパスワードでログイン(失敗)する。
2. `server.log` の該当行を確認し、各行がJSONとしてパース可能であること、リクエストパス・ステータスコード・タイムスタンプ・(失敗時は)`error_code` に相当するフィールドを含むことを確認する。

## 【実行コマンド】

* `cd server && uv run python -m pytest tests/acceptance/test_a009_structured_logging.py -v`(ログファイルの内容を検証するテストとして実装する)

## 【期待結果】

* 出力された各ログ行がJSONとしてパース可能であり、上記フィールドを含む。

## 【合否判定基準】

* 上記が満たされればPASS。ログがJSON形式でない、または必要フィールドを欠く場合はFAIL。

## 【失敗時に記録する内容】

* 実際のログ出力例(該当行の抜粋)、欠けていたフィールド名。

## 【修正禁止事項】

* アプリケーションコードを修正しない。テストコードをその場で都合よく変更しない。失敗したテストをスキップしない。期待値を変更して成功扱いにしない。同じ失敗に対して場当たり的な再テストを繰り返さない。失敗内容をテスト記録に残し、修正が必要な場合はP202以降に引き渡す。

## 【次タスクへ進む条件】

* 結果を記録した時点で、本フェーズ(P009)の全テストが完了する。全テストの結果を集計し、`docs/P009-acceptance-direction.md` の全行が `[x]` になっていることを確認する。FAIL/BLOCKEDが1件でも残っていればP202への引き渡しが必要である旨を目次に明記する。

## 重要:

* アプリケーションコードを修正しないでください。
* テスト失敗時に、その場で修正して再テストしないでください。
* テスト失敗時は、失敗内容をテスト記録に残してください。
* このテストタスクの結果を記録したら、Reviewer Loopの停止条件に該当しない限り、次のテストタスクに自動的に進んでください。1テストタスクごとに人間の指示を待つ必要はありません。
