あなたはExecutor(実装担当)です。以下は1テストタスク分の作業範囲と完了条件を定義したものです。実施後は、結果(PASS/FAIL/BLOCKED/NOT RUNいずれであっても)を記録したうえで、Executor Stepの「停止条件」(`SKILL.md` 参照)に該当しない限り、`docs/P008-test-direction.md` のWBSに従って自動的に次のテストタスクへ進んでください。人間の指示を待って停止しないでください。

# 【テストID】T008

## 【目的】

* S07(ユーザー管理画面)で既存の社員IDを使って新規ユーザーを登録しようとした際、DBのUNIQUE制約違反がService層で409 `DUPLICATE_EMPLOYEE_ID` に正しく変換され、画面にメッセージが表示されることを確認する。

## 【参照テスト計画】

* `docs/P006-test-plan.md` §2(業務ルール違反観点)

## 【対象モジュール】

* `client/src/pages/UserAdminPage.tsx`, `server/app/api/routers/users.py`, `server/app/repositories/users.py`

## 【前提条件】対象スプリントの全モジュールビルドが成功していること

* U004範囲の単体テストが全PASSであること

## 【使用するテストデータ】

* 管理者ユーザー `admin001`、既存の一般ユーザー `user001`

## 【事前準備】

* 上記データをシードした状態でバックエンド・フロントエンドを起動する。

## 【実行手順】

1. `admin001` でログインしS07を開く。
2. 社員ID `user001`(既存)で新規ユーザー登録フォームを送信する。
3. 画面上のエラーメッセージを確認する。

## 【実行コマンド】

* `cd client && npx playwright test tests/integration/t008_duplicate_employee_id.spec.ts`

## 【期待結果】

* API呼び出しが409 `DUPLICATE_EMPLOYEE_ID` を返し、画面に「この社員IDは既に登録されています」が表示される。登録済みユーザー数が増えていないこと(`GET /api/users` で確認)。

## 【合否判定基準】

* 上記がすべて満たされればPASS。

## 【失敗時に記録する内容】

* 実際のレスポンス、表示メッセージ、登録後のユーザー数。

## 【修正禁止事項】

* アプリケーションコードを修正しない。テストコードをその場で都合よく変更しない。失敗したテストをスキップしない。期待値を変更して成功扱いにしない。同じ失敗に対して場当たり的な再テストを繰り返さない。失敗内容をテスト記録に残し、次工程に引き渡す。

## 【次タスクへ進む条件】

* 結果を記録した時点で次へ進んでよい。

## 重要:

* アプリケーションコードを修正しないでください。
* テスト失敗時に、その場で修正して再テストしないでください。
* テスト失敗時は、失敗内容をテスト記録に残してください。
* このテストタスクの結果を記録したら、Executor Stepの停止条件に該当しない限り、次のテストタスクに自動的に進んでください。1テストタスクごとに人間の指示を待つ必要はありません。
