あなたはExecutor(実装担当)です。以下は1テストタスク分の作業範囲と完了条件を定義したものです。実施後は、結果(PASS/FAIL/BLOCKED/NOT RUNいずれであっても)を記録したうえで、Executor Stepの「停止条件」(`SKILL.md` 参照)に該当しない限り、`docs/P008-test-direction.md` のWBSに従って自動的に次のテストタスクへ進んでください。人間の指示を待って停止しないでください。

# 【テストID】T009

## 【目的】

* 一般ユーザーがS06/S07(管理者専用画面)へURL直打ちでアクセスした場合、クライアント側ガードで表示が防がれ、かつ仮にAPI呼び出しが発生してもサーバー側403で保護されることを確認する(`docs/P002-frontend-spec.md` §6の二重防御の結合確認)。

## 【参照テスト計画】

* `docs/P006-test-plan.md` §2(認可エラー観点)

## 【対象モジュール】

* `client/src/pages/RoomAdminPage.tsx`, `client/src/pages/UserAdminPage.tsx`, `client/src/router.tsx`, `server/app/api/deps.py`

## 【前提条件】対象スプリントの全モジュールビルドが成功していること

* U004範囲の単体テストが全PASSであること

## 【使用するテストデータ】

* 一般ユーザー `user001`

## 【事前準備】

* シードデータを用意しバックエンド・フロントエンドを起動する。

## 【実行手順】

1. `user001` でログインする。
2. ブラウザのURLバーで直接S06・S07に相当するパスへ遷移する。
3. 画面の表示内容を確認する。
4. 開発者ツール等で `GET /api/rooms?include_inactive=true` および `GET /api/users` を直接呼び、レスポンスを確認する。

## 【実行コマンド】

* `cd client && npx playwright test tests/integration/t009_admin_screen_guard.spec.ts`

## 【期待結果】

* 手順3: 会議室・ユーザーの一覧/フォームがDOMに描画されず、権限不足の案内が表示される。
* 手順4: いずれも403、`error_code: "FORBIDDEN"`。

## 【合否判定基準】

* 上記がすべて満たされればPASS。

## 【失敗時に記録する内容】

* 実際のDOM内容(フォームが描画されていた場合はそのスクリーンショット)、APIレスポンス。

## 【修正禁止事項】

* アプリケーションコードを修正しない。テストコードをその場で都合よく変更しない。失敗したテストをスキップしない。期待値を変更して成功扱いにしない。同じ失敗に対して場当たり的な再テストを繰り返さない。失敗内容をテスト記録に残し、次工程に引き渡す。

## 【次タスクへ進む条件】

* 結果を記録した時点で次へ進んでよい。本テストがP008の最終テストである。全テスト完了後、`docs/P008-test-direction.md` の全行が `[x]` になっていることを確認する。

## 重要:

* アプリケーションコードを修正しないでください。
* テスト失敗時に、その場で修正して再テストしないでください。
* テスト失敗時は、失敗内容をテスト記録に残してください。
* このテストタスクの結果を記録したら、Executor Stepの停止条件に該当しない限り、次のテストタスクに自動的に進んでください。1テストタスクごとに人間の指示を待つ必要はありません。
