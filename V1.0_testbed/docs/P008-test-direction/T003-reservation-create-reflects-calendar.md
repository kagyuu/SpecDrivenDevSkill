あなたはExecutor(実装担当)です。以下は1テストタスク分の作業範囲と完了条件を定義したものです。実施後は、結果(PASS/FAIL/BLOCKED/NOT RUNいずれであっても)を記録したうえで、Executor Stepの「停止条件」(`SKILL.md` 参照)に該当しない限り、`docs/P008-test-direction.md` のWBSに従って自動的に次のテストタスクへ進んでください。人間の指示を待って停止しないでください。

# 【テストID】T003

## 【目的】

* S03(予約作成画面)で登録した予約が、`POST /api/reservations` → DB書き込み → `GET /api/reservations` を経てS02(予約カレンダー画面)に正しく反映されることを確認する(画面/API/DBを貫く結合確認)。

## 【参照テスト計画】

* `docs/P006-test-plan.md` §2(機能観点、正常系)

## 【対象モジュール】

* `client/src/pages/ReservationCreatePage.tsx`, `client/src/pages/CalendarPage.tsx`, `server/app/api/routers/reservations.py`, `server/app/repositories/reservations.py`

## 【前提条件】対象スプリントの全モジュールビルドが成功していること

* U002範囲の単体テスト(`docs/P007-impl-direction/U002-reservation-core.md`)が全PASSであること

## 【使用するテストデータ】

* シードユーザー `user001`、会議室データ1件(`会議室A`、収容人数10)

## 【事前準備】

* 一時DBに会議室1件(`会議室A`、収容人数10)を直接SQLでINSERTする(会議室登録APIはU003の対象でありU002時点では未実装のため、本テストのテストデータ準備はAPI経由ではなく直接INSERTで行う。これはテストデータ準備の手段であり、テスト対象そのものではないため問題ない)。
* バックエンド・フロントエンドを起動する。

## 【実行手順】

1. `user001` でログインする。
2. S03で `会議室A`、本日の日付、`13:00-14:00`、件名「テスト会議」で登録する。
3. S02へ戻り、該当日時・会議室のセルに「user001 / テスト会議」が表示されることを確認する。

## 【実行コマンド】

* `cd client && npx playwright test tests/integration/t003_reservation_create_reflects_calendar.spec.ts`

## 【期待結果】

* S03登録後、201が返り、S02のセルに予約サマリが表示される。

## 【合否判定基準】

* 上記が満たされればPASS。

## 【失敗時に記録する内容】

* 失敗したステップ、APIレスポンス、画面のスクリーンショット。

## 【修正禁止事項】

* アプリケーションコードを修正しない。テストコードをその場で都合よく変更しない。失敗したテストをスキップしない。期待値を変更して成功扱いにしない。同じ失敗に対して場当たり的な再テストを繰り返さない。失敗内容をテスト記録に残し、次工程に引き渡す。

## 【次タスクへ進む条件】

* 結果を記録した時点で次へ進んでよい。

## 重要:

* アプリケーションコードを修正しないでください。
* テスト失敗時に、その場で修正して再テストしないでください。
* テスト失敗時は、失敗内容をテスト記録に残してください。
* このテストタスクの結果を記録したら、Executor Stepの停止条件に該当しない限り、次のテストタスクに自動的に進んでください。1テストタスクごとに人間の指示を待つ必要はありません。
