あなたはExecutor(実装担当)です。以下は1テストタスク分の作業範囲と完了条件を定義したものです。実施後は、結果(PASS/FAIL/BLOCKED/NOT RUNいずれであっても)を記録したうえで、Executor Stepの「停止条件」(`SKILL.md` 参照)に該当しない限り、`docs/P008-test-direction.md` のWBSに従って自動的に次のテストタスクへ進んでください。人間の指示を待って停止しないでください。

# 【テストID】T007

## 【目的】

* S06(会議室管理画面)で会議室を無効化(論理削除)した後、S03(予約作成画面)の会議室プルダウンからその会議室が除外されることを確認する(`docs/P003-backend-spec.md` §5.4の `include_inactive` 挙動の結合確認)。

## 【参照テスト計画】

* `docs/P006-test-plan.md` §2(機能観点)

## 【対象モジュール】

* `client/src/pages/RoomAdminPage.tsx`, `client/src/pages/ReservationCreatePage.tsx`, `server/app/api/routers/rooms.py`

## 【前提条件】対象スプリントの全モジュールビルドが成功していること

* U003範囲の単体テストが全PASSであること

## 【使用するテストデータ】

* 管理者ユーザー `admin001`、一般ユーザー `user001`、会議室1件(`会議室B`)

## 【事前準備】

* 上記データをシードした状態でバックエンド・フロントエンドを起動する。

## 【実行手順】

1. `admin001` でログインしS06を開き、`会議室B` を削除(無効化)する。
2. ログアウトし `user001` でログインしS03を開く。
3. 会議室プルダウンに `会議室B` が含まれないことを確認する。
4. 再度 `admin001` でログインしS06を開き、無効化した会議室が一覧(無効フラグ付き)に表示されることを確認する。

## 【実行コマンド】

* `cd client && npx playwright test tests/integration/t007_room_deactivation.spec.ts`

## 【期待結果】

* 手順3でプルダウンに `会議室B` が出ない。手順4でS06には無効状態で残っている。

## 【合否判定基準】

* 上記が満たされればPASS。

## 【失敗時に記録する内容】

* プルダウンの実際の選択肢一覧、S06の一覧表示内容。

## 【修正禁止事項】

* アプリケーションコードを修正しない。テストコードをその場で都合よく変更しない。失敗したテストをスキップしない。期待値を変更して成功扱いにしない。同じ失敗に対して場当たり的な再テストを繰り返さない。失敗内容をテスト記録に残し、次工程に引き渡す。

## 【次タスクへ進む条件】

* 結果を記録した時点で次へ進んでよい。

## 重要:

* アプリケーションコードを修正しないでください。
* テスト失敗時に、その場で修正して再テストしないでください。
* テスト失敗時は、失敗内容をテスト記録に残してください。
* このテストタスクの結果を記録したら、Executor Stepの停止条件に該当しない限り、次のテストタスクに自動的に進んでください。1テストタスクごとに人間の指示を待つ必要はありません。
