あなたはExecutor(実装担当)です。以下の1テストタスクだけを実施してください。

# 【テストID】T007

## 【目的】

* 一般ユーザーが会議室管理API(`POST`/`PUT`/`DELETE /api/rooms`)を呼び出した場合に `403 FORBIDDEN` になることを確認する(権限まわりの結合テスト。P001「一般ユーザーが管理者専用画面・APIにアクセスできないこと」に対応)。

## 【参照テスト計画】

* `docs/P006-test-plan.md` §2.1(S06行)、§2.2(rooms行)

## 【対象モジュール】

* `server/app/api/rooms.py`、`server/app/api/deps.py`(`require_admin`)

## 【前提条件】対象スプリントの全モジュールビルドが成功していること

* U002が完了していること。

## 【使用するテストデータ】

* 一般ユーザー: `employee_id=E0001`, `role=general`

## 【事前準備】

* 一般ユーザーでログインしCookieを取得する。

## 【実行手順】

1. 一般ユーザーのCookieで `POST /api/rooms` を呼ぶ。
2. 一般ユーザーのCookieで `PUT /api/rooms/1` を呼ぶ。
3. 一般ユーザーのCookieで `DELETE /api/rooms/1` を呼ぶ。

## 【実行コマンド】

* `cd server && uv run pytest tests/test_api_rooms.py::test_forbidden_for_general_user -v`

## 【期待結果】

* 3手順すべて `403 FORBIDDEN`。

## 【合否判定基準】

* 3手順すべてが期待結果どおりであればPASS。1つでも異なればFAIL。

## 【失敗時に記録する内容】

* 各手順の実際のステータスコードを記録する。

## 【修正禁止事項】

* アプリケーションコードを修正しないでください。

## 【次タスクへ進む条件】

* 記録完了後、T008の指示を待つ。

## 重要:

* アプリケーションコードを修正しないでください。
* テスト失敗時に、その場で修正して再テストしないでください。
* テスト失敗時は、失敗内容をテスト記録に残してください。
* 次のテストタスクには進まないでください。
