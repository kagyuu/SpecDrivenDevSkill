あなたはExecutor(実装担当)です。以下の1テストタスクだけを実施してください。

# 【テストID】T016

## 【目的】

* 予約者本人が自分の予約を編集でき、変更内容が反映されることを確認する。

## 【参照テスト計画】

* `docs/P006-test-plan.md` §2.1(S04行)

## 【対象モジュール】

* `server/app/api/reservations.py`、`server/app/services/reservation_service.py`(`update`、`check_editable`)

## 【前提条件】対象スプリントの全モジュールビルドが成功していること

* U004が完了していること。

## 【使用するテストデータ】

* T011の予約(作成者 `E0001`)、変更後件名「定例MTG(変更後)」

## 【事前準備】

* 予約作成者 `E0001` でログインしCookieを取得する。

## 【実行手順】

1. `PUT /api/reservations/{reservation_id}` に件名変更後のデータを送信する。
2. `GET /api/reservations/{reservation_id}` で反映を確認する。

## 【実行コマンド】

* `cd server && uv run pytest tests/test_api_reservations.py::test_update_by_owner -v`

## 【期待結果】

* 手順1: `200 OK`。
* 手順2: `title` が変更後の値になっている。

## 【合否判定基準】

* 両手順が期待結果どおりであればPASS。

## 【失敗時に記録する内容】

* 実際のレスポンスボディを記録する。

## 【修正禁止事項】

* アプリケーションコードを修正しないでください。

## 【次タスクへ進む条件】

* 記録完了後、T017の指示を待つ。

## 重要:

* アプリケーションコードを修正しないでください。
* テスト失敗時に、その場で修正して再テストしないでください。
* テスト失敗時は、失敗内容をテスト記録に残してください。
* 次のテストタスクには進まないでください。
