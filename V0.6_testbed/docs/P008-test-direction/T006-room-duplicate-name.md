あなたはExecutor(実装担当)です。以下の1テストタスクだけを実施してください。

# 【テストID】T006

## 【目的】

* 既に有効な会議室と同名の会議室を登録しようとした場合に `400 VALIDATION_ERROR` になることを確認する。

## 【参照テスト計画】

* `docs/P006-test-plan.md` §2.1(S06行)

## 【対象モジュール】

* `server/app/services/room_service.py`、`server/app/repositories/room_repository.py`

## 【前提条件】対象スプリントの全モジュールビルドが成功していること

* U002が完了していること。T005で「会議室A」が登録済みであること。

## 【使用するテストデータ】

* `{"name": "会議室A", "capacity": 4, "equipment": [], "is_active": true}`(既存と同名)

## 【事前準備】

* 管理者でログインしCookieを取得する。T005の登録データが存在すること。

## 【実行手順】

1. `POST /api/rooms` に同名の会議室データを送信する。

## 【実行コマンド】

* `cd server && uv run pytest tests/test_api_rooms.py::test_duplicate_name_rejected -v`

## 【期待結果】

* `400`、エラーコード `VALIDATION_ERROR`、`details` に `field: name` を含む。

## 【合否判定基準】

* 期待結果どおりであればPASS。

## 【失敗時に記録する内容】

* 実際のステータスコード・エラーボディを記録する。

## 【修正禁止事項】

* アプリケーションコードを修正しないでください。

## 【次タスクへ進む条件】

* 記録完了後、T007の指示を待つ。

## 重要:

* アプリケーションコードを修正しないでください。
* テスト失敗時に、その場で修正して再テストしないでください。
* テスト失敗時は、失敗内容をテスト記録に残してください。
* 次のテストタスクには進まないでください。
