あなたはExecutor(実装担当)です。以下の1テストタスクだけを実施してください。

# 【テストID】T020

## 【目的】

* 予約更新時、自分自身の予約は重複判定から除外され(同じ時間帯のまま他項目だけ更新できる)、かつ他の予約とは正しく重複判定されることを確認する。

## 【参照テスト計画】

* `docs/P006-test-plan.md` §2.2(PUT /api/reservations/{reservation_id}行)

## 【対象モジュール】

* `server/app/repositories/reservation_repository.py`(`update_with_participants`、`has_overlap` の `exclude_reservation_id`)

## 【前提条件】対象スプリントの全モジュールビルドが成功していること

* U004が完了していること。会議室Aに予約X(10:00-11:00、作成者 `E0001`)と予約Y(13:00-14:00、作成者 `E0001`)が存在すること。

## 【使用するテストデータ】

* 予約Xを同じ時間帯(10:00-11:00)のまま件名だけ変更するリクエスト
* 予約Xを予約Yの時間帯(13:00-14:00)に変更するリクエスト(他予約との重複)

## 【事前準備】

* `E0001` でログインしCookieを取得する。

## 【実行手順】

1. 予約Xを同じ時間帯のまま件名だけ変更するリクエストを送信する。
2. 予約Xを予約Yの時間帯に変更するリクエストを送信する。

## 【実行コマンド】

* `cd server && uv run pytest tests/test_api_reservations.py::test_update_overlap_self_excluded -v`

## 【期待結果】

* 手順1: `200 OK`(自分自身との重複はエラーにならない)。
* 手順2: `409 RESERVATION_CONFLICT`(予約Yとの重複)。

## 【合否判定基準】

* 両手順が期待結果どおりであればPASS。

## 【失敗時に記録する内容】

* 両手順の実際のステータスコードを記録する。

## 【修正禁止事項】

* アプリケーションコードを修正しないでください。

## 【次タスクへ進む条件】

* 記録完了後、全テスト(T001〜T020)が完了した旨をP008目次に反映する。sprint4の全テストが完了した時点でP008目次のU004行相当のチェックを進める。次工程(P009)へ引き渡す。

## 重要:

* アプリケーションコードを修正しないでください。
* テスト失敗時に、その場で修正して再テストしないでください。
* テスト失敗時は、失敗内容をテスト記録に残してください。
* 次のテストタスクには進まないでください。
