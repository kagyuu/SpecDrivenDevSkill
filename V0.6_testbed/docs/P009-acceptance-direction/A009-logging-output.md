あなたはReviewer Loop(実装横断レビュー担当)です。以下の1テストタスクだけを実施してください。

# 【テストID】A009

## 【目的】

* アプリケーションが標準出力に構造化ログ(JSON Lines)を出力し、エラー発生時にエラーログが出力されることを確認する(`docs/P003-backend-spec.md` §8、P001「ログ出力先とその監視方法」に対応)。

## 【参照テスト計画】

* `docs/P006-test-plan.md` §3(運用)

## 【対象モジュール】

* `server/app/main.py`(ロギング設定)、`server/app/api/errors.py`

## 【前提条件】全モジュールビルドが成功していること

* U001〜U004が完了していること。

## 【使用するテストデータ】

* 正常なリクエスト1件、意図的にエラーを発生させるリクエスト1件(例: 存在しないIDへのアクセス)

## 【事前準備】

* サーバーを標準出力をキャプチャできる形で起動する。

## 【実行手順】

1. 正常な `GET /api/rooms` を呼び、標準出力にアクセスログ(JSON Lines形式)が出力されることを確認する。
2. 存在しないIDへの `GET /api/reservations/99999` を呼び、標準出力にエラーログが出力されることを確認する。

## 【実行コマンド】

* `cd server && uv run pytest tests/acceptance/test_a009_logging.py -v`

## 【期待結果】

* 手順1・2ともに、標準出力に1行1JSONの形式でログが出力される。

## 【合否判定基準】

* 両手順が期待結果どおりであればPASS。

## 【失敗時に記録する内容】

* 実際の標準出力の内容(抜粋)を記録する。

## 【修正禁止事項】

* アプリケーションコードを修正しないでください。

## 【次タスクへ進む条件】

* 記録完了後、A010の指示を待つ。

## 重要:

* アプリケーションコードを修正しないでください。
* テスト失敗時に、その場で修正して再テストしないでください。
* テスト失敗時は、失敗内容をテスト記録に残してください。
* 次のテストタスクには進まないでください。
