あなたはReviewer Loop(実装横断レビュー担当)です。以下の1テストタスクだけを実施してください。

# 【テストID】A002

## 【目的】

* 予約作成(sprint3)からカレンダー反映、マイ予約一覧表示、編集、取消(sprint4)までの一連の業務フローが、スプリントをまたいで通しで動作することを確認する。

## 【参照テスト計画】

* `docs/P006-test-plan.md` §2.1(S05行「受入/システム」列)

## 【対象モジュール】

* `server/app/api/reservations.py`、`client/src/pages/CalendarPage.tsx`、`client/src/pages/ReservationCreatePage.tsx`、`client/src/pages/MyReservationsPage.tsx`、`client/src/pages/ReservationDetailPage.tsx`

## 【前提条件】全モジュールビルドが成功していること

* U001〜U004が完了していること。

## 【使用するテストデータ】

* 一般ユーザー `E0001`、会議室A、未来日1件分の予約データ

## 【事前準備】

* `E0001` でログインする。

## 【実行手順】

1. S03で予約を作成する。
2. S02のカレンダーに予約が反映されていることを確認する。
3. S05のマイ予約一覧(upcoming)に予約が表示されることを確認する。
4. S04で予約の件名を編集し保存する。
5. S02のカレンダーに変更後の件名が反映されていることを確認する。
6. S04で予約を取消す。
7. S02・S05の両方から予約が消えていることを確認する。

## 【実行コマンド】

* `cd server && uv run pytest tests/acceptance/test_a002_reservation_lifecycle.py -v`

## 【期待結果】

* 各手順が期待どおりの画面状態・API応答になること。

## 【合否判定基準】

* 7手順すべてが期待結果どおりであればPASS。

## 【失敗時に記録する内容】

* どの手順で期待と異なったかを具体的に記録する。

## 【修正禁止事項】

* アプリケーションコードを修正しないでください。

## 【次タスクへ進む条件】

* 記録完了後、A003の指示を待つ。

## 重要:

* アプリケーションコードを修正しないでください。
* テスト失敗時に、その場で修正して再テストしないでください。
* テスト失敗時は、失敗内容をテスト記録に残してください。
* 次のテストタスクには進まないでください。
