あなたはReviewer Loop(実装横断レビュー担当)です。以下の1テストタスクだけを実施してください。

# 【テストID】A005

## 【目的】

* ログイン中のユーザーが管理者によって無効化された場合、既存セッションがその場では削除されないが、次回以降のアクセス時に401となり、その時点でセッションが削除される(遅延失効方式、`docs/P003-backend-spec.md` §2)ことを確認する。sprint1の認証機能とsprint2のユーザー無効化機能をまたぐ確認。

## 【参照テスト計画】

* `docs/P006-test-plan.md` §2.1(S07行「受入/システム」列)

## 【対象モジュール】

* `server/app/services/auth_service.py`(`resolve_current_user`)、`server/app/services/user_service.py`(`deactivate`)

## 【前提条件】全モジュールビルドが成功していること

* U001・U002が完了していること。

## 【使用するテストデータ】

* 一般ユーザー `E0003`(新規、ログイン済みCookieを持つ状態にする)

## 【事前準備】

* `E0003` でログインしCookieを取得する。

## 【実行手順】

1. 管理者が `DELETE /api/users/{E0003のid}` で `E0003` を無効化する。
2. `E0003` のCookieのまま `GET /api/me` を呼ぶ。
3. 手順2の直後、同じCookieで再度 `GET /api/me` を呼ぶ。

## 【実行コマンド】

* `cd server && uv run pytest tests/acceptance/test_a005_deactivated_user_session.py -v`

## 【期待結果】

* 手順2: `401 AUTH_REQUIRED`(このタイミングでセッションが削除される)。
* 手順3: `401 AUTH_REQUIRED`(セッションが既に削除されているため)。

## 【合否判定基準】

* 両手順が期待結果どおりであればPASS。

## 【失敗時に記録する内容】

* 両手順の実際のステータスコードを記録する。

## 【修正禁止事項】

* アプリケーションコードを修正しないでください。

## 【次タスクへ進む条件】

* 記録完了後、A006の指示を待つ。

## 重要:

* アプリケーションコードを修正しないでください。
* テスト失敗時に、その場で修正して再テストしないでください。
* テスト失敗時は、失敗内容をテスト記録に残してください。
* 次のテストタスクには進まないでください。
