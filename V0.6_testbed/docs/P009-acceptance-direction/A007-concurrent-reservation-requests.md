あなたはReviewer Loop(実装横断レビュー担当)です。以下の1テストタスクだけを実施してください。

# 【テストID】A007

## 【目的】

* 同一会議室・同一時間帯に対して複数のリクエストを同時発行した場合、成立するのは1件のみであり、二重予約が発生しないことを確認する(`docs/P003-backend-spec.md` §5の排他制御の検証)。

## 【参照テスト計画】

* `docs/P006-test-plan.md` §3(負荷・同時実行)

## 【対象モジュール】

* `server/app/repositories/reservation_repository.py`(`insert_with_participants`、`BEGIN IMMEDIATE`)

## 【前提条件】全モジュールビルドが成功していること

* U003が完了していること。

## 【使用するテストデータ】

* 同一会議室・同一日時(例: 会議室A、未来日、10:00-11:00)への予約リクエストを10件、別々のユーザーから同時に発行する。

## 【事前準備】

* 10名分の一般ユーザーでそれぞれログインしCookieを取得する。

## 【実行手順】

1. 10件の `POST /api/reservations` リクエストを、同一の会議室・日時に対してほぼ同時に(スレッドまたは非同期タスクで並行して)発行する。
2. 結果を集計する。

## 【実行コマンド】

* `cd server && uv run pytest tests/acceptance/test_a007_concurrent_requests.py -v`

## 【期待結果】

* 10件中1件のみ `201 Created`、残り9件は `409 RESERVATION_CONFLICT`。
* DBに二重に予約が登録されていないこと。

## 【合否判定基準】

* 期待結果どおりであればPASS。1件も成立しない、または2件以上成立した場合はFAIL。

## 【失敗時に記録する内容】

* 各リクエストの実際の結果、DB上の登録件数を記録する。

## 【修正禁止事項】

* アプリケーションコードを修正しないでください。

## 【次タスクへ進む条件】

* 記録完了後、A008の指示を待つ。

## 重要:

* アプリケーションコードを修正しないでください。
* テスト失敗時に、その場で修正して再テストしないでください。
* テスト失敗時は、失敗内容をテスト記録に残してください。
* 次のテストタスクには進まないでください。
