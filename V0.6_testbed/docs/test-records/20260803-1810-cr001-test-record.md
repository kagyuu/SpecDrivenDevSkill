# CR-001 対応テスト実行記録 — 2026-08-03 18:10

> 本ファイルはRefactor(P901〜P905)のP903(修正実施)で実施した単体テスト・全体回帰テストの実行記録。
> `docs/P007-impl-direction/U005-cr-001-all-day-checkbox.md` にもとづき実装した「終日」チェックボックス(CR-001)を対象とする。

## テスト環境

* フロントエンド: `client/tests/`(Node.js 22標準搭載の `node --test`)。
* バックエンド: `server/tests/`(`unittest`、`docs/P101-impl-context.md` のdeviationに準じる)。CR-001はバックエンドの変更を伴わないため、既存テストを対象に回帰確認のみ実施。

## テスト結果一覧

| 対象 | 実行コマンド | 結果 | 備考 |
| --- | --- | --- | --- |
| `getAllDayDefaults` が `{startTime:"09:00", endTime:"18:00"}` を返す | `cd client && node --test tests/reservationCreatePage.test.mjs` | PASS | 新規追加(U005-1)。 |
| 終日既定値が既存の終了>開始バリデーションを満たす | 同上 | PASS | 新規追加(U005-1)。`validateReservationInput` は無変更。 |
| 終日既定値をsubmitした際、`start_time`/`end_time`のみがAPIペイロードに含まれ`all_day`は含まれない | 同上 | PASS | 新規追加(U005-1)。`docs/P002-frontend-spec.md` §4.11備考のとおりAPI契約は無変更であることを確認。 |
| フロントエンド全体回帰(既存35件 + 新規3件) | `cd client && node --test tests/*.test.mjs` | PASS(38/38) | 他画面(S01/S02/S04〜S07)への影響なし。 |
| バックエンド全体回帰(121件) | `cd server && python3 -m unittest discover -s tests -p "test_*.py"` | PASS(121/121) | CR-001はサーバー側変更なしのため、変化なしを確認する目的のみで実施。 |

## 手動確認(DOM描画部分、`docs/client/INDEX.md`のdeviation方針により自動テスト対象外)

* `mountReservationCreatePage` が生成するフォームに「終日」チェックボックス(`name="all_day"`)が表示されることをソースコードレビューで確認した(`client/js/pages/reservationCreatePage.js`)。
* ブラウザでの実機確認は本セッションでは実施していない(★FIXME★、サンドボックス環境にGUIブラウザがないため。次回、実機/結合環境での目視確認を推奨する)。

## 総合判定

* CR-001の「期待する振る舞い」(`docs/CR.md` CR-001参照)は、上記の単体テスト3件で機械的に検証できた。DOM描画・イベント配線部分は本プロジェクトの既存方針にもとづきコードレビューでのみ確認しており、実ブラウザでの目視確認は未実施(★FIXME★)。
